# LingxiAgent 发布版本与 Artifact 顺序规范

## 三个数字各管一件事

| 字段 | 回答的问题 | 规则 |
| --- | --- | --- |
| 产品版本 | 用户看到的是哪个版本 | 必须是标准 SemVer，并严格高于所有可能到达用户设备的历史 Release 与 prerelease |
| 发布世代 | 哪次源码发布更新 | 正整数、全产品单调递增；安装包 seed 与所有频道 Train 共用 |
| Train | 某一频道更新了几次 | 只在 stable 或 beta 自己的频道内单调，不得代替发布世代 |

来源提交只用于证明产物来自哪次源码，不参与大小排序。

## 运行时选择规则

1. 连续崩溃触发的 previous 回退最优先，任何新鲜度判断都不能立刻覆盖它。
2. 同产品版本或同内容哈希视为已经生效，不重复替换按版本命名的目录。
3. 新旧两侧都有发布世代时，只按发布世代决定先后。
4. 新清单第一次遇到没有发布世代的旧指针时，新清单接管并完成迁移。
5. 指针已经携带发布世代后，缺少发布世代的旧 seed 或 Train 一律不能覆盖。
6. 两侧都没有发布世代时，继续按标准产品版本读取旧数据；历史跨版本体系残留沿用既有安全兼容规则。

renderer 与 server 继续独立回退。两侧发布世代不一致时不静默：启动日志必须明确告警并列出两侧来历。

## 发布步骤

1. 先确定候选产品版本和新的发布世代。
2. 运行 `npm run release:preflight -- --tag vX.Y.Z`。
3. 生成并提交 `release-digest.v1.json`，再通过仓库生成器写入 `release-digest.v2.json`。
4. 运行类型检查、代码检查、工作区包构建、全量测试和 Artifact release smoke。
5. 只有所有前置任务成功，tag 工作流才能创建 Release；Train 只能从已完成的源 Release 发布。

正式发布前禁止仅修改 tag、文件名或 About 页版本来“修复”版本事故；这些变化不能改变客户端实际启动哪个 Artifact。
