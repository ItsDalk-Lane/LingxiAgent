# P1 证据：F9 来源分离配额 + F3 无损迁移（2026-09-07）

## 改动文件

| 文件 | 改动 |
|---|---|
| `lib/memory/tenets.ts` | 新增 `countActiveTenetsBySource`；`decideTenet` 只计 active/model_proposed（20）；`addTenetDirect` 只计 active/user_direct（200）；`isTenetError` 传 code 时精确匹配；新增 `TENET_ERRORS.STORE_*` 三码；新增 `readTenetsFileStrict`（ENOENT 才按空库，损坏/未来 schema/EACCES 抛错），`withFile` 写边界改走严格读；新增 `serializeTenetsFile`（确定性字节配方）；新增 `legacyContentKey`/`planLegacyPinnedImport`/`importLegacyPinnedItems`（迁移批量接纳：豁免 300 字符/200 条、精确内容去重、pending/rejected 历史保留、createdAt 合法保留、override 支持确定性重放）；`addTenetDirect` 遇 pending/rejected 重复改为产生新 active 条目 |
| `core/pinned-tenets-migration.ts` | 全部重写：旧版 mtime 权威规则（md.mtimeMs > json.mtimeMs+1 → md，否则 json，含合法空 items 删除语义）；权威损坏/目标损坏明确失败不回退；收据状态机 prepared→target_committed→sources_archived→completed，failed 可重试、conflict 粘性；内容寻址本地备份（memory/pinned-migration-backup/<sha8>-<file>，不覆盖）；提交前复查源/目标哈希；原子提交+提交后验证；归档不覆盖既有 .migrated（带 sha8 后缀）；逐项映射只记哈希/id；completed 不复活删除 |
| `core/pinned-tenets-recovery.ts` | 新增（P1.7）：`scanPinnedTenetsRecovery`（dry-run，present/inactive/similar/missing 分类）+ `applyPinnedTenetsRecovery`（批准清单驱动，走 importLegacyPinnedItems + recovery 收据，幂等，拒路径穿越/未知源/未知哈希/空决策） |
| `scripts/pinned-tenets-recovery.mjs` | 新增 CLI：默认 dry-run；`--apply --approval <file>` 才恢复 |
| `lib/tools/pinned-memory.ts` | pin_memory 写入失败改走 `toolError` 契约（isError + details.errorCode），删除「已添加 (storage full)」假成功分支 |
| `shared/persistence/store-registry.ts` | agent-memory 新增两个 pathPatterns（收据/备份目录）+ 迁移站点的 copy-file/atomic-write 登记 + 恢复站点登记 |
| `build/persistence-store-inventory.json` / `persistence-startup-receipt.json` | 派生清单重生成（scan-persistent-stores.mjs，751 站点） |

## 测试（全部通过，2026-09-07 darwin arm64）

- tests/tenets-source-quota.test.ts（14 项，M15–M19 + 直钉遇 pending/rejected 语义）
- tests/pinned-tenets-migration.test.ts（24 项，M01–M14、M20、既有 4 项保留/修正）
- tests/pinned-tenets-migration-recovery.test.ts（7 项，M13 + apply 边界）
- tests/memory-tenets.test.ts（13 项；「active 上限」断言按 M15 deliberate 修改，注释留痕）
- 邻接回归：agent-system-prompt-equivalence / tool-invocation-permission / agent-system-prompt-section-order / agent-master-session-decoupling（50 项）、persistence 四件套（35 项）
- CLI 冒烟：dry-run 零写入 ✓；apply 恢复 1 条 ✓；重复 apply restored=0 ✓（合成数据）
- `npm run typecheck`：0 错误

## 既有断言调整记录（任务书要求 deliberate）

1. tests/memory-tenets.test.ts「active 上限」：旧断言「active 总数满 20 拒批」验证的是来源混算缺陷；改为 M15「20 条 user_direct 不占 model_proposed 配额」。
2. tests/pinned-tenets-migration.test.ts 原「与既有 tenets 重复的 pin 跳过」用带句尾句号内容断言合并（宽规则）；改为 M07（精确重复合并+映射）与 M09（标点差异不合并）。

## 限制与遗留

- M05c（EACCES）在 win32 跳过（chmod 语义不同），darwin/linux 已验。
- 迁移失败仍不阻塞启动（沿用 runBestEffortStartupMigrationStep 语义），但失败有收据可查。
- 真实用户数据未触碰（全部测试在 os.tmpdir() 合成 home 下进行）。
