# 架构契约补强执行记录

- 基线：`e0afc0dc3b94bc9763d715f981d6f948d1c39c27`。
- 工作区：独立 detached worktree，未授权提交、推送或发布。
- 范围：输入接受/取消，记忆迁移/恢复，媒体持久化/尝试/终态，语音取消/参数/识别，MCP 连接归属，文件版本与历史展示。
- 方法：先固定实际模块反例并记录旧实现失败，再实现最小补强，执行受影响验证及本地完整门禁。
- 边界：只使用合成数据；不操作真实用户目录，不调用真实供应商，不改变权限，不恢复已撤回功能。

## 分工与状态

| 范围 | 负责人 | 状态 | 证据 |
| --- | --- | --- | --- |
| 输入接受、取消与异步插入 | input_lifecycle | 完成 | input.md |
| 记忆迁移、恢复与收据路径 | migration_contract | 完成 | migration.md |
| 媒体存储、尝试、终态及关闭 | media_lifecycle | 完成 | media 系列日志 + REPORT.md |
| MCP 连接归属、文件版本与历史 | 主代理 | 完成 | mcp-presentation.md |
| 语音适配、识别、原生协调器 | 主代理 | 完成（原生 22/22） | speech-red.log + REPORT.md |
| 集成门禁（tsc×3 / eslint / renderer build / 全量测试） | 主代理 | 完成（仅 4 个基线预存红） | REPORT.md |

## 验收要求

已接受证据不能由传输或完成事件替代；未知不能当未接受。删除的记忆不复活；收据不确定窗口保留原件并显式冲突。旧任务或旧连接不能污染新尝试。完成须有可交付产物及可靠记录。实时与历史保留同样的文件与版本。验证成功仅覆盖实际执行的平台与输入。

最终记录必须区分通过、失败、未执行；旧审计封印只约束已提交版本，不作为本次工作区正确性证明。
