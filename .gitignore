# ═══════════════════════════════════════════
# 用户数据（已迁移到 ~/.hanako/）
# ═══════════════════════════════════════════
agents/
user/
!skills2set/hana-plugin-creator/agents/
!skills2set/hana-plugin-creator/agents/**
!skills2set/office-documents/agents/
!skills2set/office-documents/agents/**

# ═══════════════════════════════════════════
# 文档 / 参考文件
# ═══════════════════════════════════════════
.docs/
CLAUDE.md
AGENTS.md
docs/superpowers/
.superpowers/
archived/
devlog/
research/
handoff-*.md
TODO.md
# 各轮迁移/重构任务书文件夹（本地规划材料，不入库）
/Lingxi_*_Taskbooks_*/

# ═══════════════════════════════════════════
# AI 生图原始素材
# ═══════════════════════════════════════════
**/jimeng-*.png

# ═══════════════════════════════════════════
# 构建产物
# ═══════════════════════════════════════════
dist/
dist-server/
dist-server-open/
dist-server-artifact/
bundled-bin/
dist-renderer-artifact/
dist-standalone/
desktop/dist-splash/
dist-server-bundle/
dist-computer-use/
dist-speech/
dist-open-export/
desktop/main.bundle.cjs
desktop/preload.bundle.cjs
# build/ 下的构建临时产物（被跟踪的是配置类 json/nsh/plist，产物不入库）
build/.cli-closure-*
build/*.mjs
.cache/
# 编辑器/同步工具生成的同名副本（"file 2.ts" 之类）
*[ ][0-9]*.*
desktop/dist-renderer/
desktop/native/**/.build/
vendor/
inbox/

# 运行时 / 依赖
# ═══════════════════════════════════════════
node_modules
.pi/

# IDE / 工具
.claude/
.agents/skills/
*.code-workspace

# 项目私有文件
CLAUDE.md

# 临时文件
tmp/
test-results/
__pycache__/
*.pyc
/index.html
homepage/
desktop/src/react/animation-mockup/

# 环境变量 / 凭证
.env
.env.*
*.pem
*.key
*.p12
*.crt
credentials.json
secrets.yaml
secrets.json
auth.json

# 日志
*.log
logs/
# F1-F12 修复轮交付证据例外（C03）：round2/round3 审计日志是测试依赖的交付物，
# 随仓库分发；先重包含目录再重包含文件，其余运行日志继续按上面规则忽略。
!artifacts/f1-f12-repair/round2/logs/
!artifacts/f1-f12-repair/round2/logs/**
!artifacts/f1-f12-repair/round3-c01-c03/logs/
!artifacts/f1-f12-repair/round3-c01-c03/logs/**
# refactor-2026 独立修复轮交付证据例外（R3）：EVIDENCE_SHA256.txt 引用的原始
# 先红后绿/回归/打包日志必须在干净 checkout 可复核；已逐文件脱敏审计（见
# recheck-r1-r2/redaction-audit.md），无密钥与真实用户数据。仅放行该证据树。
!artifacts/refactor-2026/independent-fix/**/*.log

# 系统文件
.DS_Store
*.db

# Codex reviews
reviews/
.superpowers/

# 本地工具工作台状态（会话记忆类，不入库也不入证据 manifest）
.workbuddy/

# Codex environments
.codex/

# ZCode CLI working dir
.zcode/

# Reasonix 本地 agent 状态
.reasonix/

# Mimosa 安全扫描本地状态（finding-ledger / 扫描历史 / hook 状态，208MB+，不入库）
.mimosa/

# Playwright MCP 浏览器工具产物（控制台日志 / 截图 / 页面快照）
.playwright-mcp/

# GitGraphPanel 开发期设计稿（实现已入库，mockup 留本地）
/git-graph-panel-mockup*.html
desktop/native/LingxiSpeechPermissions/build/
