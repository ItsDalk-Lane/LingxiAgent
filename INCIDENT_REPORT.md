# LingxiAgent 0.1.3 发布事故报告

## 摘要

`0.1.3` 在 `0.1.22` 之后发布，但标准版本比较会认定 `0.1.3 < 0.1.22`。已经运行过 `0.1.22` 的设备因此拒绝安装包内的新版组件，也拒绝线上 `0.1.3` 更新列车，最终继续启动磁盘上的旧组件。

修复候选为 `0.1.23`。它既在产品版本上严格高于所有已发布版本，也首次携带独立、已签名的发布世代 `1`，用于统一安装包与在线更新的先后顺序。

## 用户症状

- 安装 `0.1.3` 后，模型设置页、辅助模型分工和本地模型能力仍像旧版本。
- About 页或安装包文件名显示新版本，但实际功能没有切换。
- 再次安装、切换稳定或测试通道都不能让受影响设备自然恢复。

## 三层事实

### Source state

Git 当前提交包含供应商与模型设置、六类辅助模型、审批收口、本地模型能力、技能消息、记忆和上游 `0.444.1` 的改动。三方同步审计见 `UPSTREAM_SYNC_AUDIT.md`。

### Packaged state

GitHub `v0.1.3` 的 renderer 与 darwin-arm64 server 归档已抽检：哈希与已签名更新列车一致，归档内能找到上述新版功能。也就是说，新代码确实被装进了发布包。

### Active runtime state

启动时会先读取用户目录中的当前指针，再比较随安装包到达的 seed。事故版本只按产品版本排序，所以输入 `current=0.1.22`、`seed=0.1.3` 的真实决策是继续启动 `0.1.22`。renderer 与 server 使用独立指针，二者都可能留在旧目录，也可能形成混合状态。

## 根因

产品版本从 `0.1.22` 错误回退到 `0.1.3`，而安装包 seed、在线更新客户端和发布列车都缺少跨通道统一的发布世代；一端允许错误版本发布，另一端按标准版本规则正确拒绝“降级”，共同把旧组件锁成了实际运行版本。

## 为什么测试没有挡住

- 原有启动测试只覆盖“旧安装包不得覆盖新指针”，把 `0.1.3 < 0.1.22` 当作正常降级保护；没有“产品版本误编号后由更高发布世代修复”的场景。
- 原有在线更新测试覆盖列车号单调和产品版本不倒退，但发布脚本只检查列车号，没有检查候选版本是否高于历史安装包。
- 原有 tag 构建工作流没有把版本预检、全量质量检查和历史升级 smoke 作为创建 Release 的真实前置依赖。

## 为什么 Release 仍然发生

`v0.1.3` tag 能触发四平台构建，平台构建能生成并验签 seed，但发布前没有枚举历史产品标签并做标准版本比较。发布列车只为 stable/beta 各自分配更大的 train，也没有阻止产品版本从 `0.1.22` 降到 `0.1.3`。因此安装包、Latest 和两个频道指针都能在各自局部规则下成功写出。

## 影响范围

- renderer：模型页、供应商页、用量页、辅助模型设置及相关交互继续使用旧代码。
- server：模型解析、审批、技能消息、记忆、Ollama 参数和上游修复继续使用旧代码。
- 双组件：两侧独立回退可能形成版本不一致，过去日志无法一次看清壳、renderer、server 的真实来源。
- 更新：已经进入 `0.1.22` 的设备同时拒绝 `0.1.3` seed 和 `0.1.3` Train，普通重装无法恢复。

用户的会话、代理、设置和历史回滚目录不需要删除，本次迁移只改组件激活指针。

## 修复

### Version

- 候选产品版本改为 `0.1.23`。
- 根包与锁文件版本保持一致。
- 新增发布前检查：tag、根包、锁文件、历史最高标准版本和发布世代任一不一致即失败。

### Artifact 与迁移

- 签名清单新增可选的 `releaseGeneration` 与 `sourceCommit`；旧清单仍可读取，新构建必须提供。
- 指针和验收回执保存产品版本、发布世代、来源提交、发布时间、内容哈希和来源类型。
- 两侧都有发布世代时只按世代排序；新世代首次遇到旧指针时接管；进入新规则后，缺少世代的旧 seed/Train 不得倒灌。
- 同版本或同内容保持幂等；连续崩溃回退始终优先，不会被 seed 新鲜度立即顶回故障版本。
- OTA 与安装包 seed 复用同一激活路径和同一发布顺序。

### 可观测性与混合运行时

启动日志现在一次报告壳、renderer、server 的产品版本、发布世代、train、哈希、槽位、来源、来源提交和发布时间。两组件发布世代不同，或一侧仍是旧元数据时，会明确写兼容性告警；独立回退能力保留。

### 发布防复发

- 版本预检会读取所有已获取标签，只把包名、tag、标签内版本三者一致的 Lingxi 历史算入比较；它先于 renderer 和四平台构建。
- 两份发布摘要也在最早预检阶段校验，缺摘要时不会开始昂贵构建，更不会创建远端草稿。
- Release 必须等待全量类型检查、代码检查、工作区包构建、全量测试、平台 seed 校验，以及 Artifact bootstrap/历史升级 smoke。
- 自动与手工 Train 发布都重新执行版本预检；脚本本身还会比较现有签名频道指针，拒绝发布世代倒退或复用。
- 预发布版本按标准版本规则识别，不会再被自动标成 Latest 正式版。

## 验证结论

- Node 24.15.0 全量测试：1088 个文件通过、1 个按既有设置跳过；11016 项通过、7 项跳过、0 项失败。
- Artifact release smoke：8 个文件、304 项全部通过；覆盖签名元数据、启动、OTA、构建、Train 和历史迁移。
- 工作区包与完整客户端构建通过；darwin-arm64 独立 server 构建、临时签名 seed 验签通过，解包后的真实 server 启动并以 200 响应身份接口，包内版本为 `0.1.23`。
- 真实事故迁移覆盖旧 `0.1.22` 与旧 `0.1.3` 两种 current：安装 `0.1.23` 后，renderer/server 均切到 `0.1.23`、发布世代 `1`、来源 `seed`。
- 干净安装、同版重装、旧 Train 倒灌、混合状态告警和崩溃回退均有正反用例。

## 发布摘要状态

`release-digest.v1.json` / `release-digest.v2.json` 已由仓库生成器通过 DeepSeek Responses API 与 `deepseek-v4-flash` 生成，没有手工修改条目。真实响应中 reasoning 顺序、输出额度和默认思考造成的兼容问题均已用失败证据和测试修复。由于 `v0.1.23` tag 的 Linux 质量夹具失败且未创建 Release，最终摘要目标为严格更高的 `v0.1.24` / generation 2；两份文件校验均退出 0。
