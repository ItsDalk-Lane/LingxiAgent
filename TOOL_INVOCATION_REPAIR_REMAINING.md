# 契约执行路径不变量剩余事项

## 源代码

源代码剩余事项：`none`

P0–P11 的实施范围内没有已知未修复发现项，也没有保留兼容性 raw executor 旁路。

## 最终验证

P12-01 至 P12-05 尚待在冻结的源码候选 SHA 上执行。这是任务书规定的下一阶段，不是源码缺口。若任一最终门禁失败，必须形成新的源码候选并从 P12-01 全部重跑，不能在封印提交中夹带修复。

## 已知流程偏差

- P10-02 新增完整配置矩阵首次运行即为 `12/12`，未能复现新的旧代码红灯；原因是对应行为已经由严格在前的 P5、P9 和 P10-01 修复。该事实保留为 `completed_with_red_not_reproduced`，没有删除、跳过、放宽测试或人为破坏实现。
- 固定基线的全量测试和无签名服务构建存在已归因红灯；最终状态必须以 P12 在源码候选上的新结果为准。

## 平台边界

当前执行平台为 macOS。任务书 P12-03 要求的服务端、开放服务端、当前平台客户端和 seed-kit 门禁必须执行；未要求在本机伪造 Windows/Linux 专属打包结果。

