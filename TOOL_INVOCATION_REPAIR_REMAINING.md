# 契约执行路径不变量剩余事项

## 源代码

源代码剩余事项：`none`

P0–P11 的实施范围内没有已知未修复发现项，也没有保留兼容性底层执行器旁路。

## 最终验证

P12-01 至 P12-05 尚待在冻结的源码候选 SHA 上执行。这是任务书规定的下一阶段，不是源码缺口。若任一最终门禁失败，必须修复后形成新的源码候选，并从 P12-01 全部重跑，不能在封印提交中夹带修复。

## 已知流程偏差

- P10-02 新增完整配置矩阵首次运行即为 `12/12`，未能复现新的旧代码红灯；原因是对应行为已由严格在前的 P5、P9 和 P10-01 修复。该事实保留为 `completed_with_red_not_reproduced`，没有删除、跳过、放宽测试或人为破坏实现。
- v0.1.34 固定基线全量测试因旧审计封印有 1 项失败；无签名变量的服务构建也按原始失败留痕。最终状态必须以 P12 在源码候选上的新结果为准。

## 平台边界

当前执行平台为 macOS。任务书 P12-03 要求的服务端、开放服务端、当前平台客户端和 seed-kit 门禁必须执行；未要求在本机伪造 Windows/Linux 专属打包结果。
