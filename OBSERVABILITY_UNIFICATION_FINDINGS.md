# 模型调用统一实施事实

## 已确认基线

- 当前分支 `feat/pending-sep01`，HEAD `84cbcc6b`，开始实施前工作树干净。
- Node `24.16.0`。
- 相关九个测试文件共 166 项在实施前全部通过。
- 现有轨迹详情会按时间窗口把会话助手消息与观测调用配对；新关联必须消除该猜测。
- 会话管理器原生支持不进入模型上下文的 custom entry，且订阅者收到 `message_end` 时，助手消息尚未由 SDK 落盘，可先写关联条目。
- 现有观测数据库 schema 为 v3；新增来源快照需要 additive v4。
- 浏览器文件上传只接受图片和音频，文件选择器也未声明视频；桌面发送和服务端 WebSocket 已有视频骨架。
- 当前视频传输已声明 Gemini inline data 与 DashScope、Moonshot、MiMo 的 `video_url` 兼容路径。
- 模型观测默认总开关关闭，正文和媒体关闭；设置页仍暴露四个开关。
- 全局新建聊天明确重置到主助手的默认工作台，现有测试还断言忽略当前会话目录。
- 调用查询层是独立只读连接，不能在读取详情时顺手回写来源；来源快照必须经持久化写侧更新。
- 轨迹列表当前只聚合 `traces.origin`，没有选取因果根调用；需要把根调用一并投影后解析来源。
- 模型调用与助手消息的关联目前只有进程内对象映射；会话订阅收到助手 `message_end` 时，SDK 尚未写入助手消息，正好可以先写隐藏关联条目。
- 历史加载层已经保留多类隐藏 custom entry，可在同一层把关联条目附到其后第一条助手消息并过滤掉隐藏条目。
- 用量投影已经按调用编号幂等关联，因此统一界面无需再创建第二条可见记录，只需把用量作为调用的补充字段。
- 低层持久化策略仍保留可注入开关，供故障隔离和存量专项测试使用；产品偏好与设置接口已经收敛为始终全开，避免把运行降级误当成用户关闭。
- 视频的供应商传输骨架原本已经完整，缺口主要在浏览器上传、供应商格式交集和三段式大小校验；无需把视频伪装成图片类型。
- schema v4 已按加法方式落地；旧调用不改写，来源名称通过当前实体与不含正文的快照补充。
- 真实界面宽屏与窄屏验收确认：来源为业务标题加类型标签；调用详情没有逐项加载按钮，长载荷默认预览并可展开，技术信息默认收起。
- 四类供应商的常见环境变量均未发现凭据，因此真实付费视频调用均为 `NOT_EXECUTED`，没有用模拟请求冒充线上通过。

## 必须保持的事实语义

- 原始 `unknown`、未捕获、损坏、过期等状态不能被改写成确定事实；业务来源显示是新增投影，不覆盖底层原值。
- Usage Ledger 只是用量投影，不是第二份正文或调用真理源。
- 历史未捕获正文不回填、不伪造。

## 最终验证事实

- 工作区四个包构建通过。
- 三组类型检查通过。
- 全量测试：1270 个测试文件通过、1 个跳过；12882 个用例通过、7 个跳过。
- 开放边界门禁通过；持久化指纹已推进为兼容变更，全量测试中的持久化 tripwire 通过。
- `git diff --check` 通过；未提交、未推送、未推进审计封印。
