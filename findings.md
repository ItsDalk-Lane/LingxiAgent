# Findings & Decisions

## Requirements

- 用户明确要求：不要创建新的工作树和分支，就在当前工作树和当前状态执行。
- 任务主题：Codex Model Observatory Phase 10.1 对抗性真实性修复。
- 已知总目标：修复 Phase 1～10 完成树中暴露的数据真实性、查询语义、Blob 原子性、动态生命周期和发布证据闭合问题，然后重新执行发布验收。
- 本轮是修复轮，不是新增功能。

## Research Findings

- 当前路径：`/Users/study_superior/.codex/worktrees/4dcf/LingxiAgent`。
- 当前 Git 状态显示 `HEAD (no branch)`，没有已显示的文件改动；不得据此擅自创建或切换分支。
- 用户任务书共 2503 行，必须完整读取后才能确定全部范围。
- 相关历史记忆强调：审查与修复边界不能混淆；测试、退出码、运行时版本和发布证据必须保持真实。
- 任务书明确要求重新建立审计坐标，记录开始提交、主线提交、已验证源码提交，并验证 `.sync-audit/verified-source-sha.txt` 指向 `commit`。
- 既有发布验收文档中的“无高优先级问题/可发布”只能当待复核证据，不能当真相来源。
- A1：生产界面目前仍发送固定时差；必须优先发送浏览器解析出的真实时区，并用跨夏令时日期的完整纵向链测试验证。
- A2：夏令时切换查找只比较区间两端会漏掉中间发生、最后恢复原时差的切换；必须采用有上限的步进扫描并在发现变化后精确定位。
- A3：切换段数超过上限时不得中断循环后继续返回错误结果，必须显式失败。
- Q1/Q2：同一个多值筛选字段内部必须求并集，不同字段之间才求交集；终态和载荷可用性当前实现存在互斥条件同时成立的问题。
- Q3：读取模型中的数据库空值、空字符串和非法数字必须保持为空，不能经数字转换伪装成零；真实零和小数成本仍需保留。
- Q4/Q5：用量统计不可用、完整性读取失败或数据损坏时必须显示未知或降级，不能显示真实零；界面和导出都要保留空值及覆盖状态。
- Q6：缺少用量记录不等于明确无法关联；只有运行时留下的明确事实才能标记“无法关联”，其余情况需区分投影不可用和未知，必要时升级数据库版本。
- Q7：筛选应决定哪些调用链入选，但入选后的统计必须覆盖整条调用链的所有调用。
- Q8：只要预期载荷中有一部分明确丢失，整体可用性就必须优先标为丢失，即使仍有其他载荷可查看；映射和筛选必须共享一套语义。
- 持久化队列中的载荷若因序列化、硬上限或不支持的形状写入失败，除累计健康计数外，还必须给对应调用留下逐条丢失事实。
- P1：Blob 文件写入和数据库提交必须拆成两个阶段；数据库重试只能重试数据库事务，不能再次写文件，更不能用已释放的空字节覆盖成功文件。
- P2：数据库事务里的内存健康计数必须先记局部增量，仅在事务成功后应用一次；回滚和重试不能重复累计。
- P2：批次最终失败后不能只在内存里留下计数；需要持久化待办标志，使定时维护或关闭流程在数据库恢复后补记，同时继续诚实报告降级。
- P2：写文件失败的描述项不能产生悬空引用；只有确认文件持久存在的标识才能进入引用表，失败描述不能保留文件标识。
- P4：动态重新配置是发布阻断项。设置变化只影响新开始的调用；已经开始的调用必须继续绑定旧一代存储直到结束，再在有界等待后关闭，不能截断、重复或把一次调用前后套用不同策略。
- P5：安装 Blob 外置器前必须保存此前注册对象；卸载时仅在当前注册仍是自身时恢复旧对象，不能覆盖后来接管者。
- BlobStore 内部读、删、缺失恢复都必须从合法 Blob 标识重新计算受控目录中的路径；数据库相对路径只能作为历史信息，不能成为删除权限。
- 现有分片取标识开头导致所有新文件落在同一目录；若本轮修复必须兼容旧路径，否则需明确列为 P2 backlog，不能继续声称已有有效分片。
- 最大 64MB Blob 的下载必须流式输出，响应头和本地访问边界保持不变；验收要证明读取时事件循环仍能推进，而不只是“能读成功”。
- 查询、调用链选择、聚合与导出必须共享终态和载荷可用性的同一套多值筛选语义，并用表驱动组合测试覆盖。
- 数据损坏不得通过异常兜底伪装成零、空数组或正常状态；能识别损坏的层必须表达损坏、降级或未知。
- 旧发布验收证据不得删除或改写成事后正确；应在旧报告开头标注已被 Phase 10.1 取代，或新建 V2，并保留当时遗漏与后来发现之间的真实时间线。
- Phase 10 尚未独立闭合的并行工具、子代理、两类特殊调用、审批修复、记忆提示、后台上下文、保留策略、垃圾回收、队列溢出、写失败、安全边界、三个详情纵向链和导出纵向链，都必须链接具体测试与真值判断；缺少独立见证时必须明确原因与替代判断来源。
- 动态重配至少覆盖四类在途切换：仅元数据到载荷、载荷到仅元数据、启用到禁用、启用策略甲到策略乙。
- 涉及数据库、文件、流式读取和桌面导出的跨平台改动需要现有持续集成覆盖；未执行的平台必须如实写未执行，不能推断通过。
- 故障注入必须通过正式依赖边界实现，禁止使用“测试环境下改变生产语义”的分支；重试完整性需覆盖首次事务失败后成功、最终失败、计数不重复、引用不悬空、关闭时补记健康状态。
- 若显式用量关联状态无法在现有数据库版本中安全表达，才升级至 v3；升级必须闭合 v2→v3 单事务迁移、旧版本读取决策、未知新版本、回滚、持久化指纹、注册表、读取服务、查询和导出兼容。
- 修复后的未知、丢失、明确未关联、部分用量和空数值必须同步进入导出；默认仍只导出元数据，显式选择才含受控载荷，永不导出原始内容或 Blob 字节。
- 所有相关界面都要显示真实状态：未知数字显示破折号，部分覆盖显示告警，部分载荷丢失显示降级，只有明确运行时事实才显示无法关联，投影不可用单独呈现。
- 发布验收 V2 必须输出十类四态矩阵：调用场景、用量关联、载荷真实性、调用链真实性、查询真实性、缺失状态、重试持久化、动态重配、安全、跨平台。
- 必须新增包含 AR-01～AR-20 的对抗性发现台账，每条记录级别、修复前、失败测试、根因、修复、修复后和剩余限制。
- 最终验证顺序固定为：先失败测试与分域修复，再补独立场景，随后相关套件、类型检查、两类代码规则检查、持久化扫描与指纹、命令行闭包、多语言校验、全量测试、三个构建、打包冒烟、跨平台持续集成、V2 报告和源码提交封印。
- 功能修复提交和审计封印提交按任务书应分开；但当前用户没有明确授权提交或推送，先完成代码与验证，提交动作到边界前再依据现状和授权判断，绝不自行推送或发布。
- 最终硬验收要求高优先级问题归零、全量测试零失败、三个构建及打包冒烟通过；任何剩余中低优先级问题必须明确记录。
- 成功定义是各类状态从运行时、持久化、查询、网络响应、界面到导出保持原义，多值筛选保持同字段并集，调用链筛选不改变链内统计，时区和文件/数据库一致性协议经真实纵向验证。
- 审计起点 `PHASE10_1_START_SHA`：`cda8dbe5931f562a4eef47204ac571d29941035a`。
- `git branch --show-current` 为空；这是 detached HEAD，但 `git ls-remote` 证明远程 `feature/model-call-observability` 正好也指向 `cda8dbe5931f562a4eef47204ac571d29941035a`。
- 本地 `main` 为 `e62bb53545c3439cf798a9785f97ada3e1d6a3e1`；远程 `main` 为 `bf3c80b5681c99fc7ff05b9c168898e9ca317587`。报告必须明确区分本地命令要求与当前远程事实。
- `git merge-base main HEAD` 为本地 `main` 的 `e62bb53545c3439cf798a9785f97ada3e1d6a3e1`；功能树包含从 Phase 1 到 Phase 10 的功能提交与分开的审计封印提交。
- 当前 `.sync-audit/verified-source-sha.txt` 为 `3f20b58ebe0648aa4389447913b58642075ff73c`，`git cat-file -t` 返回 `commit`，不是树或文件对象。
- 当前项目要求 Node `>=24.12.0 <25`；后续测试必须确认实际运行版本，不用其他版本的结果冒充正式验收。
- 初始工作区状态是干净的。规划技能默认的 `progress.md` 因 macOS 大小写不敏感而落到已跟踪的 `PROGRESS.md`；发现后已把本轮内容迁至 `OBSERVABILITY_REMEDIATION_PROGRESS.md`，并从当前提交精确恢复 `PROGRESS.md`。
- 当前 Blob 分片确实使用标识最前两位，而标识统一以 `mb_` 开头，因此所谓分片实际上恒定落入同一目录。
- 当前 BlobStore 的读取、删除和缺失恢复确实从数据库 `relative_path` 拼出磁盘路径；这让数据库字段承担了不该拥有的文件系统权限。
- 当前查询侧 Blob 精确读取虽会重算受控路径，但仍使用同步整文件读取；需要与 HTTP 路由一起改为 stat + 流式响应。
- 当前持久化批次在文件写完后清空暂存字节，而事务失败后的重试会重新调用整个提交函数，形成用空字节覆盖成功文件的实际路径。
- 当前事务内部会直接增加载荷和用量丢失计数，数据库回滚无法回滚这些内存变化。
- 当前安装持久化时把此前 Blob 外置器硬编码为空，关闭时会错误清除真实的先前注册对象。
- 安全改动必须继续使用参数化查询；Blob 标识采用白名单校验并从受控根目录重新计算路径；所有解析路径需做目录包含性校验；用户响应不得泄漏本地路径、堆栈、原始敏感载荷或 Blob 字节以外的信息。
- 查询筛选根因：终态和载荷可用性每个语义分支都直接加入顶层条件，因此同字段多值实际被组合为 AND；应先在字段内部收集候选谓词，再用 OR 包成一个顶层子句。
- 数值映射根因：现有 `intOrNull` 无条件执行 `Number(value)`，所以数据库 `NULL` 与空字符串都会变成 0；同时同一个 helper 被成本小数等非整数字段复用，命名与语义不一致。
- 损坏 JSON 根因之一：类别解析失败会直接返回空数组，把“损坏”伪装成“确实没有类别”；需结合 DTO 能力决定显式损坏状态或让上层查询降级。
- 载荷可用性根因：映射函数先检查记录数，只要大于零立即返回 present，导致明确 dropped/expired/not_captured 状态被已有部分记录掩盖。
- 完整性根因：元数据缺失、JSON 损坏、非法值、查询失败和表不可读全部汇入零值；只有“键确实不存在”允许按契约解释为零，其余必须是 unknown/degraded。
- 用量关联根因：有记账表但查不到对应行时直接返回 not_correlated；当前调用行又没有显式运行时关联状态，无法区分真正不相关、投影失败和历史缺失。
- Trace 统计根因：同一别名既承载筛选又承载计数，筛选直接写在 JOIN 后的 WHERE 中，因此只有命中调用进入聚合；需用 EXISTS/候选集合只选 Trace，再对完整调用集合统计。
- DST 根因：切换查找只比较窗口起点和最多 400 天后的终点；当时差中途变化后恢复时直接返回“无切换”。
- DST 上限根因：段数到达上限后追加一个重复段并跳出，随后照常生成日期表达式，产生外观正常但错误的 200 响应；必须抛出带稳定原因码的显式查询失败。
- 查询层继续使用参数绑定，符合安全技能的注入防线；修复同字段 OR 时必须保持所有用户值绑定，不能回退到字符串拼接。
- 聚合假零根因：旧数据库没有用量表时，查询直接选择一组数值零；DTO 映射又对所有空值使用零默认，因此投影不可用被伪装成“确实消耗 0 token”。
- 部分覆盖根因：有用量表时虽然计算了覆盖调用数，但聚合 DTO 没有总覆盖状态；10 条调用只有 5 条用量行时仍展示已知和，好像是完整总和。
- 聚合字段根因：SQL 对每行空 token 使用零合并，映射层再把聚合空值变零；需要把“无调用的真实 0”“有调用但无已知值的 null”“部分调用已知的 known sum + partial”分开。
- Trace 详情用量根因：只依据用量行数组是否为空，在“空”时标明确未关联，在“非空”时标 present；没有把调用总数、显式未关联数、未知数和投影状态纳入判断。
- 成本字段当前虽然保留小数，但仍需通过通用有限数 helper 保持数据库空值为 null；token/序数/HTTP 状态等整数则用严格整数 helper。
- 载荷元数据中的记录字符数、请求序号和 HTTP 状态都复用有空值缺陷的数字 helper，属于任务书要求的全读取模型审计范围。
- 精确载荷正文解析已经区分 present/null/opaque-or-unavailable/corrupt，可直接保留；但两类 provenance JSON 损坏目前都返回 null，仍把“损坏”折叠成“没有”。
- Query health 在数据库不存在或不可读时都构造四个完整性零；虽然总状态不同，细分计数本身没有 known/unknown 标记，界面和导出无法安全解释。
- 当前 Blob 精确读取采用正确的标识白名单、参数化数据库查找、受控根路径重算和目录包含检查，这些安全边界必须保留。
- 流式读取的结构性根因是查询服务的返回类型直接携带 Buffer；应把内部读取结果改为经过验证的文件描述（路径仅服务端内部使用、精确长度和安全媒体类型），由路由创建流，HEAD 仍只做 stat。
- 现有数据库版本是 v2，调用表没有显式用量关联状态；若运行时无法通过已有正式列表达 MC-03 事实，任务书的 v3 条件成立。
- 共享契约当前把整体与分组 token、缓存计数定义为必有数字，并且没有 aggregate availability 字段；正确表达 projection_unavailable/partial/unknown 需要正式改契约，而不是仅改查询实现。
- 完整性契约当前只有四个非空数字，没有 known/unknown 状态；Query health 也强制持有该结构，需同步调整所有消费者。
- Trace 详情的用量汇总当前使用独立的非空数字结构，容易与整体聚合语义漂移；应复用统一用量摘要和覆盖状态。
- `model_calls` v1/v2 均没有正式的 usage correlation 列；现有迁移是单事务阶梯模型，适合增加 v3，但还必须同步只读支持版本和指纹/注册表。
- 写侧 `markPayloadAvailability` 仅在原列为 NULL 时更新，因此状态优先级取决于哪个事件先到；后发生的 dropped 无法覆盖 earlier not_captured/expired。需把优先级编码为一个 canonical SQL/函数并供写、读、筛选一致使用。
- 持久化重试的准确根因：`commitBatch` 先写文件并清空暂存字节，随后创建事务；`flushOnce` 捕获事务失败后再次调用整个 `commitBatch`，第二次必然重新执行文件写入。
- 回滚计数膨胀根因：用量投影和载荷插入异常在事务回调中直接增加全局健康对象；事务重跑会重复增加，而 SQLite 回滚对内存无效。
- 序列化/形状/硬限制 drop 根因：载荷插入返回 null 时只增加全局 `droppedPayloadRecords`，没有把该记录的 callId 加入 dropped 状态队列。
- 最终重试失败根因：批次已经从队列永久移除，内存计数增加，但没有 `pendingHealthMetaDirty`/失败回执；空队列的后续 flush 直接返回，close 也不会单独持久化计数。
- Blob 写失败当前在事务前立即增加全局计数；若后续事务重试，其计数和失败集合也会被重新构造，属于同一回滚外副作用问题。
- 外置器卸载根因：安装前没有读取旧注册对象，关闭时也不检查当前对象是否仍是自己，可能清空后来注册的对象。
- 正确拆分应为：批次只取一次；文件准备阶段每个 Blob 最多写一次并保留不可变结果；数据库提交阶段只消费准备结果，可重试；事务内部只积累局部计数，成功后一次性应用；最终失败另生成待持久化健康回执。
- 悬空引用根因：载荷归一化在判断文件失败/不持久之前就把 blobId 加入引用数组；随后虽然从失败描述中删掉标识，插入循环仍会为该标识写引用。
- Blob 路径越权根因：内部绝对路径函数只要求落在整个 `model-observability` 目录，未限制在 `model-observability/blobs`；数据库篡改为 `observability.sqlite` 会通过包含检查并可能被删除。
- BlobStore 的内部读、删、缺失恢复都直接使用数据库相对路径；需改为校验 blobId 后计算 canonical 新路径，并对历史旧分片提供只读/删除兼容候选。
- 新 Blob 的随机分片可使用 `mb_` 后随机 token 的前两位；历史路径 `blobs/mb/...` 必须继续可读。所有候选都要从 Blob 根目录内计算，不接受数据库路径作为权威。
- orphan recovery 当前用数据库相对路径建立“已知路径”集合；它不直接按该字段删除目标，但会让篡改路径影响保留判断。应从合法 blobId 生成受控的 canonical/legacy 集合。
- 载荷归一化应只把“本批文件准备成功或已有持久元数据证据”的标识加入 durable 引用数组；失败描述保留 `store_failed` 且不得有 blobId。
- Capture registry 已提供读取当前外置器的方法，持久化模块只需正式导入并按 observer/sink 的所有权模式恢复即可，无需新增全局机制。
- 动态重配根因：引擎先等待旧句柄 `close()`（最多 5 秒）再安装新句柄；旧句柄关闭后把 `closed=true`，而已开始调用的记录器/载荷会话固定持有旧 observer/sink，因此晚到响应和终态被直接丢弃。
- 动态代际修复需要句柄新增“退役并排空”语义：先卸载全局注册点阻止新调用绑定，旧 observer/sink 仍接受已绑定调用；按 logical start/end 维护活跃集合，归零或有界超时后刷盘关闭。引擎应立即安装新一代，并异步追踪旧一代关闭。
- 引擎销毁时需同时有界关闭当前一代与仍在排空的退役代，避免代际资源泄漏；健康接口应清楚只表示当前生效代。
- MC-03 的真实运行时接点已经在 Pi 流包装器中明确计算 `nativeSummarization`，它来自“无显式作用域且会话正在压缩”的真实路径；可从此处把闭集 usage correlation fact 放入记录器上下文和生命周期事件。
- `ModelCallEvent` 当前没有用量关联字段。新增顶层安全闭集字段比塞入自由 details 更可靠，且能由 TraceStore 正式投影到 v3 列；只有 MC-03 设置 `not_correlated`，其他调用保持 null/unknown。
- 首次相关测试基线没有执行到用例：当前工作树没有可解析的本地 Vitest；`npm exec` 临时拉取 4.1.11 后在 fsevents 的 node-gyp 阶段失败，并最终报 `vitest/config` 不可解析。该结果是环境失败，不是代码回归，也不是通过。
- 使用 Node 24.16.0 按 `package-lock.json` 执行 `npm ci` 成功，安装 1282 个包，项目 postinstall 的 Pi SDK 校验通过；依赖审计报告 1 low + 7 moderate，未执行越界的自动/强制升级。
- 依赖恢复后，仓库固定 Vitest 4.1.10 的 10 个相关文件共 78 个现有用例全部通过；这是 Phase 10.1 新增失败测试之前的可信基线。
- 测试基建使用真实临时目录、真实 SQLite 和真实持久化安装，适合新增查询真实性测试；无需 mock SQL。
- 现有 v1 fixture 通过从当前库删除后续对象并回退 `user_version` 构造；升级 v3 后需把 helper 扩展为可精确构造 v1/v2，并更新“未知更高版本”用例。
- Export 的调用 bundle 直接携带 Query call DTO，用量的 unknown/null 修复会自动进入每条导出；manifest 直接携带 Query health 完整性结构。仍需新增断言防止未来漂移。
- Export 的筛选复用 Query filter，不另建 SQL；同字段 OR 修复后应增加导出组合用例证明复用链真实生效。
- 新增 `model-observability-query-truth-integrity.test.ts` 的 9 个用例在当前实现上全部失败，无环境噪声：多选返回空集；SQL NULL 映射为 0；完整性无状态；缺 usage 行为 not_correlated；Trace 统计 3→1；partial dropped 显示 present；聚合覆盖状态缺失；损坏 provenance 无显式状态。

## Technical Decisions

| Decision | Rationale |
|----------|-----------|
| 先逐段读取任务书并做需求矩阵 | 任务书很长，且包含多处“禁止/允许/验收”条款，不能只看标题或摘要 |
| 当前不改变 Git 拓扑 | 符合用户明确边界，也避免覆盖当前 detached HEAD 的真实状态 |
| 动态重配采用“新调用进新一代、旧调用排空旧一代”的方向 | 任务书推荐且能保持一次调用的记录策略前后一致；最终仍需以当前代码调用链和失败测试验证 |
| 未经额外授权不推送、不发版 | 本轮请求明确授权原地修复，但没有明确要求推送远程或触发发布；先做到本地可交付事实闭合 |
| 同时记录本地和远程主线坐标 | 本地 `main` 已落后于远程；只写其中一个会让审计基线产生歧义 |
| 保持已有 `PROGRESS.md` 原样 | 它是仓库历史总进度，任务书另有本轮专用进度文件；大小写冲突已修复 |

## Issues Encountered

| Issue | Resolution |
|-------|------------|
| 当前没有分支名 | 保持现状原地工作；不创建、不切换分支 |
| 历史建议使用隔离工作树验证，但本轮用户明确禁止 | 以用户本轮要求为最高边界，在现有工作树内保留并区分环境问题，不创建隔离工作树 |
| `progress.md` 与 `PROGRESS.md` 在当前文件系统冲突 | 迁移到任务书指定的 `OBSERVABILITY_REMEDIATION_PROGRESS.md` 并恢复跟踪文件 |
| 首次多文件纠错补丁上下文不匹配 | 确认补丁没有部分应用，改用精确小补丁完成纠正 |
| 记录 Query RED 结果的多文件补丁上下文不匹配 | 拆为逐文件小补丁，避免跨文件上下文耦合 |
| `rg` 正则反引号被 shell 当命令替换 | 使用单引号固定正则；未产生文件改动 |

## Resources

- 任务书：`/Users/study_superior/.codex/attachments/05acf768-84b2-401c-8e91-f057caf395a1/pasted-text.txt`
- 当前仓库：`/Users/study_superior/.codex/worktrees/4dcf/LingxiAgent`

## Visual/Browser Findings

- 本阶段未使用视觉或浏览器资料。

## Phase 10.1 最终事实

- AR-01～AR-20 的生产修复与本地证据均已闭合；查询、持久化、网络响应、界面和导出不再把 null、unknown、corrupt、dropped、unavailable 或 projection unavailable 冒充成 0、空对象或正常状态。
- 数据库正式升级到 v3，只为表达运行时显式 usage correlation；v1/v2 的旧数据继续只读兼容，未知新版本拒绝写入，迁移失败保持单事务回滚。
- Blob 一致性改为“文件准备一次、数据库事务可重试”；健康计数只在提交后应用，最终失败通过持久回执补记，失败文件不产生引用。
- 动态设置使用代际排空：在途调用绑定开始时的策略，新调用使用新策略；四种切换和延迟 Provider 均未产生 duplicate 或 incomplete。
- 独立纵向证据补齐：真实并行工具、真实 spawn 子代理、MC-02、MC-03、审批格式修复的两次真实请求、记忆编译生产提示、详情/正文/调用链界面。
- 最终全量结果：1201 files passed / 1 skipped；12135 tests passed / 7 skipped；0 failed；134.40s。
- 本机构建结果：三个 build 与 macOS arm64 package smoke 通过；notarization 未执行。
- 发布证据仍有两个明确缺口：本轮功能树尚未形成提交并推送，所以远端 Windows/Linux/macOS CI 与 Phase 10.1 source seal 均为 NOT_EXECUTED。旧 seal 虽是合法 commit，但不能覆盖当前未提交工作树。
- 用户的 Git 边界已保持：始终在 `/Users/study_superior/.codex/worktrees/4dcf/LingxiAgent` 原地工作，保持 detached HEAD，没有新建或切换分支/工作树。
