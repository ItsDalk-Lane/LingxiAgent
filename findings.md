# 事故修复取证笔记

## 已知事实

- 任务书要求先取证、再修复，不能把“运行时未激活”误判成“Git 代码丢失”。
- 旧 `PROGRESS.md` / `progress.md` 明确记录仓库曾把版本改为 `0.1.3`，并将发布工作流切到正式版 Latest 路径。
- 旧 `BLOCKED.md` 记录上轮主工作树存在外部类型目录污染和若干非本任务改动；本轮必须重新核对当前状态，不能照抄旧结论。
- 任务书要求保留用户数据、有效 OTA、previous/rollback 和崩溃隔离语义；手工清缓存不算修复。

## 待验证关键点

- `0.1.3`、`0.1.21`、`0.1.22` 的真实 tag、提交、发布时间和所有构建元数据。
- 当前安装包 seed 与磁盘 current 的实际排序依据，以及 renderer/server 是否走完全相同的决策。
- 是否已有跨发布单调字段，可区分产品版本、构建、train 和 Artifact 世代。
- 所有正式 Release、Train、stable/beta pointer 的生成和写入顺序，失败后是否仍可能继续发布。
- 下游功能在 Git 中是否保留、是否有跨层协议断裂，以及运行时为什么没有体现。

## 证据来源

- `/Users/study_superior/.codex/attachments/88296525-a881-4242-accf-255bcb7d6950/pasted-text.txt`
- 仓库旧 `PROGRESS.md`、`progress.md`、`BLOCKED.md`

## 2026-08-09 初始现场

- 分支为 `main`，跟踪 `origin/main`；首次状态检查只看到本任务新建或追加的 `PROGRESS.md`、`findings.md`、`task_plan.md`，尚未发现其他当前工作树修改。
- 任务书共 1633 行；截至本次记录已读到第 1400 行，剩余交付清单仍需完整读取。
- 本地长期记忆索引中没有 LingxiAgent 或本次版本事故的相关记录；后续结论以当前仓库为准。
- 任务书明确要求发布工作默认止于 release-ready：不推 tag、不创建正式 Release、不发布 stable Train。
- 发布依赖链必须让版本预检、质量检查、Artifact 完整性、启动和历史升级 smoke 成为下游发布动作的真实前置条件。

## Git 与版本初证

- 当前分支 `main`，HEAD `b5d009d8b0ae1ab8b7df31edb0a102c98ad51b02`，与 `origin/main` 无领先或落后。
- 远程产品标签至少包括 `v0.1.0`、`v0.1.1`、`v0.1.2`、`v0.1.21`、`v0.1.22`、`v0.1.3`；`v0.1.3` 解引用到当前 HEAD。
- `train-stable-2`、`train-beta-5` 也指向当前 HEAD，说明错误版本不只进入安装包标签，也进入 Train 发布线。
- 根 `package.json` 与 lockfile 根包版本均为 `0.1.3`；`upstreamVersion` 为 `0.444.1`。
- 三个目标 pi 包的声明和 lockfile 解析条目均为 `0.84.1`，尚需验证实际安装目录与补丁脚本。
- upstream remote 由仓库事实确认是 `https://github.com/liliMozi/openhanako.git`。
- 仅按 Lingxi 产品标签比较，已进入远程的最高 SemVer 是 `0.1.22`，所以若无更高远程 Release，下一候选应为 `0.1.23`。

## 待补证

- GitHub Release 查询因 `gh release list` 不支持 `url` 字段而失败；应改用其报告的可用字段重试。
- `v0.1.3` 的 annotated tag object 是 `196d415d...`，发布提交是 `b5d009d8...`；时间线中要区分 tag object 与 commit。

## GitHub Release 实证

- `v0.1.3` 是非草稿、非 prerelease、当前 Latest，发布时间 `2026-08-08T16:10:07Z`。
- `v0.1.22` 是非草稿 prerelease，发布时间 `2026-08-06T15:04:16Z`；虽然是预览发布，但已有安装包和 Train，必须计入“可能进入用户设备”的版本上界。
- `v0.1.21` 同样是已发布 prerelease；`v0.1.2` 是此前正式版。
- `train-stable-2` 与 `train-beta-5` 都已作为 GitHub prerelease 发布，发布时间分别为 `2026-08-08T16:10:39Z` 与 `16:10:54Z`。
- 因此本次不能只修安装包标签，还要同时保护 stable/beta Train 路径。

## Artifact 源码入口初筛

- 启动与迁移：`desktop/src/shared/artifact-boot.cjs`、`desktop/src/shared/artifact-repair.cjs`、`desktop/bootstrap.cjs`。
- 核心状态：`shared/artifact-core/activation.cjs`、`manifest.cjs`、`pointer-store.cjs`、`pointer-channels.cjs`、`ota-core.cjs`。
- 在线更新：`desktop/src/shared/artifact-ota.cjs`、`train-update-apply.cjs`。
- 构建发布：`scripts/build-server-artifact.mjs`、`build-standalone-server-artifact.mjs`、`verify-seed-kit.mjs`、`publish-train.mjs` 与 `.github/workflows/*.yml`。
- 现有测试已覆盖 boot、activation、pointer、OTA、repair、seed 与 Train，但是否覆盖历史升级和发布阻断仍待逐个检查。

## 启动状态机根因证据

- `artifact-boot.cjs` 的当前规则是：没有可启动指针则激活 seed；发生崩溃回退时坚持 previous；否则直接比较 seed 与磁盘指针的产品版本，只有 seed 严格更大才激活。
- 对 `seed=0.1.3`、`current=0.1.22`，当前比较结果必为 seed 更旧，因此决策是继续启动磁盘 current。这与任务书事故假设一致，下一步用现有测试夹具跑出真实结果。
- 跨上游/下游版本号体系只用 minor 位数数量级判断并转入兼容规则；`0.1.3` 与 `0.1.22` 属于同一体系，不会触发该兼容分支。
- 旧指针没有可比较版本时：train 0 且内容哈希不同会自愈到 seed；train 大于 0 继续优先。该规则无法修复一个 metadata 完整但产品版本倒退的 `0.1.22` 指针。
- server 与 renderer 用独立指针命名空间，各自执行同构启动、失败计数和 previous 回退；这保留了独立回退能力，也允许两侧停在不同运行时。

## 当前元数据能力

- seed manifest 的 `train` 固定为 0；server 与 renderer 的 `version`、`minShell` 都直接来自根产品版本。
- Artifact 文件名分别是 `server-<version>-<platform>-<arch>.tar.gz` 与 `renderer-<version>.tar.gz`。
- manifest 已携带发布时间、内容哈希、大小、路径和契约版本，但初筛未见独立的安装包发布世代或来源提交字段。
- 在线 Train 复用同一 manifest 构造逻辑并分配各频道单调 train；train 号只在频道内有序，不能直接代表跨安装包 seed 的发布先后。

## 发布链缺口初证

- tag 触发的 `build.yml` 当前是 renderer box → 平台打包矩阵 → release → publish-train；`release` 只依赖 `build`。
- 平台打包会校验 seed kit，Windows 独立 server 还会做真实启动 smoke；但当前 tag 工作流没有把 typecheck、全量测试、lint、Artifact bootstrap smoke、历史升级 smoke 接成 release 的真实前置依赖。
- 普通 `ci.yml` 会执行 typecheck、lint、构建和全量测试，但它是 main/PR 的独立工作流，tag release 没有确定依赖它。
- 手工 `publish-train.yml` 直接调用 Train 脚本，目前未见候选产品版本相对历史 Release/频道指针的单调性预检。

## 事故复现（未改源码）

- 公开决策入口实测输入：current `{version: 0.1.22, train: 0, sha256: current-022, versionDir: /user/artifacts/renderer/0.1.22}`；seed `{version: 0.1.3, sha256: seed-013}`。
- 实测输出：决策为 `boot`，指针槽仍是 `current`，最终目录仍为 `/user/artifacts/renderer/0.1.22`。
- server 与 renderer 共用该纯决策规则，所以同样输入会让两者都留在 `0.1.22`；现有组合启动测试已证明两个 kind 都按此规则独立执行。
- 当前 `tests/artifact-boot.test.ts` 的 46 项全部通过，但其中“更旧安装包不得降级 current”把这一事故行为当成正常保护；缺少“错误产品版本后由更高发布世代修复”的表达能力。

## 本地验证环境

- 默认 `node` 是 `/Users/study_superior/.local/bin/node`，版本 `v22.23.2`，不满足仓库 Node 24 约束。
- 本机已有 `/Users/study_superior/.nvm/versions/node/v24.15.0` 与 `v24.16.0`；后续正式命令使用仓库 CI 同款 `v24.15.0` 的绝对路径优先。

## 状态机设计约束（源码细读）

- server/renderer 启动结果当前只返回目录、train、产品版本、槽位、是否激活 seed、是否崩溃回退、隔离 train 和回退前后版本；没有把内容哈希、发布时间或发布来历带到主进程。
- 每侧启动都会先晋升 next，再检查连续失败；达到阈值后先降到 previous，并明确跳过 seed 新鲜度覆盖。这一安全语义必须原样保留。
- seed 激活走与 OTA 相同的归档验签、解包和指针写入路径；修复排序不需要另造激活通道。
- OTA manifest 要求同一 train 的 server/renderer 产品版本一致，并已有内容哈希一致、同版不重复应用、产品版本不倒退和契约兼容检查。
- 当前构建 manifest 的统一来源只接收产品版本、平台、架构、签名 key、发布时间和两种归档信息，确认没有传入来源提交或独立发布世代。
- 主进程实际从已解析的版本目录启动 server，并把已激活 renderer 目录传给 server；因此错误指针会同时影响桌面页面和 server 供给的远程页面，绝不只是 About 显示问题。

## 历史发布与线上频道状态

- 产品链：`0.1.2`（pi 0.80.3）→ `0.1.21`（pi 0.80.3）→ `0.1.22`（pi 0.83.0）→ `0.1.3`（pi 0.84.1）→ 当前 main（同 `0.1.3` 提交）。
- 四个产品 Release 都真实携带 renderer 归档和 darwin arm64/x64、linux x64、win32 x64 server 归档，文件名产品版本与 tag 一致。
- 当前线上 `stable.json` 是 train 2、产品 `0.1.3`；`beta.json` 是 train 5、产品 `0.1.3`。旧 beta train 4 是产品 `0.1.22`。
- 线上 signed channel/Train manifest 只有 train、频道、发布时间、契约、产品版本和归档信息；没有来源 Release 提交或跨 seed/Train 的发布世代。
- 现有 Train 发布只防 train 号回退、同版不同内容和错误频道；当频道是 `0.1.22` 时，候选 `0.1.3` 属于“不同版本”，脚本没有比较 SemVer，因而允许 stable/beta pointer 降到 `0.1.3`。
- 客户端 OTA 又有产品版本不倒退保护，所以一个已在 `0.1.22` 的客户端既不会接受线上 `0.1.3` Train，也不会接受 `0.1.3` 安装包 seed，形成双通道锁死。

## 发布世代设计结论

- 现有 train 只在单一频道内单调，`releasedAt` 是 Train/构建发生时间，无法稳定表达“这个 OTA 来自哪个产品 Release”；二者都不能直接承担跨安装包与 OTA 的统一顺序。
- 需要一个最小的、由源码发布元数据维护的单调 `releaseGeneration`，同时写入 seed、Train、激活 receipt/指针和日志；`sourceCommit` 只做来历识别，不参与大小排序。
- 为兼容旧客户端与旧指针，新字段必须在 schema 1 中可选读取；当前及后续正式构建必须由预检强制存在、递增并与候选 tag 一起发布。

## 上游三方审计坐标

- 仓库 remote 明确：`upstream=https://github.com/liliMozi/openhanako.git`。
- 上游 `v0.443.54` 为 `4cfdb13f...`，目标 `v0.444.1` 为 `cc19cb49...`；两标签间共 52 个路径变化。
- Lingxi 同步前状态 A：`a5d1e541...`，已经包含 `0.1.22` 之后的模型设置、Ollama、审批、语义槽位等下游提交。
- 上游目标 B：`cc19cb49...`；由于 Lingxi 产品历史与上游线没有共同祖先，无法普通 merge。
- 同步结果 C：`97595264...`，单父提交；提交说明记录采用 BASE/OURS/THEIRS 逐文件三方合并并补齐 pi 0.84.1 适配，随后 `b5d009d8...` 只准备 `0.1.3` 正式发布。
- `97595264` 修改 71 个文件；需要对上游 52 个路径和 A 的下游高风险区域逐项交叉验证，不能只引用提交说明。

## SemVer 工具

- 依赖树里已有多个 `semver` 7.x，但根包未直接声明；发布预检若使用它，应增加直接开发依赖，避免依赖传递依赖的提升布局。

## 下游功能提交清单

- `cce8e863`：修复纯 skill 消息被门禁丢弃，9 文件。
- `d555c14e`：供应商、模型、用量设置界面重构，19 文件。
- `34dbb17d`：Ollama 上下文、工具声明、结构化输出、thinking 与上下文长度桥接，15 文件。
- `0250f5fc`：审批收口为单一意图审查者，7 文件。
- `283d9581`：title、summarize、memory、vision、approval、guard 六类语义槽位体系，64 文件。
- `a5d1e541`：语义槽位跨层收口修复，25 文件。
- `97595264`：上游 0.444.1 与 pi 0.84.1 同步，71 文件。
- `b5d009d8`：`0.1.3` 发布准备和审计修复，28 文件。
- 当前树能找到上述各领域的生产入口和专门测试，但还需运行定向测试并检查跨层读写，不以文件存在直接判定功能完整。

## 审计脚本问题

- 首次 A/B/C 路径分类脚本错误地把连 `package.json` 在内的所有路径都标为 missing，明显违反已知 Git 事实；该结果作废，不能用于审计结论。

## A/B/C 重跑与功能基线

- 改用显式 bash 后阳性校验通过：A、B、C 的 `package.json` 都可读取；上游 52 个变更路径在 C 中全部存在。
- 上游新增的本地轻量压缩、内部情绪块及其测试在 C 中与 B 逐字节一致；多处事件、协议、桥接、样式和实验模块也与 B 一致。
- 其余 B≠C 路径主要是 Lingxi 品牌、下游设置/语义槽位、现有测试和生成清单的合并点，需以定向测试与源码链路判断，不能要求整文件等于上游。
- Node 24.15.0 下执行 36 个功能测试文件：36 passed，661 tests passed，0 failed。
- 该基线同时覆盖语义槽位、独立 approval、Gateway、模型同步/提供方、Ollama、skills、配置归属与迁移、上游轻量压缩/情绪块/桥接、pi 边界、xAI OAuth 适配和设置 UI。
- 偏好迁移测试中的 stderr 是用例主动注入损坏 JSON 后验证“保留原始字节再替换”的预期日志，测试通过，不是失败。

## 已发布产物抽检状态

- 本地只有陈旧的 `renderer-0.1.21.tar.gz`，不能用于证明 `0.1.3` packaged state。
- 首次从 GitHub 下载 `0.1.3` renderer/server 资产时临时下载链接返回 EOF，文件未落盘；后续哈希和内容检查没有输入，因此无效且未形成结论。

## `0.1.3` packaged state 实证

- renderer 归档重试下载成功，SHA-256 为 `5806dad27c9f84eb08ed8a4c80845f923d5e38c36fdfacbb60e54e757bd3f083`，与 signed stable/beta Train 完全一致。
- renderer 包内能找到新版 `/api/providers/summary` 设置数据源、`auxiliary-vision` 调用和 skillBadge 处理代码。
- darwin-arm64 server 归档下载成功，SHA-256 为 `8891b8872fbf8274de0919bcf50117fdf7d1c9dfe6721d2bb80c09129a42a8db`，与 signed stable/beta Train 完全一致；包内 package version 为 `0.1.3`。
- server bundle 内能找到 approval/title/summarize/memory/vision 模型键、供应商汇总路由、Ollama `num_ctx`、skill 消息和上游轻量压缩标记。
- 结论：Source state 与 Packaged state 都包含用户报告“消失”的新功能；Active runtime state 因指针选择 `0.1.22` 才表现为旧界面/旧服务端。

## 修复实现与迁移验证

- 根版本已改为 `0.1.23`，根 lockfile 同步；直接生产依赖固定 `semver@7.8.0`。
- `releaseGeneration=1` 与来源提交进入签名 seed/Train、指针和 `.verified` 回执；旧 schema/指针缺字段仍可读取。
- 启动选择保持崩溃回退最高优先级、同版本/同内容幂等；新世代首次遇旧指针会接管，进入世代规则后旧元数据不能倒灌。
- OTA 同步使用发布世代，缓存状态保存世代与来源提交；server/renderer next 还必须在 train 和世代上同时一致。
- 启动日志结构化报告壳、server、renderer 的版本、世代、train、哈希、槽位、来源与提交；混合世代或新旧元数据混合会告警，但不破坏独立回退。
- `npm run test:artifact-release-smoke` 首轮：8 文件、303 项全绿；新增 prerelease 和工作流守卫后需最终重跑记录新数字。
- 真实集成用例从 legacy `0.1.22` 与 legacy `0.1.3` 分别升级，两侧都激活 `0.1.23` generation 1；干净安装、同版重装、旧 Train、crash fallback 均覆盖。

## 发布门禁

- `release-preflight` 枚举全部已获取标签，只把包名、tag 与该标签 `package.json` 版本三者一致的 Lingxi 标签算作产品历史，避免把断开的上游 0.4xx 标签混入；真实仓库结果为历史最高 `0.1.22`、候选 `0.1.23` PASS。
- Build workflow 的 Release 现在依赖：版本预检 → 平台构建与全量质量 → Artifact/历史升级 smoke → Release；缺一不会进入发布 job。
- 自动和手工 Train 都在签名密钥落盘/远程写入前重跑预检；发布脚本再比较现有签名频道的发布世代。
- 预发布 tag 通过 `semver.prerelease` 分类为 prerelease 且不是 Latest。
- 工作流结构守卫 5 项、既有 CI/seed 守卫 18 项均通过。

## 环境与剩余发布阻塞

- `npm ci` 后 Node 24 三段类型检查 0 error；lint 0 error、7958 个历史 warning，退出码 0。
- 三个目标 pi 包实际安装目录和 `npm ls --depth=0` 均为 `0.84.1`；`scripts/patch-pi-sdk.cjs` 输出 all checks passed。
- release digest 仍是 `0.1.3`。生成器需要未配置的 `OPENAI_API_KEY`，且必须在本次改动形成提交后采集提交范围；遵守项目规则没有手改，已在 `BLOCKED.md` 写明解除步骤。

## 最终验证

- Node 24.15.0 全量测试最终为 1088 个文件通过、1 个跳过；11016 项通过、7 项跳过、0 项失败。
- Artifact release smoke 最终为 8 个文件、304 项全绿；工作流/既有 CI/seed 守卫为 3 个文件、23 项全绿。
- `build:packages` 与 `build:client` 退出码均为 0；darwin-arm64 `build:server` 使用一次性本地密钥生成签名 seed，`verify-seed-kit` 通过，临时私钥目录随后已移除。
- 解包后的 darwin-arm64 server 在隔离用户目录真实启动，身份接口返回 HTTP 200、协议 1，包内版本为 `0.1.23`。
- 最终三段 typecheck 为 0 error；lint 为 0 error、7958 个仓库历史 warning；两份旧摘要对 `v0.1.23` 的校验都按预期以退出码 1 阻断。
- `git diff --check`、受控 JSON 解析和待交付文件秘密标记扫描通过；最终 Git 清单只含本任务源码、测试、生成治理清单、计划和报告。

## 正式发布续跑前置

- 用户已明确授权修复剩余阻塞、提交、创建 tag、推送并创建正式 Release，不再停留在“只准备仓库”边界。
- `gh 2.97.0` 可用，已登录 `ItsDalk-Lane`，权限包含 `repo` 与 `workflow`；`origin` 指向 `ItsDalk-Lane/LingxiAgent`。
- 当前仍在 `main`，与 `origin/main` 0 ahead / 0 behind；39 个工作区变化均来自本事故修复链，尚未提交。
- 当前 shell 未配置 `OPENAI_API_KEY`、`GH_TOKEN` 或 `LINGXI_SIGN_KEY`；GitHub CLI 可用 keyring 凭据，摘要生成器的模型凭据仍需继续核对。
- 文件化计划的 session catchup 命中一段与本仓库无关的旧 Claude 技能问答，没有需要合并进当前计划的代码事实。

## 发布前依赖安全复核

- 实时 `npm audit --omit=dev` 为 33 项：0 critical、15 high、18 moderate；完整依赖树为 47 项，其中 2 critical 来自开发/构建依赖，不进入生产依赖统计。
- 生产 high 不是纯历史噪声：直接涉及 Lark SDK、自动更新、Hono、js-yaml、undici，并均有当前兼容版本修复；完整树 critical 涉及 Vitest 与 tar，当前依赖链也有修复版本。
- 多数修复位于已声明的 caret 范围内，只因 lockfile 仍锁旧版；少数明确固定版本需要窄幅升级：Electron 42.3.0→42.8.1、Mermaid 11.10.1→11.16.1、undici 7.24.7→7.29.0。
- exceljs 与 node-telegram-bot-api 的剩余 moderate 只给出不安全的降级/主版本升级方案，不在未验证情况下强行修改；目标是先消除所有 critical/high，再按完整回归判断发布。
- 直接依赖和 lockfile 安全更新后，生产树降为 9 项 moderate、0 critical/high/low；完整树为 9 项 moderate、1 项 low、0 critical/high。原有 2 critical 与 24 high 已全部消除。
- 剩余项集中在 Hono WebSocket 适配、Excel/Telegram 的旧请求链和构建器；审计工具只给出破坏性升级、降级或尚无修复版本，因此没有使用强制修复冒险改变产品行为。
- 实际 `npm ci` 暴露出 `yauzl` 原本没有被项目直接声明，只是旧打包工具把它提升到了根目录；打包工具升级后生产代码和类型检查一起报缺包。这不是测试框架不兼容，而是隐藏依赖被可靠地揭露，现已把运行依赖和类型依赖显式归属到项目。
- 修复隐藏依赖后，全量测试重新达到 1088 文件通过、1 文件既有跳过，11016 项通过、7 项既有跳过、0 失败；类型检查与 lint 也恢复通过，说明安全升级没有改变既有业务基线。
- 升级后的独立服务端真实装包并运行：签名 seed 自验成功，隔离启动后的身份接口返回 HTTP 200、协议 1、版本 `0.1.23`。这覆盖了 Lark、网络、打包和原生依赖变动实际进入发布包的路径。

## DeepSeek 摘要生成迁移

- 用户明确选择 `DEEPSEEK_API_KEY`、`deepseek-v4-flash` 和 DeepSeek Responses API；不能只替换现有 `--model`，因为当前实现还把 OpenAI 域名、密钥名与响应提取格式写死。
- 安全约束：密钥只进 Authorization 请求头，不写来源包、摘要、日志或错误；模型返回值必须继续通过本地严格 schema、tag/version 和 v2 历史顺序校验。
- 2026-08-09 官方资料确认 `deepseek-v4-flash` 存在、OpenAI 格式 base URL 为 `https://api.deepseek.com`，且支持 JSON Output；公开 V4 公告和接口参考明确列出的却是 OpenAI Chat Completions 与 Anthropic API。
- 对 DeepSeek 官方文档做 `Responses API`、`POST /responses`、`previous_response_id` 和 `responses.create` 精确检索均无结果。该能力与用户说明存在证据差异，不能未经端点实测直接落实现有 OpenAI Responses 请求体。
- 对 `/responses`、`/v1/responses`、Chat Completions 和故意伪造路径做无密钥/假密钥探测，DeepSeek 网关都在路由判断前统一返回 401，OPTIONS 也统一返回 200；这些响应无法证明 Responses 路由存在。
- 当前进程没有 `DEEPSEEK_API_KEY`，所以暂时不能用真实鉴权请求裁决 Responses 请求体。实现前还需查官方文档目录；若官方契约仍缺失，应显式阻断或改用已文档化的 Chat Completions，而不能把猜测当正式发布依赖。
- DeepSeek 官方 `sitemap.xml` 实际列出了 `/guides/responses_api` 与 `/api/create-response`；搜索索引滞后造成了前述漏检。用户关于 Responses API 的说明成立，后续以这两份官方页面的请求/响应契约实现，不改用 Chat Completions。
- 官方 Responses 契约：`POST https://api.deepseek.com/responses`，当前只支持 `deepseek-v4-flash`；`instructions`、`input`、`max_output_tokens` 和 `text.format` 均受支持。
- `text.format` 支持 `json_schema`、`name` 与 `schema`，但官方字段没有 `strict`；现有 OpenAI 请求中的 `strict: true` 不应继续发送。服务端结构化输出后仍须执行仓库本地 `assertValidReleaseDigest`。
- 非流式响应是 Responses 结构：`status` 可为 completed/incomplete/failed，正文位于 message 项的 `output_text` content；生成器应拒绝非 completed 状态，不能从失败/截断响应里捞部分正文。
- DeepSeek Responses 是无状态接口，`store` 不支持且永远为 false；摘要是单轮生成，不需要 `previous_response_id` 或 conversation，现有请求也无需这些字段。
- 实现只改摘要生成边界：密钥改为 `DEEPSEEK_API_KEY`，默认模型改为 `deepseek-v4-flash`，端点改为官方 `/responses`；原有来源采集、v1 schema、本地校验和 v2 史册追加逻辑保持不变。
- 远端非 2xx 错误不再附带响应正文，避免服务端异常回显进入日志；非 completed 响应即使带有看似完整的部分 JSON 也会失败关闭。
- 无密钥的真实命令验证为失败关闭：退出码 1、错误明确指向 `DEEPSEEK_API_KEY`、目标摘要文件不存在。
- 真实 Responses 输出可先给出 reasoning 项，再给 message/output_text；不能遍历到任意 `text` 就返回，否则会把思考过程送进 JSON 解析。最终提取只接受 `output_text`。
- 修复后真实生成成功：v1 为 `0.1.23`，v2 头部为 `0.1.23` 且保留三条旧记录；两个校验器都退出 0，模型条目的三个 commit ref 均能在本地 Git 解析。
- 摘要进入工作树后，版本预检、304 项 Artifact release smoke 与 11021 项全量测试全部通过；因此摘要变化没有破坏发布链或包内历史读取。
- 首次 tag CI 暴露本机无法发现的测试夹具问题：Ubuntu 上 Computer Use 正确判为不支持，但 6 项路由正向用例和 2 项引擎懒加载用例没有固定受支持平台。生产行为无需修改，测试必须显式声明 darwin；现有 Linux 负向用例继续证明不支持路径。
- `v0.1.23` 的四个平台构建成功，但 quality 失败使 Release、Artifact smoke、Train 全部 skipped；远端未产生 Release。由于 tag 已推送且禁止重写，下一候选必须是 `0.1.24` / generation 2，不能强行复用 `0.1.23` / generation 1。
- 测试夹具与版本/世代修正后，本地 preflight、304 项 Artifact smoke、11021 项全量测试、类型和 lint 均通过；生产代码的平台支持判断未被放宽。
- DeepSeek 在带完整发布事实的摘要任务上以 `incomplete/max_output_tokens` 结束，4000 额度不足；失败响应没有覆盖先前摘要。提高额度比接受部分正文或删除事实约束更安全。
- 官方 Create Response 契约的 `reasoning.effort` 支持 `none`，并说明输出上限包含 reasoning tokens。8000 仍 incomplete 证明本任务不应保留默认思考；关闭思考比继续无上限加预算更确定。
- `reasoning.effort=none` 后真实请求一次 completed，最终摘要从最后一个真实 Release `v0.1.3` 汇总到 `v0.1.24`，通过双文件校验和人工事实核对。失败 tag `v0.1.23` 没有 Release，但在 v2 中透明保留其 tag 历史。

## 2026-08-09：供应商配置与模型目录状态漂移修复

- 任务书要求把四种含义拆开：持久化供应商、当前选中项、尚未保存的页面草稿、运行时真正可用的模型；当前选择不能推导供应商成员。
- 已配置供应商必须从现有 Provider Catalog/summary 恢复，不得新增第二套数据库；辅助模型必须消费统一运行时模型状态，不得直接拼供应商配置模型。
- 内置供应商删除用户配置后应从已加入左栏消失、但仍留在添加候选；临时未配置项必须可以取消，且不能覆盖持久项。
- 所有供应商和模型写操作必须检查 HTTP 与 JSON 错误；运行时同步失败时不能显示成功，也不能把不可用模型暴露给辅助选择器。
- 初始工作区为干净 `main`；已有计划文件属于已完成的发布事故任务，本任务保留历史并追加独立章节。
- `planning-with-files` 会话恢复只命中无关旧问答，未发现可复用的未同步改动。
- 基线提交为 `27d15d6696ca63113f3e2187267278f0f439f029`，分支 `main`；开始时与 `origin/main` 同步且无用户修改，当前仅计划文件因本任务变更。
- 默认终端是 Node `v22.23.2` / npm `10.9.8`，不满足仓库约束；正式基线和回归使用已安装的 Node `v24.15.0` / npm `11.12.1`。
- 任务书共 1227 行，已读完；明确要求不发布、不打 tag，允许保持工作树修改或整理为一个逻辑提交，但没有授权默认提交。
- 现有相关入口已定位到供应商设置页、供应商列表/详情/模型列表、辅助模型组件、设置状态、应用事件动作、服务端应用事件、供应商目录/注册表和模型同步模块；下一步从现有测试名反推当前契约并跑基线。
- BUG 1 的任务书怀疑已由源码检索直接命中：供应商设置页持有页面局部 `pickedProviderIds`，初始化只读取全局 `selectedProviderId`，渲染左栏和右栏成员判断都依赖这份局部数组。
- 现有供应商页面测试注释也把“初始当前选择被纳入左栏”当成契约，尚未见从全部持久供应商摘要恢复成员的断言；需要细读后用正确契约替换错误保护。
- 服务端已有统一链路证据：配置路由会触发供应商变更回调并发出 `models-changed`，模型接口从运行时 `availableModels` 序列化；前端应用事件测试已证明主模型状态会在该事件后重载。
- BUG 3 的重点已缩小到辅助模型组件：其测试直接模拟 `/api/models`，而主界面已有事件驱动的统一模型重载；下一步确认组件是否私有抓取并独立保存目录。
- 供应商设置页的实际展示公式是“局部点选数组逐项渲染”；右栏也要求当前选择必须位于该数组。页面重新挂载时数组只种入一个当前选择，因此 A/B/C 会退化为最后选中的 C，应用重启且当前选择清空时左栏归零。
- 点击候选只把供应商加入局部数组，尚未写配置，说明当前产品的“点击添加内置供应商”就是页面临时草稿；它应能取消，页面重新挂载后可以消失。自定义供应商表单则在提交成功后直接写入配置，属于持久项。
- 已配置供应商删除按钮只清空全局当前选择并刷新配置/摘要，没有任何机制从局部点选数组移除；因此服务端删除成功后仍会留下幽灵行。未配置内置草稿因摘要不可删或处于设置态，当前根本不渲染删除按钮。
- 添加自定义供应商和删除供应商已经检查 JSON `error`，但都没有检查 HTTP 状态；需要结合统一请求工具和其他写路径判断是否能用最小公共失败判断收口。
- 前端主模型权威状态位于应用级共享 store，`models-changed` 会调用统一加载动作；设置窗口另有独立设置 store，辅助组件若直接读应用共享 store，需确认独立设置窗口是否也挂载同一事件/数据通道，不能只在主窗口偶然生效。
- 服务端供应商摘要已经明确区分：目录/有效覆盖项标为 `is_configured: true`，仅注册定义但未配置的项标为 `false`；这足以推导持久成员和添加候选，无需新持久化字段。
- 摘要会合并用户目录覆盖、插件定义和登录状态；必须继续用其既有 `is_configured`/删除能力语义，不能把全部摘要键都当左栏持久成员。
- 设置窗口明确拥有独立的页面级 store 和独立 JavaScript 上下文，不能直接复用主窗口内存中的模型数组。正确“统一来源”应是：两边都以运行时 `/api/models` 为服务端真理源，设置窗口维护一份共享于所有辅助组件的模型资源，并订阅现有跨窗口事件后重载，而不是每个组件私抓。
- 主窗口的 `models-changed` 已稳定走统一加载动作；主进程也已有把 `settings-changed` 广播到主窗口和设置窗口的 IPC 通道，但尚需确认服务端事件是否会转发给设置窗口，或供应商写成功后是否需要沿已有通道通知。
- BUG 3 的断点已经确认：每个辅助模型组件各自持有私有模型数组，挂载和再次打开下拉时各自请求运行时模型接口；设置窗口没有共享模型资源，也不响应 `models-changed`。因此已打开的多个辅助下拉不会随运行时事件同步，只能靠组件自身再次抓取碰巧修复。
- 设置内容当前只把 `skills-changed` 从跨窗口 IPC 转成浏览器事件，忽略 `models-changed`；主窗口处理服务端 `models-changed` 时也只刷新自己的模型 store，没有把该事件转发给设置窗口。现有 IPC 广播机制可复用，无需新增事件体系。
- 服务端配置写路由严格按顺序执行：先写 Provider Catalog，再等待运行时刷新，刷新成功后才发 `models-changed` 和返回 `{ok:true}`；刷新抛错会返回 500，不发事件。这条失败关闭顺序符合任务要求，前端必须真正检查响应。
- 模型列表的添加、删除和自定义添加三条路径完全忽略 `/api/config` 的响应状态与 JSON，随后无条件刷新 UI；这是 M4/T7 的直接根因之一。API Key 保存助手只检查 JSON error、不检查 HTTP status；自定义新增和供应商删除同样缺 HTTP 检查。
- 供应商目录删除实现已有正确区分：用户覆盖被删除；本地自定义插件同时移除；内置插件声明保留并重建注册定义。因此前端只需正确重载权威摘要，不能另造“永久删除定义”。
- HTTP 非成功状态已由设置窗口统一请求封装在返回组件前抛错；真正不一致的是部分调用方不检查 HTTP 200 中的 JSON `error`。修复应复用或增加最小 JSON 成功读取助手，避免每条写路径各写半套判断。
- Provider Catalog 的准确前端投影是设置配置中的 `providers` 键集合：服务端明确只返回用户目录覆盖，并为本地自定义插件合并其完整定义；内置/plugin 仅声明而未配置的项不会进入该集合。它比 `has_credentials` 更准确，因为空 Key 或缺模型的持久配置也必须显示。
- 草稿语义已由交互和写路径裁决：点内置/注册候选只改页面局部状态，不写后端；首次保存才写入 Provider Catalog。故草稿在页面卸载后不保留，必须可在当前页面取消，并在保存成功后转为持久项。

### Phase 1 状态源审计结论

| 状态 | 权威源 | 写入方 | 读取方 | 生命周期 |
| --- | --- | --- | --- | --- |
| 持久供应商成员 | Provider Catalog，经 `settingsConfig.providers` 投影 | `/api/config` → Provider Registry | Providers 设置左栏 | 跨 tab、窗口和应用重启 |
| 可添加供应商定义 | 内置/插件注册定义与 summary | 启动注册、插件加载 | 添加供应商候选层 | 随安装和插件生命周期 |
| 当前选择 | `selectedProviderId` | 设置导航/用户点击 | 右栏编辑态和选中样式 | UI 导航状态，不定义成员 |
| 临时设置草稿 | Providers 页局部 draft id 集合 | 用户从候选层点选 | 当前页左栏/右栏 | 保存后转持久；取消或卸载即销毁 |
| 已启用模型 | Provider Catalog 中 provider models | 配置/模型写路由 | Provider Registry 投影 | 持久化 |
| 远端发现模型 | `models-cache.json` | Fetch Models discovery | Provider 模型候选层 | 缓存，不代表启用/运行时可用 |
| 运行时模型目录 | `engine.availableModels`，经 `/api/models` 暴露 | Provider 变更后的同步刷新 | 所有模型选择 UI | 运行时权威状态 |

- BUG 1 漂移路径：页面局部点选历史被误当成员集合，重挂载时又只从当前选择恢复一个 id；真实目录从未丢失，但左栏脱离目录。
- BUG 2 漂移路径：草稿没有取消动作；持久删除只更新后端和当前选择，局部点选历史不收敛，所以留下幽灵行。
- BUG 3 断点：配置写入 → 运行时同步 → `models-changed` → 主模型 store 全部存在；设置窗口没有接事件，每个辅助组件各自持有一次性抓取结果，形成六份短生命周期副本。
- 目标收口：左栏成员 = 持久目录键集合 ∪ 当前页草稿集合；选择独立。设置窗口建立一份运行时模型资源，所有辅助组件直接订阅；主窗口收到服务端既有事件后沿既有 IPC 广播给设置窗口并触发重载。

## 2026-08-10：真实界面点击复测

- 用户在真实设置界面确认辅助模型列表仍不同步，说明上一轮组件测试和事件单元测试没有覆盖实际运行窗口、真实点击与真实通信链，先前完成结论作废并重新打开任务。
- 本轮必须以实际页面点击为准：在供应商模型界面新增或删除模型，再直接打开已挂载的辅助模型下拉框观察；同时记录请求、应用事件、跨窗口消息和模型接口返回，不能再用直接改测试状态代替用户操作。
- `planning-with-files` 恢复脚本再次只命中无关旧问答；当前工作树就是上一轮尚未提交的修复，必须在其上诊断并保护已有改动。
- 桌面桥本身确实同时暴露了“发送设置变化”和“订阅设置变化”，主进程也会把该消息广播给主窗口和设置窗口；上一轮假设的接口并非测试虚构。下一步要验证真实服务事件是否一定先到主窗口、再由主窗口转发，以及设置窗口在该时刻是否仍正确订阅。
- 仓库有独立的设置页面入口和 Vite 开发入口，也有 Electron 开发启动脚本；真实点击应优先连接正在运行的 Electron 页面，避免只在普通浏览器里伪造桌面桥。
- 现场已有一套真实开发应用在运行：Vite 占用 5173，Electron 与独立服务均已启动，使用 Node 24.16.0；它没有开启远程调试端口。不能为测试直接终止或改写这套用户现场。
- 隔离测试应复用现有 Vite，但用独立 `LINGXI_HOME` 和独立 Electron 调试端口再启动一套应用；这样既能点击真实 Electron 设置窗口和真实后端，又不会碰现有开发数据。
- 开发启动包装脚本会无条件把数据目录改回用户的 `.lingxi-dev`，所以隔离实例不能走该包装脚本；必须直接启动 Electron，并显式传入临时数据目录、现有 Vite 地址和远程调试端口。
- 找到一个高度可疑的真实界面差异：正常主窗口存在时，“设置”实际以内嵌弹层打开，并不创建独立设置窗口。主进程广播设置变化时会排除消息发送者；因此主窗口自己发出的模型变化通知不会回到同一主窗口，而上一轮只验证了独立设置窗口收到广播。这正好可能让真实内嵌辅助选择器继续保留旧目录，必须用真实点击先证实。
- 隔离 Electron 已成功以真实服务启动在临时目录，服务端口为 25378，Playwright 通过独立调试端口看到了真实 onboarding 页面；没有使用普通浏览器 mock，也没有接触用户现有 `.lingxi-dev`。
- 第一轮实际点击“开始设置”成功进入名字与记忆步骤，证明当前连接可以驱动真实 React/Electron 控件；接下来完成最小 onboarding 后在主窗口设置弹层复现模型同步。
- 名字步骤已通过真实输入和点击进入供应商步骤；该步骤在没有 Key 时直接点“下一步”的首次命令未返回可用页面输出，暂不判断原因，先重新侦察窗口状态，避免重复同一失败动作。
- 重新侦察确认不是应用卡死：“下一步”在未选择供应商时就是禁用状态；首次自动化错误来自等待一个不可点击按钮。供应商候选层已通过真实点击展开，包含无 Key 的本地 Ollama，可用它完成不含秘密的隔离 onboarding。
- 选择本地 Ollama并继续的组合脚本仍超过短等待窗口；同期服务日志只有上一阶段的记忆配置写入，没有供应商写入证据。后续把选择、继续、页面检查拆成独立动作，避免 Playwright 对页面切换的自动等待掩盖真实状态。
- 为避免把 onboarding 连接测试变成任务主体，已通过真实桌面桥触发最小完成流程；旧 onboarding 页面随即按设计关闭，Playwright 对旧目标的等待报 `TargetClosedError`。这不是业务失败，下一步重新连接新建的主窗口。
- 重新连接后真实主窗口已创建；通过真实 `Cmd+,` 键盘操作打开了主窗口内嵌设置弹层，页面包含供应商与全部助手配置。这证实用户实际使用路径确实是“同一主窗口内的设置”，不是独立设置窗口。
- 已实际点击“供应商”以及“配置模型”。初始隔离环境的六个辅助选择器都只显示跟随主模型或关闭选项，符合运行时目录为空的基线；截图已保存到临时证据目录。
- 已通过界面依次点击“配置 API”→“添加服务商”→“添加自定义供应商”，真实表单提供名称、地址、密钥、Headers 和协议字段；可用完全隔离的假地址与假密钥建立测试目录，不会触碰真实供应商账户。
- 已用真实输入和“添加”按钮持久化 `ui-sync-e2e`，界面显示成功提示和左栏条目。点击“添加模型”后，弹层内容不在设置对话框节点内，符合该控件通过页面级悬浮层渲染的实现；下一步改为从整个页面侦察并点击该层。
- 全页侦察时悬浮层已经关闭；源码确认它确实通过页面级 portal 渲染，并由定位钩子管理打开/关闭。下一步用单次脚本在点击后立刻定位 portal 输入并操作，避免跨脚本连接时失去打开态。
- 首次完整组合脚本超过工具等待窗口，但页面已经真实切换到“配置模型”；当前六个辅助选择器的界面文字里没有 `ui-model-e2e`。还需分别确认测试模型是否已进入持久目录和运行时 `/api/models`，才能把它定性为前端通知断链，而不是模型写入失败。
- 后端核对显示 `ui-sync-e2e` 已持久化且密钥存在，但模型数组和运行时目录仍为空，因此前一轮并未成功添加模型，不能拿辅助列表为空当同步缺陷证据。
- 分段脚本再次点击“添加模型”时 portal 没有保持可见。该控件处于可滚动设置区，Playwright 点击前的自动滚动可能触发控件自身“滚动即关闭”逻辑；下一次先显式滚动、等待稳定，再执行点击，不修改产品代码来迁就自动化。
- 显式滚动并等待后，真实“添加模型”portal 能稳定打开；它一度显示 `ui-model-e2e` 和已添加标记，说明前一条长脚本的异步动作确实继续推进过。
- 但随后直接读取当前文件和运行时接口仍返回空模型，辅助下拉也为空；这还不是有效的同步复现。必须先排除仍在后台的旧 Playwright 进程或读错目录结构，并以一次明确捕获到成功写响应的点击建立稳定前置条件。
- 已排除后台 Playwright 进程。真实点击写入后，供应商摘要和配置接口都明确返回 `ui-model-e2e`、配置状态为正常，但运行时 `/api/models` 仍是空数组；因此用户现象已被真实界面复现，断点首先发生在“已保存模型 → 运行时模型目录”，不是单纯辅助下拉重绘。
- 原始 `provider-catalog.json` 的供应商条目没有模型字段，但配置接口又能返回模型，说明模型可能另存于模型目录文件后再由配置快照合并。下一步定位实际存储与运行时刷新读取的来源是否不一致。
- 已定位实际存储：自定义供应商会生成本地 provider plugin，模型定义写入插件文件并正确投影到 `models.json`；Provider Catalog 只保留用户覆盖和凭证，因此其原始条目不带模型是刻意设计，不是丢数据。
- `models.json` 已正确包含 `ui-sync-e2e/ui-model-e2e`、地址、协议和运行时凭证占位，但模型运行时返回空。服务端配置路由确实等待了统一刷新且仍返回成功，说明下一断点在 SDK 模型运行时的凭证/刷新顺序或动态 provider 注册，而不是配置保存或投影。
- 根因已命中：运行时密钥写入门面是异步方法，但模型管理器在初始化和重新同步时都没有等待它完成，就立刻刷新模型运行时。真实 SDK 因此在“密钥尚未到位”时判定模型不可用，随后密钥虽写入却没有第二次刷新；单元测试把该方法模拟成同步函数，掩盖了这个竞态。
- 门面源码确认“设置运行时密钥”和“移除运行时密钥”都返回 Promise；模型管理器的两个调用点均未 `await`，而现有相关测试主要检查最终凭证或使用同步 mock，没有断言“密钥完成后才能刷新”的顺序。
- 补齐等待后，异步顺序测试转绿，但全新真实界面点击仍得到“模型写入 200、运行时目录为空”。因此漏 `await` 是确定存在的竞态，却不是用户现象的全部根因；不能在此提前宣布完成。
- 下一步比较同一份配置在全新 ModelManager 初始化与进程内 reload 两种路径的结果：若冷启动可见而热刷新不可见，断点在动态注册/刷新；若冷启动也不可见，则继续查 SDK 凭证可用性与投影契约。
- 修复后现场再次确认投影和本地插件定义都完整；服务端正式日志明确记录 `onProviderChanged OK after provider change (0 models)`。也就是说路由把“刷新流程没有抛异常”误当成成功，但刷新后的模型数量仍为 0；继续做冷启动对照。
- 修复方向是让运行时密钥覆盖步骤返回并等待所有异步写入完成，再执行模型注册/刷新；需要先补一个使用真实异步时序的红灯测试，随后用同一套界面点击证明 `/api/models` 和辅助下拉都自动出现模型。
- 新增异步屏障测试在旧代码上稳定红灯：密钥 Promise 尚未释放时，模型注册表刷新已被调用 1 次；1 项失败、29 项按过滤跳过，失败点正是预期顺序断言。
- 顺序修复后的同一测试已转绿：1 项通过。全新隔离实例也已成功通过界面写入测试供应商，服务端返回 200；自动化随后只因同名文字同时出现在左栏和标题而触发严格选择器错误，属于测试脚本定位问题，不是产品失败。
- 改用标题语义后又命中了隐藏副本，但只读侦察确认真实详情和“添加模型”按钮都可见；后续不再等待不参与操作的标题，以可见按钮作为稳定前置条件。
- portal 的打开态会跨短脚本保留，盲目再点可能把它关闭。改为先读当前状态、仅在关闭时点击后，真实悬浮层稳定可见并显示“暂无模型”和自定义输入入口。
- 冷启动对照确认同一份插件与 `models.json` 能正常产出测试模型；只有进程内连续新增供应商、再新增模型的热刷新路径会丢失，持久化格式和模型合法性已经排除。
- Pi SDK 的模型目录刷新本身会重新读取 `models.json`，但供应商注册和注销会额外启动无法由调用方直接等待的后台刷新。旧刷新与显式刷新并发时，最后结束的旧快照会覆盖新快照。
- 使用与真实界面相同的不可访问本地地址后，新增的热刷新测试能命中该竞态；可访问形态的 HTTPS 假地址会因时序不同偶尔掩盖它，说明测试必须保留事故现场条件。
- 适配层现已把同一运行时上的所有目录刷新串行化；显式等待的刷新同时成为此前后台刷新的完成屏障，且某一次刷新失败不会阻断后续刷新。相同回归用例并行运行 8 份全部通过。
- 全新 Electron 现场验证第一段已修复：真实添加模型请求返回 200，服务日志从 `0 models` 变为 `1 models`，产品自身的 `/api/models` 请求也返回该模型。
- 同一现场继续证实第二段断链：主聊天区已进入“请选择模型”，但已挂载设置弹层的辅助下拉仍为空。原因是主进程广播刻意排除事件发送窗口，设置页又恰好作为该主窗口内的 modal 存在。
- 同窗口浏览器事件补齐后，设置弹层会直接重载共享运行时目录；独立设置窗口仍复用原 IPC 通道，不新增持久状态或服务端事件。
- 真实删除模型后，产品自身连续三次读取模型目录均返回 0，已挂载辅助下拉立即移除选项；真实重新添加后连续三次返回 1，辅助下拉立即出现格式化模型名，并可实际点击选中、成功写入辅助模型偏好。

## 2026-08-10：skillBadge 消息持久化修复

- 用户报告指出实时消息已有技能列表，丢失发生在展示记录写入、历史投影、重试/分叉还原三段；本轮先以当前源码和测试独立复核，不直接照抄结论。
- 当前工作区已有一组供应商配置相关未提交修改；本 Bug 必须避开这些文件，只在消息展示持久化链路和对应测试中做最小改动。
- 正式验证使用仓库要求的 Node 24；若共享依赖出现类型目录污染，不能把环境假失败归因于本次源码。
- 当前源码独立复核确认三个断点全部存在：展示记录的结构化守卫与数据体都没有技能列表；历史消息投影不输出技能列表；重试/分叉还原也不读取技能列表。
- 实时事件已经从展示消息读取技能列表，提示词拼接和前端消费也已有实现，因此正确修复边界就是这三处，不需要改实时链路或界面。
- 三个目标源码文件目前都没有未提交差异，可在不覆盖现有供应商配置改动的前提下安全修改。
- 现有测试入口分别是 `tests/desktop-session-submit.test.ts`、`tests/sessions-route.test.ts`、`tests/session-turn-actions.test.ts`；下一步复用已有结构化展示断言补三段回归。
- 写入测试已有“审阅展示记录”用例，可在同一展示消息中加入技能列表并直接断言自定义条目保存该数组；重试测试已有完整展示信封还原用例，可在同一夹具中加入并断言技能列表。
- 历史路由测试集中已有多组真实会话文件到 `/api/sessions/messages` 的投影用例；需要选取其中展示记录最接近的一组，避免新建重复的大型夹具。
- 历史路由的时间戳投影用例使用最小引擎和两条普通消息，适合紧邻新增一个展示记录加用户消息的独立测试；这样能直接断言重载接口返回技能数组，不与后台任务等复杂投影耦合。
- 路由测试把消息读取工具替换为可控夹具，展示记录配对逻辑仍执行真实代码，因此能准确覆盖本 Bug 的读取断点。
- 进一步核对发现：展示信封还原函数只被“重试”调用；“分叉”不是通过该函数重发消息，而是把目标边界前的原始会话条目整体复制到子会话。因而写入记录一旦包含技能列表，分叉会自然保留；历史投影测试随后覆盖子会话读取所需的同一投影逻辑。
- 因此三处实现修复仍正确，但测试命名应准确：第三条证明重试还原；分叉保障来自“展示记录被原样复制 + 通用历史投影”，不虚构不存在的调用链。
- 红灯实测：Node 24 定向运行三个测试文件，165 项中 162 通过、3 失败。失败差异分别显示自定义展示条目无 `skills`、历史用户消息无 `skills`、重试提交的展示消息无 `skills`，与三个源码断点一一对应。
- 最小实现只新增四个对称点：展示记录守卫识别技能数组、展示记录保存技能数组、历史用户消息投影技能数组、重试展示消息还原技能数组；没有改提示词、实时事件、前端渲染或存储格式版本。
- 同一组三文件测试转绿：`3 passed`、`165 passed`、0 失败，说明三条新增断言和原有 162 项行为同时成立。
- 三段类型检查退出 0。六个目标文件的 lint 退出 0、0 errors；182 个 warning 全是这些大文件原有规则债务，新增行没有命中新 warning。
- 差异复核发现现有写入测试虽然证明了技能数组进入记录，但夹具同时带有审阅字段，不能单独证明结构化守卫已把“只有技能列表”视为值得持久化；需要再补一个纯技能展示回归，防止未来只保留数据字段却误删守卫条件。
- 纯技能守卫用例加入后，定向测试为 `3 files / 166 tests` 全绿；目标文件 lint 回落到原来的 182 个 warning、0 errors，确认本任务没有新增静态检查警告。
- 首次低并发全量测试命令无效：虽然入口是 Node 24 调 npm，测试子进程实际使用系统 Node 22。原生模块编译接口为 137（Node 24），失败进程要求 127（Node 22），造成 28 文件、155 项级联失败；该结果不能归因于源码，也不能当成通过。
- 现场独立核对：Node 24.15.0 的原生模块接口为 137，系统 Node 22.23.2 为 127。正确复跑必须由 Node 24 直接执行 Vitest 入口，而不是间接走 npm 子进程。
- Node 24 直接启动的低并发全量测试退出 0：1089 文件通过、1 文件既有跳过；11043 项通过、7 项既有跳过、0 失败。此前的 155 项失败确定为错误运行时造成的无效结果。
- 最终行为闭环：新消息写入展示记录时保存技能数组；会话历史接口把数组还原到用户消息；重试重新提交时保留数组；分叉复制同一展示记录，子会话再走已修复的通用历史投影。

# 2026-08-13：只读终端输出查看器

## 已确认需求与边界

- 产品定位是只读、能理解 ANSI 样式的终端输出查看器，不是可输入或能模拟完整屏幕的终端。
- 生命周期状态必须直接来自终端会话管理器的公开快照；禁止经活动中心转换，也禁止修改 `lib/activity-hub.ts` 和 `desktop/src/react/stores/agent-activity-slice.ts`。
- 生命周期原生状态保持 `running / exited / killed / stale`；界面只在展示层映射为运行中、已完成、失败、已终止、已失效。
- 实时输出要保留每个块的序号；服务端按 50ms 与约 32–64 KiB 双阈值合并，结束或关闭状态广播前必须先发送尾部输出。
- 新客户端和重连客户端必须主动请求终端快照；历史输出只有展开时才请求有界尾部，不能从头读取无限大的记录。
- V1 只向本机所有者连接开放终端消息和请求，不借用聊天读取权限向远程客户端泄露本机终端内容。
- 前端低频元数据进入独立会话状态；高频输出只进入按终端订阅的轻量通道，无订阅时丢弃，不触发全局状态更新。
- 展开顺序必须先订阅实时流、再请求历史尾部，并通过序号处理重复、乱序和缺口；发现缺口时从最后已知位置补读。
- 每个展开预览独立维护流式 ANSI 解析状态，危险 HTML 必须作为文本或受控链接处理；不预先按行切分原始块。
- 卡片只展示全部运行中和最近 10 个非运行终端，不删除持久化记录；名称优先级为标签、命令、终端编号。
- 根依赖清单和根锁文件是唯一安装位置；若引入 `ansi_up`，必须从仓库根目录锁定版本。
- 性能基准与正确性实现分开；只有现场数据证明同步元数据写入是瓶颈时，才另立持久化节流任务。

## 初始现场

- `planning-with-files` 恢复报告来自 2026-08-12 的旧任务，没有本目标的未同步计划内容。
- `git status --short` 本轮初始没有输出，说明开始调查时工作树干净；后续仍需用完整状态和 HEAD 再确认。
- 仓库只发现根 `AGENTS.md`，用户消息已给出其完整规则；暂无更深目录规则需要叠加。
- 记忆索引没有命中本功能的直接历史实现；本轮不把旧发布/启动结论当作终端查看器事实。

## 待源码回答的问题

1. 当前公开终端条目的确切字段、状态转换、关闭后再退出的防回跳规则和持久化恢复时机是什么？
2. 服务端如何拿到当前引擎、稳定会话编号和本机所有者连接身份？
3. 现有消息发送与会话订阅过滤在哪一层执行，客户端请求是否已有统一验证入口？
4. 前端发送消息、重连钩子、会话范围键和右侧栏测试夹具应复用哪些现成模式？
5. 当前测试环境是否已有 DOM、滚动、定时器和消息连接替身，可避免新增平行基础设施？

## 终端会话管理器初查

- 当前公开条目已经包含终端编号、会话路径、代理、目录、命令、标签、原生状态、最新序号、三组时间、退出码、信号和 transcript 文件路径，但没有稳定会话编号。
- 启动流程在后端成功创建伪终端后写 metadata 并发送 `terminal_started`；每个输出块同步追加 JSONL、同步重写 metadata，再发送 `terminal_output`。
- 普通 `read()` 会把完整 JSONL 一次性读入内存后按序号过滤，确实不适合作为界面首次展开的无界历史读取；目前没有尾部 API。
- 关闭流程先把状态设成 `killed`，再调用后端释放；后端退出回调只在原状态仍为 `running` 时改成 `exited`，因此关闭后不会回跳成完成。但若后端同步触发退出，事件顺序会是 `terminal_exited(killed)` 后 `terminal_closed(killed)`。
- 重启加载会把持久化的 `running` 改为 `stale` 并立即落盘；退出或终止记录及 transcript 均保留。
- 同一稳定会话编号可以容忍会话文件路径迁移：索引与所有权检查优先使用注入的 path-to-id 解析器，访问新路径时会更新并持久化条目路径。
- 当前事件发送器只给出事件类型、终端编号、状态、最新序号和输出增量；生命周期事件没有完整公开快照，正是目标要求补齐的缺口。
- 当前测试已覆盖会话所有权、缺失目录、写入、分会话关闭、路径迁移、释放契约和重启失效恢复；尚未覆盖完整事件快照、有界尾部、大小/块数限制及关闭后的事件顺序。

## 服务端消息与权限初查

- 引擎创建终端会话管理器时，事件直接进入引擎公共事件总线；聊天路由已经单次订阅该总线，所以实时终端转发应直接在现有订阅分支处理，不需要活动中心。
- 广播函数会给所有带会话路径的消息补运行时工作区编号，并在扇出前只解析一次稳定会话编号；这可以直接复用到终端状态和输出。
- 本地所有者连接在收发权限判断中目前直接放行；远程连接的会话消息通常按聊天读取权限和订阅投递。终端 V1 必须在权限函数中增加显式消息集合：本地放行后，远程对终端入站请求和出站消息一律拒绝。
- 消息路由在调用具体分支前统一执行入站权限判断，并在有会话路径时登记会话订阅；终端快照与尾部请求可沿用统一会话身份解析，拿到规范后的路径和稳定编号。
- 现有终端事件在总线分支中没有消费位置，位于子代理活动分支附近可做独立处理；这不改变活动卡片职责。
- 现有聊天路由测试有轻量真实消息连接夹具，可以直接驱动总线订阅者、连接打开和消息到达；无需新建服务器进程。
- 现有 `ws-scope` 测试已经区分本地所有者、局域网设备、工作区订阅和会话订阅，适合补终端远程拒绝的正反断言。

## 初步服务端实现裁决

- 输出合并状态应独立于聊天流状态，按终端编号保存 pending chunks、字节数和定时器；结束/关闭事件处理前按该编号同步 flush。
- 高水位按 UTF-8 字节计算，不能用字符串字符数代替；每个 chunk 保留原序号和原始文本。
- 状态消息携带完整公开条目、规范会话路径和稳定会话编号；输出消息携带终端编号、会话身份与 ordered chunks，不夹带低频 metadata。
- 快照与尾部请求只给请求者单播，避免一次展开让同会话其他窗口重复收到历史内容；实时状态和输出继续广播给所有本地所有者连接。

## 前端骨架初查

- 主消息连接是模块级单例；每次连接成功后已经统一执行聊天流恢复和当前会话上下文刷新。终端快照恢复应从同一个连接成功钩子发起，才能覆盖服务端重启和网络重连。
- 右侧栏组件在非紧凑模式下始终挂载辅助卡片；在这里把终端卡放到工作流卡之前即可保持紧凑模式不展示的既有语义。
- 当前会话状态提供稳定会话编号到路径的双向定位辅助；终端低频状态可以完整复用 `sessionScopedKey` / `sessionScopedValue`，避免归档换路径丢卡。
- 当前消息处理器会先校验消息携带的稳定编号与已知路径是否冲突，再进入具体类型分支。终端状态、快照和输出都能自动获得这层防串会话保护。
- 现有全局状态是显式组合各 slice；新增终端 slice 需要同步加入总状态类型、初始化组合和类型导出，没有自动注册。
- 当前连接单测只验证流式会话恢复目标；需要扩展为连接建立时当前会话终端快照请求，并证明携带稳定会话编号。
- 右侧栏综合测试会替换其他辅助卡片，新增终端卡后也需要替换，避免与该测试的工作台职责混杂；终端卡和预览应有独立组件测试。

## 初步前端实现裁决

- `terminal-client` 只负责用当前打开的消息连接发送快照与尾部请求，并在连接未开放时返回失败；连接成功钩子与卡片会话变化钩子都调用它。
- 高频通道应负责每个终端的订阅、序号去重、乱序暂存、缺口通知和历史尾部合并；界面组件只保存当前终端已经交付的有序原始块及缺口回调状态。
- 为避免“先收实时块、后到尾部覆盖”，通道订阅要在请求历史前建立；tail 处理应与已见实时序号合并并只交付连续的新块，不能清空已交付内容。
- 终端 metadata slice 的快照替换必须按消息所属会话写入，而不是当前焦点会话兜底；状态 upsert 也拒绝缺少会话路径或终端编号的消息。

## ANSI 与滚动事实

- 官方包页当前显示 `ansi_up` 稳定版为 `6.0.6`、零依赖、内置类型；与目标文件给出的版本一致。
- 官方说明明确要求同一个实例连续接收每段文本并把每次生成的 HTML 追加到旧输出；不完整的转义序列和链接序列会在实例内缓冲，正好覆盖跨 chunk 状态。
- 默认 `escape_html=true` 会转义 HTML 保留字符；默认链接协议白名单只有 `http` 和 `https`。实现仍要显式保持这些安全值，并用恶意图片、脚本和链接序列做回归。
- 该库只解释样式与链接，光标移动会被忽略并从输出隐藏，因此符合“输出记录查看器”而不是完整终端模拟器的产品边界。
- 项目已有经过完整单测的连续粘底 Hook：内容增长时平滑跟随，用户上滚即停止，能强制回到底部并处理异常尺寸。终端预览可直接复用，不另写滚动状态机。

## Phase 2 实现结果

- 新共享契约统一了四类服务端消息、两类客户端请求、原生状态、公开条目、输出块与尾部读取上下限。
- 公开条目现在包含稳定会话编号；解析器暂时拿不到时会保留已持久化编号，兼容重启加载。
- 生命周期三类事件现在携带完整公开快照；高频输出事件仍只有终端编号、状态、序号和文本，不重复 metadata。
- 新尾部读取默认 64 KiB / 500 块，服务端硬上限 256 KiB / 2000 块；从文件尾部读取固定扫描窗口，不随完整 transcript 变大而扩大内存。
- 未传起点时取最近历史；传起点时补该序号之后的窗口；任何省略或单块裁剪都会明确返回 `truncated=true`，不伪装完整。
- 单块超过字节上限时只返回 UTF-8 边界安全的尾部，并给该块标 `truncatedStart`；普通模型工具的完整 `read()` 没有任何语义变化。
- 权限层已用共享消息集合明确失败关闭：本机所有者可收发终端消息，远程连接即使有完整聊天权限和会话订阅也不能收状态/输出/快照/尾部或发读取请求。

## Phase 3 实现结果

- 新服务端桥独立消费终端领域事件；聊天解析器、聊天流恢复缓存和活动条目均不参与终端转发。
- 输出按终端编号分别积累，默认最多等待 50ms，累计达到 64 KiB 立即发送；字节计算使用 UTF-8 实际字节数。
- 每批输出保留所有原始 `{seq,data}`，没有把多块压成一个假序号；不同终端各自有定时器和缓冲区。
- `terminal_exited` 与 `terminal_closed` 在状态广播前同步清空该终端 pending 输出，测试同时钉住两类事件和定时器不重复发送。
- 生命周期状态广播会统一补稳定会话编号，并把快照里的会话路径/编号对齐到服务端解析后的权威身份。
- 快照和尾部只单播给请求连接；尾部请求的大小/块数由服务端固定，客户端不能放大读取窗口。
- 聊天路由在进入聊天流状态机前直接消费终端事件；入站快照/尾部请求先过统一权限与稳定会话身份门禁，再调用终端桥。

## Phase 4 实现结果

- 新终端 slice 只保存低频公开条目，支持快照整桶替换、单条状态更新、清空、稳定会话编号和路径迁移；高频输出没有字段或 action 可写入该 slice。
- 新输出通道仅在有订阅者时创建每个预览的短期缓冲；无订阅者时实时消息立即丢弃，折叠卡片不积累内存。
- 订阅后、初始历史到达前的实时块先暂存在该订阅者内；历史到达时以实时完整块优先，按序号合并、去重并连续交付，已用 `tail 1/2 + live 2/3` 锁住竞态。
- 连续流发现缺口只通知一次；补读历史到达后会合并缺失块并继续排空原先挂起的实时块。若服务端明确历史窗口已截断且缺口无法补齐，则用最近窗口做显式 reset。
- 通道键使用稳定会话编号（没有时才回退路径）加终端编号，阻止同名终端跨会话串流。
- 新请求客户端不缓存请求；连接未开放直接返回失败。快照与尾部均携带稳定会话身份，缺口补读可携带最后序号。
- 主消息处理器对高频输出只做不写状态的身份一致性检查，然后直接交给输出通道；测试用全局状态写入 spy 证明该分支没有调用 `setState`。
- 连接每次成功都会请求当前会话终端快照，覆盖断线和服务端重启；会话切换后的请求将在卡片挂载阶段补齐。

## Phase 5 依赖与组件事实

- 已从仓库根使用精确版本安装 `ansi_up@6.0.6`；`package.json` 只新增一条依赖，根锁文件新增 10 行，`npm ls` 确认只有该直接版本。
- 安装过程提示本地 `node_modules` 清掉 190 个多余/平台项，但 Git 受控依赖文件只有预期两处变化；后续完整门禁会验证依赖树没有受损。
- 安装后的审计现场为 8 项（1 low、7 moderate）；本任务不运行会扩大或破坏版本范围的自动修复，最终会如实保留审计结果。
- 实测流式解析：第一块 `ESC[3` 不输出，下一块 `1mERROR` 正确生成红色 span，证明必须保留同一解析实例。
- 实测危险图片和脚本字符串会被转义为普通文本；把链接协议白名单设为空时，终端提供的链接序列不产生可点击链接，适合 V1 最小权限。
- 右侧卡片现有折叠组件在关闭时会卸载子树，因此行展开收起会自然创建/销毁输出订阅；卡片整体折叠也会移除所有预览订阅。

## Phase 5 实现结果

- 新终端卡会在当前会话变化时请求快照；没有终端时不占右侧栏空间。右侧辅助卡顺序已固定为终端、工作流、子助手、本次对话，紧凑模式全部不挂载。
- 展示名严格按标签、命令、终端编号回退，同时仍在次行展示真实命令、工作目录和退出码。
- 状态只在界面层从原生状态推导：运行中、成功退出、失败退出、已终止、已失效；失效提示使用目标文件指定文案。
- 展示集合是所有运行中加最近 10 个非运行终端；再按运行中、失效、失败、终止、完成和创建时间倒序排列，不删除持久化数据。
- 预览挂载顺序是先订阅实时流、再请求尾部；收起行或整卡会卸载预览并取消订阅。
- 每个预览独立持有流式 ANSI 解析器；跨块样式测试已通过。HTML 保持转义，终端链接白名单为空，图片、脚本和链接回归都没有生成可执行节点。
- 预览复用连续粘底 Hook；组件测试证明首次落到底部，用户上滚后新输出不再抢回底部。
- 五份语言包已补同一组 10 个终端键，并通过语言键一致性与覆盖测试。
- 初始尾部请求的空起点现在保持 `null`，不会误编码成序号 0；服务端、前端通道和客户端三处都补了空值规范化回归。

## Phase 6 实测结果

- 新增可重复命令 `npm run benchmark:terminal-ui`，使用真实终端会话持久化、服务端合并桥和前端输出通道，分别覆盖 10 MB、200 个细小高频块和 4 终端交错输出。
- 10 MB 连续突发：状态首发 0.721ms，10,485,760 字节合并为 160 批，展开态 160 次订阅交付，折叠态 0 次；总耗时 544.961ms，系统 CPU 448.488ms，显式事件循环探针最大停顿 475.086ms，落盘约 10.59 MB。
- 细小高频块：状态首发 0.38ms，200 块合并为 22 批，18.13 批/秒，展开态 22 次交付、折叠态 0；事件循环探针最大 2.044ms。
- 四终端交错：状态首发 0.513ms，4,194,304 字节合并为 64 批，展开态 64 次交付、折叠态 0；事件循环探针最大停顿 205.424ms。
- 因此约 20 批/秒的目标在“细小高频输出”场景成立；大带宽突发会按 64 KiB 大小阈值立即发送，这是防止单包无限增长的预期行为。
- 同步 transcript + metadata 落盘确实是突发场景瓶颈，尤其系统 CPU 与事件循环停顿明显；按目标约束不在本次混入 checkpoint 节流，作为独立性能任务处理。
- 真实 `node-pty` 烟测：成功命令 15.369ms 发出启动状态，ANSI 输出保留、退出码 0；失败命令退出码 7；关闭后保持 `killed`；模拟后端重启后原运行终端变 `stale` 且历史 `STALE-HISTORY` 可读；同一会话同时保留 4 条记录。

## Phase 7 门禁结果

- 首次原始全量测试为 11162 通过、10 失败、7 跳过；10 项失败全部是受控仓库门禁，分别要求登记两个新增开放模块、重新生成运行闭包、声明终端元数据新增字段的向后兼容性，以及把新样式的 19 处裸值改成设计变量。
- 没有修改门禁基线或放宽断言：开放清单新增两个真实运行模块；运行闭包重新生成后为 10562 文件（源码图 666、运行资产 11、运行追踪 9885）；持久化登记明确旧 metadata 没有 `sessionId` 仍可按 `sessionPath` 恢复，不提升数据纪元。
- 五个曾失败的门禁文件先单独复跑，75/75 通过；随后原始 `npm test` 全量为 1101 文件通过、1 文件跳过，11172 项通过、7 项跳过，退出 0。
- 三段类型检查退出 0；本次新增/修改核心文件的 eslint 为 0 errors、0 warnings；`npm run build:renderer` 退出 0（保留仓库现有构建 warnings）；`git diff --check` 退出 0。
- 禁止文件 `lib/activity-hub.ts` 与 `desktop/src/react/stores/agent-activity-slice.ts` 无差异；没有新增高频 Zustand slice、输入、终止、尺寸调整、完整屏幕模拟或移动端终端界面。

## 基线环境

- HEAD：`1cd20990addbfeee0fdf62e0d15630403f993ca9`，分支 `main`。
- Node `v24.16.0`、npm `11.13.0`，满足仓库 `>=24.12.0 <25`。
- `ansi_up` 当前未声明；若最终采用，需要更新根 `package.json` 与根 `package-lock.json`。
- 初始产品工作树干净；现在只有本任务三份小写计划文件发生修改。

## 错误与处理

| 问题 | 处理 |
| --- | --- |
| 手动创建目标失败，因为 `/goal` 已创建活动目标 | 读取并沿用现有目标；不重复尝试 |
| 计划技能恢复到无关旧任务 | 只保留当前工作树和文件存在性的现场事实，不继承旧任务结论 |
| 首次全量测试 10 项失败 | 失败不是产品回归，而是变更登记和样式纪律门禁；按门禁指导完成开放清单、运行闭包、持久化兼容声明和设计变量修正，未更新基线掩盖新增问题 |

# 2026-08-13：终端与子助手卡片交互重构

## 参考图提取

- 图一是深色右侧栏中的紧凑运行列表：区块标题旁显示后台运行数量；每项第一行是带终端小图标的命令标题，第二行左侧为“已运行 X 分 X 秒”，右侧为明确的停止按钮。
- 图一没有区块折叠按钮、单项展开箭头、命令路径、退出码或输出详情；项目之间靠留白分隔，不使用带边框的独立小卡。
- 图二是对话内的深色圆角内嵌详情框：首段以命令提示符开头并使用等宽字体展示完整多行命令；下方输出为较弱颜色，长文本自然换行。
- 两张图共同表达“右侧负责观察和控制、对话负责历史和详情”，因此终端和子助手都应使用同一信息分工。

## 待核对事实

- 终端右侧条目和聊天工具卡是否共享可持久化的终端编号。
- 子助手活动条目和对话卡是否共享任务编号/工具调用编号，以及现有停止入口如何校验会话所有权。
- 当前聊天虚拟列表是否已有按消息/块标识滚动和临时展开状态通道。

## 首轮源码事实

- 右侧子助手当前确实重复挂载完整子会话预览，并且区块与单项都有折叠状态；这正是本次要移除的重复详情层。
- 对话内子助手卡当前只有静态概览与停止按钮，没有把已有子会话预览作为展开内容挂回卡片；但预览组件本身已经支持历史加载、实时续传、工具块和只读聊天转录，可直接复用。
- 子助手对话卡已有安全停止入口，但它请求的现有路由只按任务编号直接终止，尚未看到父会话所有权校验；模型工具侧的停止实现反而已有稳定会话编号比对。界面停止不能照搬这个较弱边界。
- 右侧子助手活动记录与对话卡共享任务编号，且活动记录包含子会话路径，因此“点击右侧标题定位对话卡”可以使用任务编号做精确锚点。
- 终端现有公开记录只有终端编号和会话身份，没有工具调用编号；要把右侧终端精确跳到对应对话工具卡，必须沿执行工具结果或展示块补一条稳定关联，不能按命令文字猜测。
- 当前通用聊天资源卡原生支持可展开头部与详情子树，子助手可以在现有组件契约内增加展开，不必另造折叠基础设施。
- 实时 `tool_end` 只把工具结果的 `details` 推给前端；历史接口又只把工具调用的成败投影回 assistant 消息，没有把对应 `toolResult` 的命令输出投影到卡片。因此“所有终端命令可展开”需要同时补实时与历史的输出投影，不能只改组件样式。
- 现有 `ToolGroupBlock` 是对话中所有工具调用的统一入口；`exec_command` 可在这里单独渲染成可展开命令卡，其他工具保持原样，修改范围最小。
- `SubagentSessionPreview` 已经能加载并续接子会话输出；把它从右侧栏移入对话的 `SubagentCard` 就能复用现成详情界面，不需要再造一套子助手流。
- 右侧终端状态条目已有稳定 `terminalId`，但命令卡在工具开始时只稳定拥有 `toolCallId`。为了让“运行刚开始就点击标题”也能准确跳转，终端公开条目需要携带可选的 `toolCallId`；老记录仍可用 `terminalId` 回退匹配。

## 交互重构实现与真实界面结论

- 终端公开状态新增可选工具调用关联；工具启动时一次写入，旧 metadata 缺字段仍可加载。右栏优先用该关联定位，旧记录回退到终端编号。
- 所有 `exec_command` 都从普通工具折叠组中单独渲染，因此即使同一批其他工具已收起，命令卡仍可见；一次性命令从受限历史结果读取输出，持续命令从按终端订阅的有序输出流读取。
- 后台终端启动后工具调用本身会先结束，聊天卡状态不能跟着工具结果提前打勾；当前实现若终端仍运行就持续显示运行态，结束后再按真实退出状态显示成功或失败。
- 子助手右栏不再挂载子会话详情，只保留当前会话的运行项；聊天卡复用原有子会话预览，因此历史加载、实时续传和工具输出仍走一套实现。
- 两种停止都走同一条本机 WebSocket 请求/回执通道。服务端先用稳定父会话身份校验归属，再终止精确任务；远程连接在消息权限层直接拒绝。界面不乐观移除，等权威状态事件到达后才从右栏消失。
- 实时工具结束与历史重载都只投影白名单内的命令详情，文本输出保留尾部且限制为 64 KiB，不把任意工具内部字段或无界输出塞进消息状态。
- 真实 Electron 隔离验收中，右栏终端与子助手都呈现“标题 / 已运行 + 停止”两行，无 `aria-expanded`；点击标题后相应聊天卡自动变为展开，终端显示完整工作目录命令和多行输出，子助手显示子会话正文。
- 停止子助手的最后一道服务端门禁同时核对“归属当前稳定会话”和“任务仍处于活动状态”；完成、失败、取消或已终止的记录不会再次调用终止处理，避免界面状态竞态改变历史结果。
- 最终低并发全量为 1103 文件通过、1 文件跳过，11184 项通过、7 项跳过；三段类型、renderer 构建、修改源码静态检查、开放边界、差异格式和新增敏感信息扫描全部通过。

# 2026-08-13：技能调用消息流卡片

## 参考图提取

- 收起行由技能图标、状态“已运行技能”、技能名和展开箭头组成，是消息正文中的独立记录。
- 展开区域是同宽的圆角内嵌面板：先列“技能 + 名称”，再列“参数 + 说明”，随后以等宽纯文本展示实际技能内容。
- 用户特别强调位置语义：卡片必须出现在模型调用发生处，因此验收必须同时证明卡片存在和正文前后顺序，不能把卡片集中到消息顶部或底部。
- 卡片沿用前序“消息区所有卡片占满可用宽度”的统一要求，收起和展开都应同宽。

## 待核对事实

- 技能在运行时的真实工具名、参数字段和结果结构。
- 当前不显示是可见性过滤导致，还是实时/历史投影丢失，抑或两处同时存在。
- 连续工具调用是否会被合成一个工具组，以及这个工具组在文本块之间的落点是否稳定。
- 技能结果进入消息状态前是否已有大小上限和敏感字段白名单。

## 首轮源码事实

- 当前界面可见性规则没有直接隐藏名为 `skill` 的工具；它会隐藏子助手、文件暂存和已有独立产物卡承接的工具。因此“完全不显示”不能只凭过滤规则下结论。
- 对话中的工具统一由同一个工具组块渲染；工具组块处在消息块数组的原位置，持续终端命令会单独常显，其他单个工具直接显示、多个普通工具则进入同一折叠组。
- 通用工具行只显示工具标签、从调用参数提取的一小段摘要、状态和错误，不会显示任意工具返回正文；技能卡要展示实际内容，必须新增技能专用渲染和受限结果投影。
- 进程折叠统计与工具组使用同一可见性规则。新增技能卡后还要验证它不会被外层“思考过程”折叠吞到与真实调用位置不一致的地方。
- 当前 SDK 没有名为 `skill` 的模型工具。它在系统提示中明确要求模型通过普通读文件工具打开技能目录下的 `SKILL.md`；SDK 自己的终端界面会把这种读文件调用识别成技能调用并使用专用组件展示。
- 因而产品侧可靠的识别信号应是“读文件工具 + 目标文件名为 `SKILL.md`”，不能等待一个实际不存在的 `skill` 工具事件，也不能把安装技能工具误当成运行技能。
- 实时流在工具开始前会先把已到达正文落成文本块，再把新工具组追加到块数组；工具结束后原位补结果。这个路径天然保留“正文前 → 技能调用 → 正文后”的先后顺序。
- 历史重建目前把一条助手消息拆成“思考 → 全部工具 → 全部正文”，不保留同一消息内部原始内容块顺序；是否会影响真实技能调用历史，要继续检查服务端是否把工具前后模型生成保存为独立助手消息。
- 共享工具结果投影当前只对白名单内的终端命令保留 64 KiB 输出；普通读文件结果只留下成功/失败，没有正文。技能详情当前因此无法在实时结束事件或历史重载中取得技能内容。
- SDK 对模型侧技能调用的判定非常明确：读文件参数解析成绝对路径后，只要文件名严格等于 `SKILL.md`，就标成技能，展示名取父目录名；普通说明文件和项目规则文件走另外两种紧凑分类。
- SDK 的读文件工具本身已对文本输出按行数或字节数截断，技能卡仍不能直接信任任意结果；产品共享投影层应再以“工具名 + 参数路径”做白名单，并保留自己的硬上限。
- SDK 自带终端展示收起时是 `[skill] + 技能名`，展开后显示技能名和完整内容；用户参考图额外希望看到参数，因此桌面卡可把本次读文件参数独立列出，但不应虚构不存在的业务参数。
- 服务端实时开始事件会先把工具参数裁成前端摘要，结束事件再经共享投影发送成功、错误和允许的详情；当前结束投影没有收到工具名或开始参数，无法安全判断一个读文件结果是否真的是技能。
- 历史接口先按调用编号把助手消息中的工具使用与随后工具结果配对，再把成功状态和白名单详情挂回工具调用；当前配对层只看工具结果，仍缺对应工具名/参数上下文。
- 服务端持久化中的每次模型生成是独立助手消息，工具调用与工具后的下一段正文不会被压进同一条历史消息；因此历史构建器“工具先于该条消息正文”的现状恰好对应模型在同一生成中先说话、再调用工具时会发生重排，必须用保留内容块顺序的字段或更窄的技能位置方案修正。
- 当前实时链已经能原位更新同一工具组；只要共享投影把技能详情挂到对应结束事件上，界面无需另建脱离消息流的全局状态。
- SDK 的工具结束事件只带调用编号、工具名和结果，不回带开始参数；为了只投影真正的技能读取，实时服务端需要在调用开始时暂存已经裁剪过的安全参数，结束后按调用编号取回并立即清理。
- 前端流缓冲在服务端 `turn_end` 才释放当前消息绑定；其快照仍依赖整轮累计正文。若增加分段文字，必须保留这份整轮累计值，不能为了时序修复破坏断线重载。
- 服务端只在一个用户轮次第一次 `turn_start` 时广播“开始流式”，工具后的后续模型生成仍聚合进同一条前端助手消息。因此旧实时缓冲确实会把工具后的正文继续覆盖回第一段文字，最终把工具卡推到整段正文末尾。
- 安全参数摘要白名单包含技能读取所需的 `path` 和兼容字段 `file_path`，不包含文件正文；暂存这份摘要足以识别技能且不会扩大现有前端参数暴露面。
- 通用聊天资源卡已提供全宽、收起/展开、箭头和任务型底色，技能卡可直接复用；只需补技能专用详情布局，不需要再造卡片壳。

## 实现与回归结论

- 红灯阶段 6 个文件共 103 项中新增 8 项失败、原有 95 项通过；失败分别命中内容没投影、实时顺序错、历史顺序错、普通读文件行、外层折叠隐藏和多工具折叠隐藏。
- 共享投影只在“读文件工具 + 路径最终文件名严格为 `SKILL.md`”时保留文本，并再次限制为 64 KiB；普通文件正文仍不进入聊天状态。
- 实时服务端按工具调用编号暂存已经白名单裁剪过的参数，结束后立即取回并清理；并行同名读取不会串内容，跨轮次残留会被清空。
- 实时缓冲把工具前后正文拆成两个文本块，同时保留整轮累计正文和有序块快照；中途重载也能恢复“前文 → 技能卡 → 后文”，不会因断线合并回一段。
- 历史投影从原始助手工具块取得调用参数并按调用编号配对结果；历史构建只把技能调用放在该段正文之后，其他工具旧顺序不变。
- 技能消息显式成为外层过程折叠的边界；即便和其他普通工具处于同组、普通工具组已折叠，技能卡仍单独可见。
- 复核扩大到 9 个文件、295 项全部通过；三段类型检查在修正一次显式类型收窄后退出 0，断线快照两套 112 项再次通过。

## 真实界面验收

- 复用用户已运行的 Vite renderer，只添加并随后删除一个临时验收入口；没有停止或改动用户的开发进程。
- 使用实际 `AssistantMessage` 和实际工具组组件渲染“前文 → 技能调用 → 后文”，不是静态仿图；点击收起行后实际 `aria-expanded` 变为 `true`。
- 实测纵向坐标依次为 96.09、126.34、186.34，证明卡片处在两段正文之间；收起宽度、展开宽度和消息宽度均为 730.47 像素。
- 展开后能看到技能名、JSON 参数、`<skill_content>` 包裹的实际正文；控制台错误为 0。视觉复核发现深色主题下参数对比度不足后，改用详情面板自身文字变量并重跑通过。

## 最终门禁

- 修改文件定向 lint 退出 0，renderer 正式构建完成；构建仅保留仓库已有的旧脚本语法、静态脚本和大分块提示。
- 原样全库 lint 唯一命中被忽略的 `build/.cli-closure-nft-scratch-*` 合并临时副本，共 2895 项均为打包产物缺少运行环境全局量；该文件不属于本次改动，也未被删除。排除这份临时副本后，其余全库源码 lint 退出 0。
- 最终低并发全量为 1104 文件通过、1 文件跳过，11200 项通过、7 项跳过；三段类型检查退出 0。
- 全工作树差异格式检查退出 0；本次技能链路 81248 字节差异中新增可疑密钥/令牌行数为 0；临时视觉验收入口已删除。

# 2026-08-13：技能卡参数语义修正

## 参考图复核

- 参考图中技能为 `leader`，其“参数”是一整段具体产品改造要求，内容与用户任务一致；不是 `SKILL.md` 路径，也不是读文件工具的参数对象。
- 图中随后才出现 `<skill_content name="leader">` 与技能规则正文，说明展开区明确区分“用户要做什么”和“技能规定怎样做”。
- 因此上一版把底层读文件参数直接 JSON 化展示，虽然能证明技能识别来源，却错误暴露了内部实现，并改变了“参数”的产品语义。

## 待核对

- 当前前端一轮流式消息是否能直接取得触发轮次的用户消息文本。
- 历史消息是否带有稳定的父子轮次关系，还是需要在构建聊天记录时按相邻用户消息投影。
- 用户消息若包含技能标签、附件或结构化展示字段，参数行应该采用哪一份纯文本，才能既完整又不泄露内部结构。

## 前端消息结构事实

- 技能卡目前只收到工具调用对象，因此只能看到读文件参数；展示层没有收到所属用户轮次的提示词，单改标签无法得到正确内容。
- 聊天消息本身保留用户纯文本，并为助手消息保留真实轮次输入条目标识及“该输入是否可见”的标记；这比按页面上最近一条可见用户消息盲猜更可靠。
- 对话渲染层掌握完整时间线，普通助手消息当前只把助手消息本身传给内容块；要展示配套提示词，需要在时间线建立一次“助手消息 → 真实用户输入”关联，再把纯文本向下传到技能卡。
- 历史中同一用户轮次可能包含多条助手消息，且外层过程折叠会把若干助手消息另行渲染；修复不能只取技能卡直属消息的紧邻上一项，也要覆盖折叠组内的助手消息。

## 关联方案结论

- 现有时间线已经在一趟扫描中按真实输入条目标识划分助手轮次，并保存该轮可重试的用户消息；在这里同时建立“每条助手消息对应的用户提示词”最小且一致，不需要把提示词复制进每个工具结果或持久化结构。
- 用户提示词应取历史构建后已经去除内部标记、附件标记和后台通知的可见纯文本字段；这样与消息气泡看到的任务内容一致，也不会把内部协议重新展示出来。
- 有真实输入条目标识时只接受标识匹配的用户消息；输入被明确标记为不可见时不回退到最近可见消息。旧记录缺标识时，才沿用现有轮次逻辑回退到前一条可见用户消息。
- 技能消息已被定义为外层过程折叠边界，正常情况下不会进入折叠面板；仍会把同一映射传给折叠渲染路径，避免未来规则变化再次丢失参数语义。

## 红灯证据

- 新增的组件与时间线测试共 30 项：27 项旧行为通过，3 项新增断言失败。
- 失败界面明确显示 JSON 路径对象，既找不到配套用户提示词，也会在隐藏输入轮次继续暴露路径；说明测试准确复现了用户指出的问题。

## 实现与定向回归

- 时间线按真实用户输入条目标识为每条助手消息计算配套提示词；实时消息缺持久化标识时沿用当前可见用户轮次，历史消息优先精确匹配持久化标识。
- 技能卡只接收已关联好的用户纯文本，不再序列化或展示读文件参数；内部路径仍保留用于识别技能名与取得技能正文。
- 后台隐藏输入、标识无法匹配或老历史没有文本时显示本地化“未记录该次调用的用户提示词”，不会猜用上一轮任务。
- 2 文件 30 项红灯已全部转绿；扩大到 9 文件 192 项全部通过，覆盖实时缓冲、断线快照、历史重建、过程折叠、完成动作和五语言一致性。三段类型检查与修改文件 lint 均退出 0。

## 真实界面验收准备

- 用户现有 Vite renderer 仍在 `127.0.0.1:5173` 运行；验收会复用它，不停止或重启用户进程。
- Vite 的真实根目录是 `desktop/src`，可添加临时独立入口并在验收后删除；界面使用实际聊天时间线、实际技能卡和现有 midnight 主题，不制作静态仿图。
- 既有 Playwright 验收脚本已遵循无头 Chromium、等待动态页面完成、点击展开、采集控制台错误和尺寸等要求；本次将在它上面增加“参数等于用户提示词、页面不包含路径”的断言并另存修正后截图。

## 真实界面验收结果

- 使用实际聊天时间线渲染一条用户任务及其所属技能调用；展开卡片后，参考图中的完整用户任务文本可见，内部 `SKILL.md` 路径在整个页面出现次数为 0。
- 展开按钮真实变为 `aria-expanded=true`，收起与展开宽度均为 664.23 像素，控制台错误为 0。
- 已保存修正后截图 `skill-call-card-prompt-fixed.png`；临时 HTML/TSX 入口随后删除，用户现有 Vite 进程未被停止或修改。

## 最终门禁

- 9 文件 192 项相关回归、三段类型检查、修改文件 lint、renderer 正式构建、差异格式和 48391 字节本次差异敏感信息扫描全部通过。
- 原样低并发全量中 1103 文件通过、1 文件跳过、1 文件失败；11203 项通过、7 项跳过、1 项失败。唯一失败是并行用量统计改动在 `Settings.module.css` 新增 `animation: ... 120ms ...`，触发样式裸时长计数 12 大于基线 11，与技能参数文件无交集。
- 没有修改该范围外样式、没有下调基线、没有跳过测试；本次临时验收入口不存在，工作区其他未提交修改均保留。
