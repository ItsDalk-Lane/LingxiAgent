# LingxiAgent 聊天消息渲染架构重构进度

## 会话：2026-08-16

### 阶段 1：事实、基线与不变量

- **状态：** 已完成
- **开始时间：** 2026-08-16
- 已完成动作：
  - 完整读取 2340 行重构任务书，确认十个交付批次及六条架构不变量。
  - 确认当前 Codex 任务已有活动目标，该目标以附件路径承载完整任务书；未覆盖或新建第二个目标。
  - 启用文件化规划，并确认根目录已有历史计划文件，后续仅追加本任务章节。
  - 记录基线提交、仓库版本、Node/npm 与 detached HEAD 状态。
  - 定位旧过程折叠、流式消息写入、助手文字阶段、历史构建和 Markdown 预渲染入口。
  - 细读过程折叠、时间线回合扫描、聊天内容类型和按消息更新实现，确认每次流式发布会复制整条结构数组。
  - 细读实时缓冲与服务端事件管道，确认阶段只在少数来源的服务端结束事件里解析，前端未收到阶段身份。
  - 细读历史重载保护和助手渲染入口，确认历史/实时仍是两条投影链，内容块页面 key 仍依赖数组位置。
  - 细读流式 Markdown 组件和历史助手构建器，确认完整解析发生在发布前，且历史文字默认没有原文与阶段信息。
  - 细读 Markdown 页面节点协调，确认它能保留未变顶层节点，但每次仍会处理完整富文本和全部顶层节点。
  - 细读历史接口分页和旧回归测试，区分已有分页能力与仍缺失的重载语义、载荷延迟和解析次数保障。
  - 核对终端前端状态，确认运行状态已经独立，当前折叠阻塞点只是定位订阅仍由卡片挂载生命周期持有。
  - 读透终端导航待重投机制，确定过程组可先展开、再复用现有子卡重投完成精确滚动。
  - 核对阶段签名现有测试和相关提交历史，确认当前实时/历史都刻意丢弃过程旁白，而不是错误地混入正文。
  - 按锁文件恢复当前 worktree 依赖；锁文件无差异，相关基线 7 文件/247 项全部通过。
  - 新增默认关闭的性能观测器、压力基线和长过程/短答案语义夹具；没有改变产品行为。
  - 完成 10 文件/256 项相关回归、三段类型、renderer 构建、定向代码规范与差异格式检查。
  - 按用户命令执行 `git checkout -B chat-rendering-restucturing`，把基点 `080948ae` 附着到任务分支；现有规划文件保持不变。
- 已修改文件：
  - `chat_rendering_progress.md`（新建；避免 macOS 大小写文件名与已有 `PROGRESS.md` 冲突）

## 测试结果

| 检查 | 输入 | 预期 | 实际 | 状态 |
| --- | --- | --- | --- | --- |
| 目标任务书读取 | 附件 1-2340 行 | 全文读完后再执行 | 已完整读取 | 通过 |
| 运行环境 | `node --version`、`npm --version`、仓库 engines | 使用 Node 24 | Node 24.16.0 / npm 11.13.0，满足约束 | 通过 |
| 原文件恢复 | `git diff --exit-code -- PROGRESS.md` | 无差异 | 退出 0 | 通过 |
| 分支附着 | `git branch --show-current` | `chat-rendering-restucturing` | `chat-rendering-restucturing` | 通过 |
| 依赖恢复 | `npm ci` | 按锁文件成功安装且锁文件无差异 | 1282 packages；补丁校验通过；锁文件无差异 | 通过 |
| 相关行为基线 | 7 个服务端/前端相关测试文件 | 现有行为全绿 | 7 文件、247 项通过，2.55 秒 | 通过 |
| 阶段 1 相关回归 | 10 个相关测试文件 | 基线与新设施全绿 | 10 文件、256 项通过，1.78 秒 | 通过 |
| 类型检查 | `npm run typecheck` | 三段类型检查通过 | 退出 0 | 通过 |
| Renderer 构建 | `npm run build:renderer` | 正式构建成功 | 3431 模块，6.31 秒，退出 0 | 通过 |
| 代码规范 | 本阶段修改源码与测试 | 0 error | 0 error；69 条既有文件 warning | 通过 |
| 差异格式 | `git diff --check` | 无空白错误 | 退出 0 | 通过 |

### 阶段 2：统一语义类型与稳定内容标识

- **状态：** 已完成
- 已完成动作：
  - 为所有内容块增加可选的稳定标识、助手阶段、界面角色和流式生命周期，旧会话仍可按旧形状读取。
  - 建立统一语义补全入口，实时与历史构建都从同一规则得到过程、答案、结果和待操作角色。
  - 稳定标识优先复用工具、文件、任务和确认项自身身份；无身份内容使用助手条目、类型和同类型顺序。
  - 回合落盘获得正式条目标识时，只重写本地生成的临时前缀，上游显式标识不变，保证刷新前后身份一致。
  - 助手内容渲染优先使用稳定标识作为页面节点身份，旧数据才回退数组位置。
- 验证结果：4 个定向文件、56 项测试通过；三段类型检查、定向代码规范和差异格式检查通过，代码规范为 0 错误、72 条既有警告。

### 阶段 3：服务端事件归一化

- **状态：** 已完成
- 已完成动作：
  - 新增独立的助手事件归一化层，把供应商差异收敛为分段开始、分段增量和分段结束三类事件。
  - 明确阶段通道的三种能力：直接给阶段、结束时才给阶段、完全不给阶段；界面层不根据自然语言猜测。
  - 结束时才能确定阶段的文字会先作为未决过程段发布；过程旁白不会进入旧正文兼容链，最终答复只在阶段确定后进入。
  - 结构化推理同步发布为统一推理分段；旧思考事件和旧正文事件在迁移期继续并行发送。
  - 缺少结束阶段时按供应商正文约定回退为最终答复，同时输出服务端诊断，不静默吞字。
  - 会话改名后继续复用同一流状态；统一分段与旧兼容事件共享严格递增的续传序号。
- 验证结果：5 个定向文件、82 项测试通过；三段类型检查和差异格式检查通过；定向代码规范为 0 错误、49 条既有警告。

### 阶段 4：结构状态与实时状态双平面

- **状态：** 已完成
- 已完成动作：
  - 新增独立于全局结构仓库的当前回合仓库，按“会话 + 助手消息”保存内容块、统一语义分段、顺序、生命周期和修订号。
  - 当前回合首次有内容时只向结构列表追加一个空助手座位；普通文字、思考、情绪、工具和统一分段后续只更新当前回合。
  - 助手消息按自身标识单独订阅实时快照；更新当前消息不会重渲染另一条历史助手消息。
  - 统一分段开始和结束立即发布，普通分段增量沿既有合并节奏发布；一千个分段增量只触发一次合并刷新。
  - 真实 `turn_end` 把正式条目标识、用户轮次关联和最终内容合成一次结构提交，再清空实时快照。
  - 清会话、缓冲失效和全量清理会同步清除当前回合快照，不留下跨会话内容。
- 验证结果：扩大回归 15 个文件、437 项全部通过；三段类型检查通过；定向代码规范 0 错误、6 条既有警告；差异格式检查通过。

### 阶段 5：回合投影与确定性收口

- **状态：** 已完成
- 已完成动作：
  - 新增纯回合投影器，把统一分段和兼容内容按显式身份固定分成过程、答案、结果和待操作四区。
  - 回合结束统一封口；未决阶段显式回退并记录诊断，不以文字内容猜测。
  - 正常结束但没有答案或结果时显示“未生成最终回复”；失败、中止和空回合也有明确状态结果。
  - 服务端历史接口保留过程旁白分段，同时维持旧正文只包含最终答复的兼容契约。
  - 同一用户回合内的多条助手记录按稳定序号合成一个回合，正式投影保留完整助手条目标识集合。
  - 建立完整流式事件到双助手历史记录的等价性测试，逐字段比较内容块与回合投影。
  - 延迟文件结果到达时同步更新四区索引，并移除已经不成立的“未生成最终回复”状态。
  - 新增五语言状态文案和实际组件渲染测试。
- 验证结果：扩大回归 9 个文件、208 项通过；异常边界定向 4 个文件、166 项通过；三段类型检查和差异格式检查通过。

### 阶段 6：过程折叠第二版与终端运行时解耦

- **状态：** 已完成
- 已完成动作：
  - 用显式界面角色与正式回合投影替换数量、长度、“最后文字保护”和命令/技能排除规则。
  - 同一助手消息同时含过程与答案时拆成一个过程组和一个外显正文，结果与待操作卡同样保持外显。
  - 单个过程块也会形成稳定过程组；完成回合默认收起，失败和中止默认展开，流式尾回合保持展开。
  - 思考、情绪、命令和技能调用均可折叠；旧畸形块保持原样外显，避免兼容历史崩溃。
  - 过程组持有回合、内容块、终端和子助手锚点；右栏定位先展开父组，再由挂载后的子卡精确滚动。
  - 终端状态继续由独立会话状态承载，折叠与展开不改变进程运行状态。
  - 修复拆分消息可能重复显示回合结束操作的问题，并验证原消息选择、重试和页面锚点只绑定一次。
- 验证结果：11 个相关文件、109 项通过；三段类型检查通过；renderer 构建 3434 个模块、6.72 秒完成；定向代码规范 0 错误、2 条既有警告；差异格式检查通过。

### 阶段 7：原文优先与增量 Markdown

- **状态：** 已完成
- 已完成动作：
  - 新助手正文只保存权威原文；实时、历史、断线恢复、子会话预览和频道实时消息不再预生成富文本。
  - 保留只有旧富文本的会话兼容读取；原文和旧缓存同时存在时只信原文，禁止反向恢复。
  - 用 Markdown 解析器顶层行范围确定冻结边界，组件内缓存冻结前缀，只保留最后两个顶层块为活动尾部。
  - 101 段正文继续增长时，冻结前缀不再进入边界解析或富文本生成；页面节点与原生文字选区保持稳定。
  - 流式公式和图表重活延迟到回合收口；超过 100,000 字符时使用可调的安全纯文本熔断，收口后恢复完整富渲染。
  - 截图导出在低频操作时从原文现场生成富文本，字符预算直接读取原文。
- 验证结果：24 个相关文件、300 项通过；三段类型检查通过；renderer 构建 3435 个模块、6.55 秒完成；定向代码规范 0 错误、14 条既有警告；差异格式检查通过。

### 阶段 8：语义感知发布策略

- **状态：** 已完成
- 已完成动作：
  - 把普通正文、思考增量、情绪增量和统一分段增量对齐到下一次页面刷新，同一回合同一画面只保留一个待发布任务。
  - 保留每秒最多 30 次的发布预算，高刷新率屏幕不会把页面更新放大到 120 或 240 次每秒。
  - 思考开始/结束、工具开始/结束、最终回答开始和回合结束会取消待发布画面并立即发布。
  - 边界前尚未发布的正文会与边界合并为同一次更新，工具开始不再产生“先空刷正文、再刷工具”的两次更新。
  - 没有新可见内容时，回合结束不再额外空刷；用量与压缩记账事件不创建消息、不安排画面。
  - 清会话、回合重置和全量清理会同时取消定时任务与页面刷新任务，不留下过期回调。
- 验证结果：新增 6 项发布策略契约全部通过；扩大回归 12 个文件、253 项通过；三段类型检查通过；renderer 构建 3435 个模块、6.38 秒完成；定向代码规范 0 错误；差异格式检查通过。

### 阶段 9：历史重载减负与渲染窗口

- **状态：** 已完成
- 已完成动作：
  - 保留既有默认 50、最大 200 条服务端分页，只对当前页超过 8 KiB 的思考、命令/技能结果和老内联媒体发预览与会话内取件凭证。
  - 新增同授权的详情读取入口；损坏凭证、条目改写和错误会话均失败关闭，不接受任意文件路径。
  - 前端思考、命令、技能、截图、旧产物和用户旧图片在展开或点击时才读取完整内容；相同内容在会话内合并请求并缓存。
  - 验证超过旧 64 KiB 上限的 70,000 字符命令结果仍能按完整大小取回，不把旧投影截断误当原文。
  - 初始只挂最近 100 个列表项，向上每次增加 50 个；向上补页前预留窗口并用原高度修正保持视口。
  - 查找命中已加载但未挂载消息时先扩大窗口，再走现有滚动与闪烁流程；时间线和框选只消费已挂载节点。
- 验证结果：4 个核心文件、169 项回归通过；思考与命令按需读取 2 文件、26 项通过；三段类型检查通过；renderer 构建 3436 个模块、6.40 秒完成；定向代码规范 0 错误；差异格式检查通过。

### 阶段 10：类型化渲染注册表、兼容收口与全量验收

- **状态：** 已完成
- 已完成动作：
  - 把助手内容中央类型分支和无类型动态表收敛为联合类型约束的完整注册表，显式覆盖全部 20 种内容块。
  - 新增注册表完整性测试；未来增加内容种类但漏掉渲染器时，类型检查和运行时键断言会同时阻断。
  - 保持输入区确认只在输入区出现，保留旧会话富文本读取、旧客户端事件和畸形历史的短期兼容；新数据继续只读权威原文和显式语义。
  - 把三个新增核心模块纳入公开导出清单，用权威生成器刷新运行闭包；公开边界没有新增债务。
  - 修正渲染窗口和原文优先后的两项旧测试断言，样式使用项目真实存在的设计尺寸，不更新债务基线。
- 验证结果：相关组件 11 个文件、61 项通过；边界/闭包 2 个文件、38 项通过；尺寸与样式 2 个文件、9 项通过；三段类型检查通过；renderer 构建 3436 个模块、6.83 秒完成；修改文件代码规范 0 错误、1 条既有警告；低并发全量 1120 文件/11328 项通过，1 文件/7 项原有跳过，退出 0。

## 错误日志

| 时间 | 错误 | 尝试 | 处理 |
| --- | --- | --- | --- |
| 2026-08-16 | 新建目标失败：当前任务已有未完成目标 | 1 | 读取现有目标，确认它正是本附件任务，直接续跑，不再重复创建 |
| 2026-08-16 | `progress.md` 与已有 `PROGRESS.md` 在当前文件系统冲突，误覆盖旧文件 | 1 | 对照 `HEAD` 完整回读 37 行原文件，用精确补丁恢复；本任务改用 `chat_rendering_progress.md` |
| 2026-08-16 | 预期的 `/opt/homebrew/opt/node@24/bin/*` 不存在 | 1 | 不重复使用历史路径；当前 PATH 已是满足约束的 Node 24.16.0 / npm 11.13.0 |
| 2026-08-16 | 首次读取流式 Markdown 组件使用了不存在的上级目录路径 | 1 | 检索结果确认组件实际位于 `components/chat/StreamingMarkdownContent.tsx`，下一轮改读真实路径 |
| 2026-08-16 | 按计划中的概念名读取了不存在的 `terminal-runtime-slice.ts` | 1 | 当前实际实现是 `stores/terminal-slice.ts`；按真实文件继续审计，不假设计划概念已经落地 |
| 2026-08-16 | 首次运行 7 个相关测试文件时配置加载失败，`vitest` 包不存在 | 1 | 测试未执行，不记入产品红灯；核对依赖状态后用锁文件恢复环境，再原样重跑 |
| 2026-08-16 | 阶段 2 首轮有 3 个旧内容块精确形状断言失败 | 1 | 新语义字段是预期差异；更新为同时检查旧字段和全部新字段的强断言 |
| 2026-08-16 | `AssistantMessage.interlude` 在加载阶段找不到工作区包入口 | 1 | 套件 0 项执行；检查工作区包构建产物后按仓库标准命令恢复，再原样重跑 |
| 2026-08-16 | 阶段 2 首次类型检查唯一错误：插件卡详情无通用 `id` | 1 | 不修改插件协议；按真实字段生成稳定候选，无法生成时使用有序回退标识 |
| 2026-08-16 | 阶段 3 首轮新契约测试加载失败：统一助手事件模块尚不存在 | 1 | 这是预期红灯；实现独立归一化层后原样重跑，纯逻辑契约转绿 |
| 2026-08-16 | 新分段事件占用续传序号后，旧会话改名用例仍期望正文序号为 1、2 | 1 | 更新为同时强断言分段序号 1/2/4、正文序号 3/5、路径迁移和同一流标识，不删新事件也不放宽检查 |
| 2026-08-16 | 阶段 4 红灯证明一千个普通增量仍会替换结构列表引用 | 1 | 把实时内容迁到消息级独立仓库后原样转绿；结束前结构引用稳定，真实结束只写一次 |
| 2026-08-16 | 双平面首轮相关回归出现 18 项失败 | 1 | 4 项是性能目标达成后的预期数字变化；14 项直接读取旧结构位置，改为读取与产品一致的实时快照，旧字段和内容断言均保留 |
| 2026-08-16 | 阶段 5 首个回合投影测试因模块尚不存在而加载失败 | 1 | 作为先测后改红灯；实现纯投影器后原样重跑，4 项初始契约全部通过 |
| 2026-08-16 | 未决阶段测试首次把夹具生命周期写成已封口 | 1 | 修正夹具为真实流式生命周期，不修改产品逻辑；实时过程与结束答案保持同一稳定标识 |
| 2026-08-16 | 阶段 5 首次扩大回归有 5 项旧顺序/纯过程断言失败 | 1 | 固定四区会把工具放入过程区，并为只有过程的回合增加明确状态；更新旧断言，同时修复延迟文件结果到达后状态未撤销的真实问题 |
| 2026-08-16 | 阶段 6 首轮过程折叠回归有 12 项旧断言失败 | 1 | 旧断言仍依赖至少三条、短文字过程化和命令/技能排除；改用显式四区夹具，并为畸形旧内容保留原样外显兼容 |
| 2026-08-16 | 终端定位用例在扩大运行时一次找不到过程摘要 | 1 | 清理测试残留的流式会话身份后稳定转绿；单跑与 109 项扩大回归均通过 |
| 2026-08-16 | 原文优先扩大回归出现 6 项旧成本和选区夹具失败 | 1 | 更新预解析成本为真实新值；修正原文与手写富文本不一致的夹具，节点身份断言原样保留并转绿 |
| 2026-08-16 | 代码工具栏测试的 JSX 属性使用字面 `\\n` | 1 | 改成真实 Markdown 围栏换行，固定权威原文渲染后的工具栏与复制换行语义 |

| 2026-08-16 | 阶段 8 发布策略红灯 4 项中 3 项失败 | 1 | 失败准确命中旧的首增量立即发布、边界双刷和无页面预算；实现同画面单待办后 6 项完整契约转绿 |
| 2026-08-16 | 阶段 8 压力基线仍期待无内容的结束空刷 | 1 | 保留真实退出码；将三组发布次数从 3 更新为 2，正文长度、零预解析和单次结构提交断言不变 |
| 2026-08-16 | 阶段 9 首次给同一测试命令传了两个 `-t` 筛选值 | 1 | 测试框架拒绝参数且 0 项执行；拆成两条命令，分别得到历史重内容和渲染窗口的真实红灯 |
| 2026-08-16 | 渲染窗口红灯筛选词未命中中文用例名 | 1 | 该次整文件跳过，不计为通过；直接运行完整文件后确认 250 个节点全部挂载的失败 |
| 2026-08-16 | 阶段 10 首次低并发全量有 7 项失败 | 1 | 4 项为公开清单/运行闭包未同步，1 项为裸间距，2 项为窗口化与原文优先后的旧断言；逐项修正后对应门禁全部通过 |
| 2026-08-16 | 样式修正后的首次全量仍有 1 项尺寸引用失败 | 1 | 新尺寸名字并未在项目定义，引用守卫准确失败；换成真实 6/10 规格后定向 9 项和完整 11328 项全部通过 |

## 断点自检

| 问题 | 答案 |
| --- | --- |
| 当前在哪？ | 十个实现与验收阶段均已完成并提交，最终分支与工作区状态已回读 |
| 接下来去哪？ | 无；向用户交付分支、提交和验证结果 |
| 最终目标是什么？ | 把聊天体系升级为由统一语义事件、当前回合实时状态和确定性回合投影驱动的增量架构 |
| 已知道什么？ | 新架构已由显式阶段、实时回合和确定性投影驱动；兼容字段只服务旧会话/旧客户端，不再决定新数据语义 |
| 已做什么？ | 十个阶段全部完成；最终低并发全量 1120 文件、11328 项通过，类型、构建、边界、闭包与样式门禁均通过 |
