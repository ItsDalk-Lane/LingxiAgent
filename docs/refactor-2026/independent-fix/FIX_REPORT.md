# 独立审查修复报告

**总体：BLOCKED；工程全量门禁：FAIL。不能宣布九阶段全部完成。** F01—F04 的实际代码缺陷已修复并通过本地针对性验证；F05 的证据核对、真实桌面/产物补验及阻塞登记已交付，必需验收仍有未闭合项。逐项机器结果以 FIX_RESULT.json 和 FIX_ACCEPTANCE_MAP.json 为准，不把某份旧阶段报告当当前结论。

## recheck 轮追加（R1/R2/R3，2026-09-22）

复审对前轮候选提出 R1（接纳先于序号校验产生退休副作用）、R2（旧异步恢复覆盖等待期间接纳的新流）、R3（证据清单引用未被版本管理跟踪的原始日志）。三者在真实环境确认并处理，逐项证据见 `artifacts/refactor-2026/independent-fix/recheck-r1-r2/`：

- R1/R2 均真实复现（红基线 3+2 例）后修复：接纳拆分为纯校验与延迟提交、水位原子化、重放层按真实接纳计数；恢复按会话接纳代次守卫，resume 请求/响应新增可选 `resumeToken` 代次关联（服务器透传，旧两端兼容）。新增长 7 例真实 WS 链路反例（含结束帧缺口与切流），原 C1/C14—C19 族全部保留普通通过，F03 定向回归 19 文件 283 测试 exit 0。细节见 F03.md recheck 节。
- R3：EVIDENCE_SHA256.txt 的 4070 项引用中 123 个原始 `.log` 被 `*.log` 忽略规则排除在版本管理外（干净 checkout 不可复核）。全部经脱敏审计（0 密钥；"Application Support" 命中均为随机后缀隔离测试目录；机器路径与已跟踪 commands.jsonl 先例一致，见 redaction-audit.md），以 `.gitignore` 精确反向规则纳入暂存候选；清单按同一规则重生成并经干净 checkout（write-tree + archive，非本工作区）验证引用存在与哈希一致（4110/4110、源码 3535/3535，见 clean-checkout-verify.out）。验证还暴露并修复了 123 之外的同族缺陷：两个 f05-package 的 pdf-ax 快照 txt 磁盘为 CRLF 而 git blob 已按 `* text=auto eol=lf` 归一化为 LF，旧清单哈希在任何干净 checkout 都必然失败——已把磁盘字节对齐为已入库的 LF 版本（各仅 1 处行尾差异，内容不变）。新增 recheck 证据以新命令/新时间登记，未改写早先记录；未发现需要"丢失登记"的清单引用（盘上 4070/4070 存在）。
- 本轮入场 HEAD 8d55046d5646a008f71f57de745b3564e94a5f21（前轮修复提交），工作区干净；Node v24.16.0、锁文件未变。全量复跑（`engineering-full-tests-recheck`）：1466 文件，14874 通过、4 失败、15 跳过，退出码 1——4 例与上一轮完全相同（旧封印/证据坐标检查，B01 范畴），无新增回归。typecheck 三连、lint（0 error）、严格契约 28 文件、受影响面 19 文件 249 测试均通过。
- 复审所引 RECHECK_REPORT.md / RECHECK_FACTS.json / probes/README.md 未在本机存在；本轮以复审提示词内嵌的 R1/R2/R3 事实描述为输入，并在真实源码逐条核实后实施，未将无法核对的描述当作已验证结论。

## 基线与执行边界

分支 docs/knowledge-closeout-2026-09-21，F01—F05 轮进入与退出 HEAD 均为 b0e9118427e16206dcd25ecfb566d766ed476727，提交树 b43a2a1528c5d8f91884aae1e57ae5bd92788b76；入场工作区干净。该轮修复随后以提交 8d55046d5646a008f71f57de745b3564e94a5f21 落库；recheck 轮（R1/R2/R3）在该提交之上进行，修复留在未提交工作区，最终实际字节由 SOURCE_MANIFEST.json 及校验和绑定。Node v24.16.0、npm 11.13.0、macOS arm64；package-lock 的安装版本对照通过，没有升级依赖。

已阅读三份独立审查材料、原任务书入口/通用约束/相关阶段及当前工程规则。审查的源码摘录日志没有当作本轮执行证据。F01、F02、F03 和检查器反例均在真实 Node 24、锁定依赖和生产模块中重新复现；真实总线、HTTP、WS、Pi、实际消费者的范围分别标明。

没有提交、推送、PR、发布、真实供应商费用或对外联系人消息。没有第二套 Agent 循环、任务管理器、工具网关或历史库。保留 Electron、React、TypeScript/Node.js、Pi 和已有功能。

## 修复与实际消费者

| 包 | 原问题与本轮结果 | 真实入口和旧路径 | 证据 |
|---|---|---|---|
| F01 | 所有状态写入口统一终态保护；已结束任务不被 update/abort/cancel 覆盖，合法展示元数据仍可更新。任务复用后旧、缺省、null 和错误批次不能冒认新执行。真实异步调用登记时绑定不可变批次。 | TaskRegistry、task 总线、后台命令、回档、子代理、媒体生产方。旧回调不再读取当前最新批次；有限墓碑淘汰后仍防旧回调重入。 | F01.md、F01_RESULT.json；f01 原始红绿日志 |
| O01 | 确认注册表为 best-effort 可见性存储，新增内存接受/未落盘/durable 诊断；权威 DeferredResultStore 持久失败仍拒绝交接。 | 不把可见性落盘失败变成重复外部动作的理由。原观察不是“全部存储都保证 durable”的证据。 | F01 的 C07/C08 与持久失败注入 |
| F02 | 两个会话创建入口在任何写入/模型调用前拒绝损坏 JSON、错误对象形状及实际字段错类型。保留合法空 body、字段 null、未知字段等原语义。 | 两入口统一消费真实 strict 解析器；detached 退出吞错 safeJson。未机械更改其它无关写路由。 | F02.md、INPUT_BOUNDARY_MAP.json；真实 HTTP 44 条及原路由回归 |
| F03 | 原 C1 断言保留，移除 it.fails 并普通通过。旧 start/delta/end/tool/status/恢复帧不能污染新流，runId/streamId/model call/trace 不混用。 | 真实 WS 路由→handleServerMessage→恢复服务/manager→Zustand 投影。旧协议仅受控兼容；退休标识有界并走真实快照恢复。 | F03.md；真实 TCP 接线、多模型轮次、超过256轮、历史/恢复/导出分层证据 |
| F04 | 从六个入口扩展至17入口/28个实际项目编译文件；本轮参数、状态、批次、消息接纳、清理和真实生产调用受检。 | 无假声明、无新 ignore、无削弱白名单。真实调用负例、删真实状态分支、全局错误、缺文件/配置均能使检查失败。 | F04.md、STRICT_SCOPE.json、独立交叉复核报告 |
| F05 | 重算32条回归及原124场景/68任务，区分真实GUI、协议替身、源码升级、产物和平台。 | 旧执行日志保留，各阶段追加当前勘误；局部测试不升级为全产品PASS。 | FIX_ACCEPTANCE_MAP.json、BLOCKED_ITEMS.md、原始命令索引 |

同轮复核还闭合三个实际缺口：后台取消处理不再错用另一会话归属；媒体可见性批次索引在总线清理抛错时也释放且错误不吞；实际界面发现的“已批准工具被后续输出重新显示为待批准”通过现有 manager 同步确认终态修复。后者在原审查 HEAD 同样可复现，但属于本轮消费者投影验收范围，没有作为旧问题逃避。

## 验证口径

最终 typecheck、lint、工具调用边界、依赖边界、严格契约正反例、开放服务端构建与冒烟分别保留实际命令/退出码。lint 为零 error，存量 warning 如实保留，未用关闭规则换通过。最后全量的具体数量和失败名称只取 FIX_RESULT.json 的 full_test，不累计多次运行，不使用旧阶段数字。

旧审计封印及坐标的四个失败没有通过删断言、改白名单或冒填已验证提交修绿。另有旧 R10-09 补丁重放测试会对全仓文件做快照；并行日志变化曾触发额外失败，稳定后独立重跑通过。相关原始失败仍保留。全量运行临时写日志到仓库外，结束归档；其会改写的历史补丁用入场备份恢复原字节，不 reset 工作区。

全量中的旧协议环境跳过不能计通过。随后创建所需旧版本隔离副本，按它自己的锁文件安装，原有两文件8条真实 HTTP/当前消费者测试全部通过且无跳过；旧副本清理，原测试跳过条件未改，原全量数字不重写。其它平台限定跳过仍保留。

真实GUI完成了任务接收、持续中文/代码输出期间的粘贴、选中、复制、滚动和工具详情展开。**未证明真实中文输入法合成**。审批显示缺陷先记录再修复，修后真实消费者回归和最终产物另行执行；修前截图不冒充修后全部GUI场景。

资源验证扩展至42次实际浏览器任务、42次取消、99次采样/33窗口。浏览器实例上限与进程回收有证据；新增会话及重复同批会话均仍见 RSS 增长，缓存保留与泄漏尚未区分，C26保持BLOCKED。没有改阈值或强制GC来制造通过，也不将五分钟多窗口说成小时级压力验证。

最终本机新包的生产源码与配置逐文件对照、独立seed、真实桌面任务、浏览器、PDF和PTY证据见 f05-package/PACKAGE_REPORT.md。正式安装器/公证、其它平台、完整终端界面审核链和更新激活/回退仍不具备完整证据。源码旧版本→新版本→半写故障→旧版本回退演练14项通过，但不能替代安装产物升级，也未证明所有观测内容逐字段相同。

## 测试隔离异常

业务数据均使用测试目录。首次GUI测试曾因 LINGXI_HOME 末级同名 data，使 Electron 缓存落入账户的 Application Support/Data 并发生实例锁碰撞；不能证明该目录完全由本轮创建，故没有删除或恢复它，也不宣称首次所有写路径隔离通过。之后改为唯一随机末级目录并用 lsof 核对和清理。该已发生的隔离缺口在 P00-A03 记FAIL；没有证据表明真实会话/配置被覆盖。最终包首次冒烟参数错误被内核所有权保护拒绝，修正后通过；原失败日志保留。

## 结论与限制

没有审查缺口仅靠改文案被否定。O01 按权威存储/可见性存储区分后修复反馈，而非盲改为重试外部动作。F01—F04为已修复的本地契约结论，F05及整体验收仍受阻。需恢复的具体环境、授权和补验动作逐项见 BLOCKED_ITEMS.md；任何相关必需FAIL/BLOCKED尚在时，不建议生产发布。

最终全量实跑：1466个测试文件，14867项通过、4项失败、15项跳过，总计14886；退出码1。失败仍为四项旧封印/证据坐标检查。最终候选另补旧协议8/8通过、0跳过，不改写该次全量统计。
