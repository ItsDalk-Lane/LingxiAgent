# F02 输入边界修复

状态：实现及本地真实 HTTP 回归 PASS；不代表真实供应商/GUI/整产品验收。

## 已修复

`/sessions/new` 与 `/sessions/new-detached` 在引擎创建、工作区历史更新、会话元数据和项目/谱系写入前调用同一真实解析器 `server/session-create-input.ts`。前者原来缺少 cwd/workspaceMountId/workspaceFolders/projectId 契约，后者原来 safeJson 吞掉损坏 JSON；错误字段不再变成默认创建。保留现有对象校验/稳定错误码 helper。无新框架或依赖。

合法空 body/{} 仍创建；body 本身为 null/数组/原始值拒绝；字段 null 仍表示未提供。工作目录列表拒绝非数组及非字符串元素，保留旧有空白字符串过滤。未知扩展仍忽略；model 并非这两个入口实际支持字段，focused 的 permissionMode 也不消费，不能擅自把它们扩成新功能。权限与思考档位值域继续由原引擎 normalizer 裁决。

所有实际消费字段、同族范围及剩余 23 处 session safeJson 调用见 INPUT_BOUNDARY_MAP.json。两种空白创建入口均已退出 safeJson；继续已删除 agent、切换、复制等其余操作有不同资源授权语义，本包未机械替换，明确留下清单。

## C09—C13 映射

真实测试：`tests/session-create-input-contract.test.ts`。Node 24.16.0 + 项目已安装锁定依赖，真实随机 loopback HTTP 端口 → Hono 路由 → 原 createRequestContext/authorizeCapability → 实际解析器。只替换引擎外部副作用边界：成功时写隔离临时 session 文件并记录模型计数；拒绝时所有创建/元数据/配置/项目/谱系/模型计数为零，目录和授权文件不变。主体由测试 middleware 提供合成认证结果；这不是登录协议或真实供应商测试。

| 条目 | 实际断言与结果 | 原任务书 |
|---|---|---|
| C09 | 两入口损坏 JSON、null、数组、数字、字符串、布尔均 400；invalid_json/invalid_body；零副作用 | P01-T05/A07 |
| C10 | focused cwd/mount/folders/目录元素/memory/agent/currentAgent/thinking/project 错类型 400 invalid_field_type | P01-T05/A07，P08-T03 |
| C11 | detached 同上及 permission/forkedFrom/recordWorkspaceHistory；同等错误码和零副作用 | P01-T05/A07，P08-T03 |
| C12 | 空 body、{}、允许 null、指定目录/关闭记忆/切 agent/思考、detached read_only；旧路由挂载工作区和项目回归 | P01-T05/A07，P08-T03 |
| C13 | 原授权策略：只读账号/无授权 device 均 403，不创建；有写权限账号仍须过字段验证 | P01-T05/A07 |

## 实际证据

目录 `artifacts/refactor-2026/independent-fix/f02/`，commands.jsonl 记录命令/开始结束/Node/cwd/退出码。red.log 首轮存在夹具未预初始化授权注册表的额外失败；其日志保留但不当成产品缺陷。修正夹具后、生产代码修改前 `red-corrected-fixture.log`：44 项中 22 FAIL/22 PASS，明确复现错误 cwd 等返回 200 以及 detached 错输入进入创建。

修复后 green.log：新 HTTP 测试和原 sessions-route/session-projects-route 三文件共 173 PASS，exit 0。后续 strict/typecheck 及新增范围回归另记同目录；不要把并行修改中的其他包错误归给本包。

## 严格范围和安全检查

`server/hono-helpers.ts` 与生产真实调用的 `server/session-create-input.ts` 通过 strict/noUncheckedIndexedAccess/exactOptionalPropertyTypes。移除 helper 的 error:any，创建 options 从路由 any 改为真实 string/string[]/boolean 契约。旧 safeJson 保留原兼容调用行为，其 JSON.parse 的宽松返回仅供清单中未改遗留入口；新创建路径严格解析 unknown。总 strict 配置由主修复包纳入，不伪造声明。

已按 security-review 技能核查真实输入→写入边界与授权策略；没有真实模型、真实账号或真实用户数据操作。该测试证明路由拒绝时无法进入产生外部动作的引擎边界，不声称完整引擎/供应商效果已测。

## 同轮 F04 独立类型子任务

`core/session-permission-mode.ts`、`lib/plan-mode/plan-file.ts`、`core/session-teardown.ts` 和 `lib/pi-sdk/session-shutdown.ts` 已纳入真实严格实现：权限输入从 unknown 运行时收窄，输出 action 限定 allow/deny/prompt/review；清理 session/unsub/warn 契约明确，catch 保留 unknown 异常并继续后续清理。Pi index 公共 API 原位 re-export 窄适配，清理真实使用同一实现。

普通 typecheck 揭示原 Pi 适配事件缺少锁定 SDK 必需的 reason；现明确发送 `{type:'session_shutdown',reason:'quit'}`，对应本入口结束资源而非会话替换。没有用假声明或 cast 绕过 SDK。bridge 回归移除同源 shutdown mock，验证真实 runner.emit 调用及 emit→unsub→dispose 顺序。首轮回归因为旧 mock 断言不再走到记录 2 FAIL，修正测试接线后 `regression-final.log` 六文件 140 PASS；`strict-final.log` 精确严格编译 exit 0。


完整回归首轮另发现三组 teardown 测试仍重复 mock 旧 index 导出；已改为观察真实 extensionRunner.emit，保留 emit→unsub→dispose 次序断言。输入边界新增 currentAgentId 校验处补充原规则要求的焦点用途注释。对应 `full-suite-regressions.log` 为四套 53 项通过；未降低断言或改门禁。
