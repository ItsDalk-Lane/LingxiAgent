# R00-T03 连续两轮 FAIL 后根因分析 R2

状态：**只读根因分析，供全新修复代理使用；不作 PASS 判定。**基于 HEAD `60dbe0384e553f0a0aab7d173b2dc1ba43987b82`、R00-T03/A05/A06 任务书、R1/R2 审核、R1 修复记录、八份当前候选及现役源码。未改候选、产品、任务书或总账，未提交/推送。`R00-A05` 的旧轮 PASS 仅表示当时没有发现实际 Pi 能力漏项，不证明导入图的运行时边准确。

## 一、全部 finding 与共同根因

| 轮次 | finding | 事实 | 深层原因 |
|---|---|---|---|
| R1 F01 | D10/D11/D13 等运行依赖被写成按 Office/文件操作加载，D40/D41/D49/D50 漏服务端或桌面消费者。 | `lib/knowledge/source-processors.ts:4-6` 顶层加载文档库；`core/first-run.ts:10`、`lib/tools/invocation/schema-validator.ts:2`、`shared/artifact-core/release-order.cjs:10` 分别落在服务配置、工具校验、更新排序链。 | `r00_t03_build_matrices.py:240-268` 先按包名/前端模板分类、只取少量引用；加载时点、功能调用时点和权威负责人没有由完整消费者链推导。R1 已修这批实例，但方法仍有盲区。 |
| R2 F01 | `lib/pi-sdk/index.ts:61,105` 两条 `export type` 被标为运行时边，7 个符号与 D01/D02 位置误记。 | TypeScript AST 的 `ExportDeclaration.isTypeOnly=true`、每个 `ExportSpecifier.isTypeOnly=false`；图生成器在具名导出处只看后者。Pi 包另有真实运行时边，不能整包改判未使用。 | `r00_t03_import_graph.mjs:59-63` 丢弃父节点类型语义；`r00_t03_build_matrices.py:465-508` 沿用图；`r00_t03_validate.py:163-220` 又以同一图为 oracle，所以 37 个绿色检查是同源自证。 |
| R2 F02 | `subagent-tool`、`workflow-tool`、`loop-controller` 的隔离 Agent/任务终态未进入五条 `worker_counterexamples`。 | `core/agent.ts:899-969` 注入执行器；`core/session-coordinator.ts:8048,8385,8657` 建 Pi session 并 prompt；三处工具/循环自行完成、失败、中止或唤醒任务。 | `r00_t03_build_matrices.py:133-139` 是人工列出的五个例子；`r00_t03_validate.py:221-255` 只逐个检查已知名字与 `status:` 字符串，未从执行入口、注入边、状态存储反查全部候选。 |

**共同根因是“候选与校验共同依赖一份不完整的抽取/固定名单”。**A05 的静态导入图把语法边当运行时加载边，A06 的反例表把已知对象当全集；生成器、候选和验证器顺次复制同一前提。任务书 `R00-A06` 的边界是“含模型循环**或**任务终态写入的候选 worker 必须归内核迁移”，因此即使没有直接 `createAgentSession`，通过依赖注入启动轮次、写任务结果、决定父会话续跑的组件也必须查。修复应同时覆盖**发现、分类、独立验证**三层，不能只补 R2 点名三行。

## 二、F01：导入/依赖图的覆盖式修复矩阵

当前扫描器在 `r00_t03_import_graph.mjs:39-70` 收集导入，在 `:74-94` 将**所有**本地解析边（含 `import type`、`export type`、函数内动态 `import`、条件 `require`）放入 `edges`，再把 `reachable_from` 用在依赖账本。它只能叫“语法上可达”，不能直接叫“进程启动即求值”。独立源码探针已确认 `lib/pi-sdk/index.ts:61,105` 的 `node.isTypeOnly=true`、元素 `isTypeOnly=false`；当前图均记成 false。另一个实际漏边是 `shared/safe-fs.ts:37` 的 `createRequire(import.meta.url)('js-yaml')`：当前 `external_imports` 的 js-yaml 位置清单没有这一行，R1 全表修复后仍漏消费者。

| 编号 | 必须一次处理的形式/边界 | 当前源码锚点与预期 | 独立 oracle / 正负例 |
|---|---|---|---|
| I1 | `ImportDeclaration` 的 `importClause.isTypeOnly` 与各 `ImportSpecifier.isTypeOnly` 合并；default、namespace、具名、混合、仅副作用分别保留。 | `lib/pi-sdk/stream-guard.ts:1` 是类型专用；`lib/bridge/feishu-adapter.ts:8` 是运行时 namespace，`:24` 是另一模块的类型专用导入。具名混合必须逐符号，不得整条边一刀切。 | 用独立 TypeScript AST 加 `transpileModule` 小夹具，分别测试 `import type D`、`import type * as N`、`import {type T,V}`、`import D,{type T,V}`、`import 'x'`；类型符号在 JS 中消失，V/default/副作用保留。故意把 type-only 改 false 必须失败。 |
| I2 | `ExportDeclaration.isTypeOnly || ExportSpecifier.isTypeOnly`，包括 `export type {X}`、`export {type X,Y}`、`export type *`、`export * as N`。 | `lib/pi-sdk/index.ts:61,105` 七个类型符号是 R2 必测反例；`:60` 的 `runAgentLoop` 仍是真实运行时导出，`:57,97,104,108` 需逐元素判定。 | 独立 AST 逐条比源码，编译后检查类型专用转发不留下 JS 模块边；故意把 61/105 任一条误标为运行时必须失败，同时保留 :60 为运行时正例。 |
| I3 | CJS `require`、解构/namespace/default、`createRequire` 别名与直接调用、`require.resolve` 区分“加载”和“只解析路径”。 | `shared/artifact-core/release-order.cjs:10` 是运行时 `require('semver')`；`shared/safe-fs.ts:37` 直接 `createRequire(import.meta.url)('js-yaml')` 现被漏记；`lib/knowledge/usearch-vector-backend.ts:28` 同时有 `require('usearch')` 与 `require.resolve('usearch')`。 | 从源码对所有 `createRequire`/`require`/`module.require` 形式单独扫描；js-yaml 新位置必须出现在 D41，`require.resolve` 只能记解析/定位，不能算模块执行。插入别名或直接调用夹具应触发差集。 |
| I4 | 动态 `import('x')`、无替换模板、表达式路径与条件加载；本地解析和跨进程/注入边分层。 | `lib/document-extract/anydoc-loader.ts:28` 为调用时动态加载；`core/fresh-import.ts:14` 用户插件路径是未知边；`server/bootstrap.ts:62` 服务产物入口。 | 独立比对 AST 静态/动态/不透明集合；生产不透明边须逐个说明来源与去向。故意把动态加载说成启动静态加载、把用户插件说成“无 Pi”，均失败。 |
| I5 | 外部依赖 51 行的“模块求值”和“实际操作”分开，记录全量直接位置、消费者进程、负责人、协议、退出证据；不能靠包名模板。 | R1 的 D10/11/13/40/41/49/50 全部重审；特别补 `shared/safe-fs.ts:37`。`r00_t03_build_matrices.py:469-508` 现在仍完全信任图；`source_refs` 在存在图命中时丢弃初始手工引用，易吞新盲点。 | 锁文件/清单作包集合 oracle；源码 AST 与手工 CJS/动态反查作位置 oracle；对 51/51 行逐项核加载时点、功能触发、owner、协议、退场。负例包括遗漏 shared/safe-fs、知识库静态说成按需、截掉服务端 diff、版本排序归前端。 |
| I6 | 生产可达性只沿实际运行时**顶层静态**边；条件 `require`、动态 import、type-only 另给“可能触发”链，跨进程/注入手工接桥。 | `r00_t03_import_graph.mjs:74-94` 目前把所有边同权；`R00-T03_REPORT.md` 所称 81 个静态可达文件须在修正后重新计算、重新解释。 | 独立抽样服务 full/open、Electron、CLI、Bridge、Hub；用 TS 编译/真实导入探针核类型擦除，用调用条件核延迟加载。若类型边改变“启动静态可达数”，即失败。 |

验证器不应再将 `GRAPH.external_imports` 同时当“被测值”和“唯一预期”。至少用另一段独立 AST/编译探针直接读取源码，外加对 CJS/注入的反向搜索；对图中每种 `kind` 和 `type_only` 校验语义。`r00_t03_validate.py:189-220` 的现有“改账本值”负例只能证明账本照图抄写，不能证明图正确。重新生成前须保留 R1 改正的真实消费者和 Pi 真运行时边。

## 三、F02：所有同类执行/终态路径的覆盖式矩阵

下表按**权威状态或轮次控制者**列，而非按文件名猜 worker。`KERNEL_MIGRATION` 指该列的调度、模型决策、任务终态及父会话续跑由 Rust 唯一负责；文件格式转换、SDK 单次收发、供应商查询或 OS 命令本身可作为限权外围操作。默认关闭的功能只影响何时激活，不把其生产代码判为可长期保留的 Node 内核。

| 编号 | 路径、实际入口与源码锚点 | 激活条件 / 当前终态 | 修复后的分类与协议/退场证据 |
|---|---|---|---|
| W1 | **现有五条复核**：`hub/agent-executor.ts:290,343,556,716` 建 session/prompt；`lib/llm/cache-preserving-compaction-agent-run.ts:550` 直接 `runAgentLoop`；`hub/scheduler.ts:440-514` 隔离执行/活动状态；`lib/bridge/bridge-manager.ts:2093,2369` loop turn；`plugins/office/lib/read-document.ts:1` 单次解析。 | Hub 临时/电话、压缩、cron/heartbeat、Bridge 按各自条件触发；Office 按文件调用。 | 前四条保持内核迁移，Office 仅解析可外围。对照取消/超时、任务终态唯一写者、Pi 清退；Office 证明无 session 与状态写入。 |
| W2 | **子代理新建与续接**：`core/agent.ts:899-938` 注入，`lib/tools/subagent-tool.ts:466,551-627,802,865-911` 运行、延迟结果、run/thread/hub 终态；`core/engine.ts:2304` → `core/session-coordinator.ts:8048,8385,8657`。 | 工具在 `core/agent.ts:1228-1230` 纳入快照；用户调用时激活，主动委派实验开关只管自动建议。 | `KERNEL_MIGRATION`：Rust RunSupervisor/TaskRegistry/DeferredResult/Thread 唯一裁决新建、续接、取消和父会话交付；对照完成/失败/中止/重复回调/重启；旧 store 不再是权威。 |
| W3 | **Workflow 节点/恢复**：`core/agent.ts:940-969,1231` 注入；`lib/tools/workflow-tool.ts:283,297,304,320-346,418-429,460,483`；`lib/workflow/host-api.ts:278` 调隔离 Agent。 | per-agent `tools.disabled` 控制，源码注释默认关；启用后节点可产生多次模型调用并写 deferred、thread、hub 终态。 | `KERNEL_MIGRATION`：Rust 持节点/脚本预算、journal 恢复、run 终态；脚本沙箱可限权外围。对照成功/失败/取消/恢复/重复节点与父 turn；关闭开关不等于可漏账。 |
| W4 | **循环任务**：`server/index.ts:488,910` 注入持久 LoopStore；`core/engine.ts:1374,1435-1510` 装配；`lib/loop/loop-controller.ts:125,145-199,280,353,366` 写 stopped/completed/paused 并触发后续 turn；`lib/tools/loop-control-tool.ts:49` 为工具入口。 | 用户启动循环后，桌面走 `deliverCustomMessage(triggerTurn)`，Bridge 走 `executeLoopTurn`；闹钟/恢复继续。 | `KERNEL_MIGRATION`：Rust 掌握循环目标、预算、唤醒、停止和唯一终态。对照完成/暂停/恢复/会话换代/后台任务仍在跑的守恒；Node 不再写 loop-state。 |
| W5 | **桌面封面生成**：`server/routes/desk.ts:488-535,836-924` 在 HTTP 路由建活动并 `engine.executeIsolated`，超时 abort，自写 done/error；`lib/desk/activity-store.ts:65-100` 持久化。 | 美化插件及工具启用、图片模型可用、用户发起生成时。 | `KERNEL_MIGRATION`：Rust 持活动 ID、隔离 run、取消/超时和终态；图片转换可外围。对照 HTTP 成功/失败/超时/并发重复收尾及 ActivityStore 退出。 |
| W6 | **Cron/heartbeat 的内层调度器**：`hub/scheduler.ts:457,514` 已列，但 `lib/desk/cron-scheduler.ts:63-82,137-181` 独自判到期、超时、推进游标、`logRun` 成败；`core/studio-cron-service.ts:194-198` 转发持久存储。 | 到期且 job 启用；heartbeat 按配置；一次失败也推进游标。 | 与 W1 scheduler 同一 `KERNEL_MIGRATION`，不能只迁 Agent 调用而留下 Node 任务决策。对照配置换代、超时、失败退避、重启、恰好一次游标；旧 CronStore 不写权威。 |
| W7 | **Hub 临时隔离及频道手机轮次**：`hub/index.ts:334` 的 `ephemeral` 分支直调 `executeIsolated`；`hub/channel-router.ts:370-381,508-668,790-820` 注入 `lib/channels/channel-ticker.ts:280-351,452-625`，后者轮询/重试、更新已读书签，router 调 `runAgentPhoneSession` 并决定 reply/pass。 | `core/preferences-manager.ts:335-336` 频道总开关默认关；开启、有频道成员/新消息或主动提醒时。 | `KERNEL_MIGRATION`：Rust 持轮次预算、消息游标、回复/跳过决定与 Agent run；平台消息读写可外围。对照多 agent、重试、取消、书签不提前推进；旧 ticker/router 不再决策。 |
| W8 | **Agent 私信多轮**：`hub/index.ts:1041-1047` 注入回调；`hub/dm-router.ts:144-183,187-343` 去重/冷却，最多三轮调用 `runAgentPhoneSession`，按 `[NO_REPLY]`/`<done/>` 停止并写双方 DM 与 phone activity；Pi prompt 在 `hub/agent-executor.ts:556,716`。 | 同一频道总开关默认关；启用后新私信触发。 | 独立 `KERNEL_MIGRATION`：Rust 持多轮上限、停止/转交、双边消息一致性；文件/平台收发可外围。对照无回复、done、三轮上限、重复回调/重启；Node 不再决定 DM 终止。 |
| W9 | **延迟结果与父 turn**：`server/index.ts:480-486` 创建持久 store；`core/engine.ts:994-1005` 启动 `lib/deferred-result-coordinator.ts:10-28,55-75,90-153`；`lib/deferred-result-store.ts:139,159,204,243-285,470` 写结果/中止。 | 任一后台任务返回、重启后补投或 30 秒重试；coordinator 决定是否 `deliverCustomMessage(...,{triggerTurn})`。 | `KERNEL_MIGRATION`：Rust 统一收据、交付屏蔽、幂等标记、父会话继续/仅 UI 记录；外围只能回传结果。对照结果先到/会话失效/重复/崩溃恢复，旧 deferred JSON 不再权威。 |
| W10 | **通用任务状态入口与取消**：`core/engine.ts:866-889` 持久 TaskRegistry；`server/task-bus-handlers.ts:20-63` 对插件暴露 register/update/complete/fail/abort；`server/deferred-result-bus-handlers.ts:91-115` 对插件暴露 deferred 结果；`server/routes/chat.ts:485-509`、`lib/tools/stop-task-tool.ts:93`、`core/session-turn-actions.ts:789-808` 可中止/回档旧任务。 | 插件/用户停任务、重试与回档。 | `KERNEL_MIGRATION` 的公共写入口与主体/attempt 校验；单次插件操作可外围，**插件不得直接裁决任务终态**。对照旧 attempt、重复/跨会话/插件消息、取消竞态；所有 bus 写者均需可枚举。 |
| W11 | **命令转后台**：`lib/exec-command/tool.ts:120-123,180-260` 的 `wait_mode=auto` → `lib/exec-command/background.ts:64-138`，进程退出后 `TaskRegistry.complete` + `DeferredResultStore.resolve`。 | 长命令超出前台窗口后才转后台；PTY 退出触发。 | 命令/PTY 可限权外围；任务登记、终态与父 turn 属 Rust `KERNEL_MIGRATION`。对照窗口内退出、超窗、非零退出、取消、迟到结果/旧 attempt。 |
| W12 | **回档后台化**：`core/agent.ts:863,1224` 装工具；`lib/tools/rewind-tool.ts:131-170` 在当前轮仍流式时延迟回档，写 registry/deferred 成败。 | 用户确认回档且会话仍流式；否则走直接回档。 | 文件恢复可受控单次操作；会话回档事务、后台终态及父回合通知属 Rust。对照确认、流结束后执行、失败回滚、被丢弃后台任务取消。 |
| W13 | **媒体生成/轮询**：`core/engine.ts:583,3748` → `core/media/universal-media-manager.ts:577-595,1039-1040`；`core/media/image-task-runner.ts:458-489` 提交失败可直接写终态；`core/media/poller.ts:154,175-178,303,326-359,379-443` 写 done/failed/cancelled 并通过 deferred bus 交付；`core/media/task-store.ts:391-393,471,505` 持久状态。 | 媒体任务提交、供应商异步查询或重启恢复。 | 供应商查询/下载可外围；持久 task、轮询/重试/取消/结果交付属 Rust `KERNEL_MIGRATION`。对照重启 pending、同一 attempt、提交失败、取消、仅 UI 与父 turn 两种交付。 |
| W14 | **记忆 Dream**：`server/routes/memory-dream.ts:66-82` 手动触发；`lib/memory/memory-ticker.ts:744-752,1065-1082` 自动触发；`lib/memory/dream/runner.ts:98-120,150-200,250-309,314-353` 多阶段模型调用并持久写 succeeded/failed，`lib/memory/dream/state-store.ts:71-76,121-129` 落盘。 | 手动或自动条件满足；不调用 Pi AgentLoop，但独立持模型作业/终态。 | 模型作业编排与记忆写入权属 Rust `KERNEL_MIGRATION`；单次文本处理算法可外围。对照阶段失败、输入变更、重启、取消、修订回滚；不能因无 `executeIsolated` 而遗漏。 |
| W15 | **相邻状态，不冒充同类 worker**：`server/index.ts:972` 装 `lib/goal/goal-engine.ts:128-166,213-225`，`lib/tools/goal-tool.ts:105` 写会话 goal 完成/放弃；`server/routes/chat.ts:1000-1020` 发普通 assistant run 终态。 | 用户/模型目标工具及聊天 turn。 | Goal 不是独立后台 worker，但属于 Rust 会话预算与状态权威，应在 PI-11/SessionService 交接；普通 run 终态是 RunSupervisor 基线，不以“worker 反例”重复计数。协议对照状态、用量、steer 切分与恰好一次收尾。 |

`lib/tools/stop-task-tool.ts`、插件 task bus、`core/media/*` 里的状态词不能简单按 `status:` 搜索后全部判为 Agent loop；反过来只找 `createAgentSession` 会漏 W2-W14。修复代理须给每个发现位点一个“候选身份 + 终态 sink + 注入/调用来源 + 激活条件 + 分类理由”记录；相同组件多条状态写入可聚合，独立业务状态（如 UI 展示投影）须留排除证据。A06 的核心否定例仍须坚持：任何保留为外围的单次解析/SDK/命令/供应商查询，不得创建 Pi session、决定后续 Agent turn，或成为任务/会话终态唯一写者。

## 四、修复后独立 oracle、正负例和完整重跑集合

1. **导入 oracle 独立于候选生成器。**用 TypeScript AST 直接检查 `node.isTypeOnly`、`clause.isTypeOnly`、`specifier.isTypeOnly`，再以 TS emit 核实际 JS 边；CJS 用独立源码调用形态检查。正例是 `lib/pi-sdk/index.ts:60`、`shared/artifact-core/release-order.cjs:10`、`lib/bridge/feishu-adapter.ts:8`；负例是 `lib/pi-sdk/index.ts:61,105`、`lib/pi-sdk/stream-guard.ts:1`；遗漏负例是 `shared/safe-fs.ts:37`。故意反转任一类型位、删除 safe-fs 位置、把条件导入当启动静态，验证必须失败。`export type` 两处的实际包仍有其他运行时边。
2. **worker oracle 从源码双向枚举。**源头搜索 `createAgentSession`、`runAgentLoop`、`.prompt`、`executeIsolated` 的直接/注入转发、`runAgentPhoneSession`、`executeLoopTurn`、`deliverCustomMessage(triggerTurn)`；终点搜索 TaskRegistry、DeferredResultStore、SubagentRun/Thread、Activity、Loop、Cron、Media、Dream 的 `complete/fail/abort/resolve/finishRun/settleTask/logRun/update`，并核 `server/task-bus-handlers.ts` 的外部写入口。让“发现位点集合 − 已分类位点集合”必须为空；合成新增一个隔离调用或状态 sink、把 W2/W5/W8/W9/W13 任一项改外围，验证必须失败。Office 单次解析无模型循环/终态写入应通过外围正例；文件行号是否存在不能替代语义核验。
3. **重新生成与静态门禁。**按顺序运行 `node docs/rust-tauri/R00/r00_t03_import_graph.mjs`、`python3 docs/rust-tauri/R00/r00_t03_build_matrices.py`、`python3 docs/rust-tauri/R00/r00_t03_validate.py`；独立 oracle 应在脚本之后单独运行。逐行复核 51/51 依赖、全部 worker 及全入口差集，重新写报告中的计数/哈希，JSON 可解析，`git diff --check`、`python3 -m py_compile`（仅任务脚本）及 `node scripts/patch-pi-sdk.cjs`。不要只把验证器预期值改成新绿色。
4. **现役行为回归，完整集合。**运行 `npx vitest run`，至少覆盖：`tests/pi-sdk-import-boundary.test.ts tests/pi-sdk-create-session-adapter.test.ts tests/knowledge-source-processors.test.ts tests/tool-schema-validator.test.ts tests/artifact-release-order.test.ts tests/artifact-boot.test.ts tests/artifact-boot-channel-consistency.test.ts tests/hub-plugin-session-agent-capabilities.test.ts tests/cron-scheduler.test.ts tests/scheduler-studio-cron.test.ts tests/office-plugin-tools.test.ts tests/config-loader.test.ts tests/device-registry.test.ts tests/access-route.test.ts tests/subagent-tool.test.ts tests/subagent-run-store.test.ts tests/subagent-thread-store.test.ts tests/workflow-tool.test.ts tests/workflow-host-api.test.ts tests/workflow-integration.test.ts tests/workflow-agent-toggle.test.ts tests/workflow-journal.test.ts tests/loop/loop-controller.test.ts tests/loop/bridge-loop-turn.test.ts tests/loop/alarm-service.test.ts tests/deferred-result-store.test.ts tests/deferred-result-coordinator.test.ts tests/deferred-result-bus-handlers.test.ts tests/task-registry.test.ts tests/exec-command-tool.test.ts tests/rewind-checkpoint-tools.test.ts tests/rewind-to-checkpoint-e2e.test.ts tests/media-poller.test.ts tests/media-task-store.test.ts tests/media-gen-retry.test.ts tests/universal-media-manager.test.ts tests/memory-dream-runner.test.ts tests/memory-dream-model-runner.test.ts tests/channel-ticker-membership.test.ts tests/channel-router-trigger.test.ts tests/agent-phone-activity.test.ts tests/assistant-run-lifecycle.test.ts tests/desk-activities-route.test.ts tests/desk-route.test.ts tests/goal-engine.test.ts`。若只改盘点脚本，既有测试是回归佐证，真正的 A05/A06 证明仍是独立 oracle 和全量差集；若修复触及产品运行代码，再跑 `npm run typecheck` 与 `npm test`。
5. **独立再次验收。**新 Reviewer 从任务书、当前源码、锁文件与所有新候选重审 A05/A06 和两轮旧 finding；不能沿用旧轮 PASS，也不能以本报告替代验收。真实进程、用户插件、外部模型/平台及安装包行为若未跑须如实标为静态范围限制。

## 五、避免下一轮同根因再漏项

修复应让候选表成为**完整发现集合的分类结果**，而不是由人工预选五个例子；验证器对每个导入形式和每个终态 sink 都有独立源码 oracle。把新增源码位点自动变成未分类差集，要求明确分类或可核的排除理由；默认关闭、动态注入、函数内加载、跨进程入口均不能自动消失。只有在 I1-I6、W1-W15 的发现与分类闭合、正负例均有效、R1 与 R2 旧 finding 复现不再出现，并由全新 Reviewer 完整复审后，才有条件考虑 R00-T03 的 PASS。
