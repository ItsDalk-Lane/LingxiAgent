# R00-T01 研究基线与实施基线差异

状态：READY_FOR_REVIEW。记录日期：2026-09-23。

## 已观察事实

- 研究 SHA 与本次实施 `HEAD` 完全相同：`7d1a0c6bc28062ff455adcf8f68c3a80b117e90d`。当前分支为 `codex/rust-tauri-migration`；本机 `origin/HEAD` 指向 `origin/main`，两者在本机也指向同一 SHA。因此**已提交源码没有差异**，任务书中的 R00-T01 路径无需改名定位。
- 启动前工作区已带有 17 个旧任务书的已跟踪删除，以及新任务书目录的 33 个未跟踪文件。它们属于用户已有改动，未恢复、覆盖、纳入本任务成果或提交。`AGENTS.md` 在磁盘存在、受本地 `.gitignore` 忽略，本次已读取。相关范围没有另一份局部 `AGENTS.md`。
- `docs/rust-tauri/ORCHESTRATOR_PROGRESS.json` 由总控维护，本任务未写。开始 R00-T01 时 `docs/rust-tauri/` 没有阶段 HANDOFF/REPORT；不继承历史报告中的 PASS。
- 工作树原有改动不改变本任务实际启动源码；本任务没有修改生产代码或配置。旧任务书删除、新任务书未跟踪文件的前后哈希核对见 `artifacts/rust-tauri/R00/T01/worktree-before.json`、`worktree-after.json`、`worktree-preservation.json`。

## 启动定位差异与选择

`npm run server` 先经 `scripts/launch.js` 调用 `scripts/dev-env.js`，会把传入的 `LINGXI_HOME` 强制改为真实用户目录下的 `~/.lingxi-dev`。本次没有用它执行隔离探针。实际桌面开发态的服务链为 `desktop/main.cjs` → `server/bootstrap.ts` → `server/main-full.ts` → `server/index.ts:startServer`；独立服务也落在同一个 `main-full.ts` 组合入口。本次直接启动 `server/bootstrap.ts`，明确传入 `LINGXI_SERVER_ENTRY=server/main-full.ts`、临时 `HOME`/`LINGXI_HOME` 和随机监听端口，沿真实产品组合根检查启动和关闭。

## 首轮验收边界（历史记录）

真实 Lingxi.app 及其生产服务在探针期间保持运行，未停止或修改。真实 `~/.lingxi` 与 `~/.lingxi-dev` 仅做前后只读目录清单摘要；`~/.lingxi/data-epoch.json` 仅做内容哈希，没有在真实目录内新建哨兵。另建受控生产样例目录并设置只读哨兵，在服务启动前后对该目录及哨兵做摘要比较；沙盒同时拒绝服务进程写入整个真实用户 HOME。真实目录两次清单摘要一致，但并发应用可能在两个扫描之间自行写入再恢复，单靠清单不能排除这种情况；沙盒拒写负向探针和隔离服务进程参数用于归因。

一次更严的全局拒写沙盒试验使临时 `LINGXI_HOME` 自身的 `mkdir` 也被 macOS 拒绝，退出码 1，记录在 `startup-probe-attempt2-deny-all.json` 和对应输出。该试验是沙盒策略失败，未计作产品启动失败；最终采用拒写真实 HOME、允许独立临时目录的策略，并保留所有尝试证据。

## F01 复测后的现状

第 2 轮在真实 `~/.lingxi` 与 `~/.lingxi-dev` 内分别排他创建唯一命名、权限为 `0400` 的临时哨兵，逐文件读取计算内容及目录完整摘要；没有保存文件名、文件内容或链接目标。两枚哨兵在真实服务启动、系统拒写探针和关闭后均保持原样，随后只清理本任务创建且身份、权限、内容均未变化的哨兵。两次系统拒写探针均返回 `EPERM`，真实服务在隔离 `HOME`/`LINGXI_HOME` 启动、健康检查 200、关闭退出码 0；真实 Lingxi.app 始终运行。

首次完整复测中，`~/.lingxi-dev` 的完整摘要及内容摘要均未变化；`~/.lingxi` 的内容摘要相同，但包含文件身份与时间的完整目录摘要发生变化，说明有并发元数据活动，见 `startup-probe-r2-attempt1.json`。随后完整复测中两个目录摘要均稳定，见 `startup-probe-r2.json`，但真实应用仍持续运行，不能据单次稳定结果确认无其他写者的安静窗口。本轮 R00-A01 整体为 **BLOCKED**；须在两目录无其他写者的安静窗口重跑完整验收并观察完整摘要稳定，或由规格负责人正式批准不同验收条件。R00-A02 以首次快照为锚再次核对，17 个删除及 33 个未跟踪任务书文件保持不变。详细摘要和清理记录见两次探针结果与 `worktree-preservation-r2.json`。

## R2 findings 修正与本机复测（2026-09-23）

F02 已在 `/tmp` 重现并修正，故障注入与两真实目录哨兵清理均有证据。F03 改为动态枚举与集合相等校验；A02 增加暂存区和未跟踪集合核对。应用退出后，本轮第一次 A01 因探针误查不存在的服务信息字段退出 1，原始证据保留；修正后一次完整复测退出 0，两真实目录完整及内容摘要稳定、哨兵安全清理、系统拒写、隔离服务启动/退出与安静窗口均通过。R2 在应用运行时的目录变化仍作为历史失败保留，不能推断为本轮服务写入。现行结果见 `R00-A01.result.json`、`R00-A02.result.json` 和动态候选摘要；任务仅为 READY_FOR_REVIEW。

最终探针增补自身脚本摘要后第三次完整复测退出 0；两个真实目录完整/内容摘要、哨兵和清理、拒写、安静窗口与隔离服务检查均通过。三次 R3 原始尝试均保留，最终脚本摘要与第三次记录相符。

最终核查补上普通文件初次身份与打开对象的一致性检查，并增加 `/tmp` 替换反例；定稿脚本第四次完整 A01 复测退出 0，脚本摘要与原始记录一致。此前三次 R3 尝试及原 R2 失败均保留。

## R3 findings 修正后的 R4 候选

R3 独立审查确定最后父目录关闭失败会遗留哨兵、源名字 `stat → unlink` 竞态会误删外来项、总校验器不现场重核 A02。旧自报结论由 `R00-T01_REVIEW_R3.md` 的 FAIL 覆盖。本轮先在 `/tmp` 重现，再以排他原子移动和隔离目录清理、事务登记、只读工作树现场收集一并修正；负向矩阵与当前实测见最新 `R00-T01_REPORT.md`。原 R1/R2/R3 失败证据及六次真实尝试保留，新增真实尝试有唯一文件名。当前仅提交 READY_FOR_REVIEW，未获独立放行。
