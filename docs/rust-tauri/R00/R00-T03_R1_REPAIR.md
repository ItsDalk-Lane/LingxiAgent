# R00-T03 第一轮修复交接

状态：**READY_FOR_REVIEW**；仅修复 R1 审核的 F01，不自行判 PASS。Task base `4b4a1d98f6d2d0e03f9573aa75b7992efed03db6`，当前 HEAD `60dbe0384e553f0a0aab7d173b2dc1ba43987b82`。后者相对基线的已提交差异只涉及 `.gitignore` 和旧任务书解除跟踪；本次未改产品源码、锁文件、任务书、审核报告、总账或 R00-T02 冻结文件，也未提交/推送。

## 根因与同类修复

原依赖生成器把额外直接依赖套用“前端视图按需加载”模板，并截取前三个导入位置；重点依赖的单行描述也混淆“加载模块”和“使用功能”。这使服务组合静态依赖、桌面宿主与实际调用方被漏掉。

- 51 行现在逐行列出模块加载时点、功能调用时点、完整直接导入位置及其 `import`/`require`/动态 `import`/类型专用标记、组合入口可达性、消费者所在进程、Rust/外围/React 归属、协议对照和退出证据。`source_refs` 不再截取前三个；无直接生产导入的 `@tiptap/pm`、`codemirror` 明确标出证据边界，Pi 图片 WASM 的间接动态加载给出已安装供应商包定位。
- D10/D11/D13 明确知识库静态导入与文件/桌面按操作加载并存；D40 补服务端文件补丁；D41/D50 归回服务端配置与工具校验；D49 补桌面启动、更新、CLI bundle 的旧产物排序和 Rust/Tauri 负责人。
- 全表复核还修正 D04 SQLite addon 延迟加载与文件历史消费，D16/D17 网络代理服务组合，D21 Bridge/桌面浏览器 WebSocket，D22 文件监听，D23 设备配对与微信登录两条二维码链，D24 技能/角色卡/搜索二进制 ZIP，D37/D42/D43/D44 React 与桌面截图渲染，以及 D45/D46 的类型专用与运行时导入区别。
- 验证器现从导入图和锁文件逐行独立比对全部 51 项，并要求关键跨端负责人、协议场景和调用链定位。负例分别注入未登记 Pi 能力、错误 worker 分类、静态依赖被写成动态、截断服务端引用、泛化产物负责人、把类型专用导入误作运行时、把无直接消费的包写成已直接加载；均被拒绝。

## 重放与结果

```bash
node docs/rust-tauri/R00/r00_t03_import_graph.mjs
python3 docs/rust-tauri/R00/r00_t03_build_matrices.py
python3 docs/rust-tauri/R00/r00_t03_validate.py
```

上述重放退出码均为 0：1664 个受控源码文件、20 处 Pi 包导入、97 处适配层导入、16 处不透明动态导入、81 个静态可达 Pi 消费文件；14 项 Pi 职责、51 项依赖，差集为空。验证器 37 项通过，其中 7 项负例。`node scripts/patch-pi-sdk.cjs` 通过，三个已安装 Pi 目标文件前后哈希不变。

两组相关既有测试共 13 个文件、244 项通过，覆盖 Pi 边界、Hub/cron/Office、知识来源与导入、工具 schema、配置、设备接入、产物版本排序及桌面启动/OTA。没有启动真实服务或安装包、外部模型/Bridge，也没有碰用户数据；静态可达与本机测试不构成跨平台发布证明。

冻结核对：八份候选（`PI_REPLACEMENT_MATRIX.json`、`R00-T03_IMPORT_GRAPH.json`、`R00-T03_REPORT.md`、`R00-T03_VALIDATION.json`、`RUNTIME_DEPENDENCIES.json`、三个 `r00_t03_*` 脚本）按文件名排序，以每行 `SHA256  文件名\n` 拼接再取 SHA-256，结果为 `997167baefa82512e60ef667c8db062a90c469f7822b1a6b5475f941b2909e96`。报告单文件摘要 `b46de92fa1c5d0eac45240273298fcfd9f8a54de6047269585ca31a40cf12e50`；锁文件摘要仍为 `e54a16fe14f15b4797069106392040924a5dd616c69a73bd025729090505ac8b`。全部 JSON 可解析，八份候选无行尾空白。

## 复审重点

新的独立验收者应从源码和锁文件重新审 51 行，抽查服务组合静态加载与实际操作触发是否分清；特别复核知识库文档、服务配置、工具参数校验、设备配对、桌面产物排序、Bridge/浏览器 WS、前端类型专用导入及两个无直接生产消费的包。R00-A05/A06 的旧轮 PASS 不作为本轮自动结论。
