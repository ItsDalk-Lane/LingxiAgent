# R00-T01 两轮验收后的覆盖式根因分析

日期：2026-09-23。范围仅为 R00-T01；本文件是供新修复代理使用的分析，不是修复提交或验收 PASS。候选分支 `codex/rust-tauri-migration`，Task Base SHA 与当前已提交 HEAD 均为 `7d1a0c6bc28062ff455adcf8f68c3a80b117e90d`。工作树原有的旧任务书 17 个删除、新任务书 33 个未跟踪文件不得恢复、覆盖或混入本任务。`R00-A01` 和 `R00-A02` 在原规格中均为 REQUIRED；环境不足时保持 BLOCKED，不能放宽 A01 或用重试挑选一次成功结果。

## 结论与边界

现行结论是 **R00-A01 BLOCKED、R00-A02 PASS、R00-T01 不放行**。R1 的 FAIL 是真实目录哨兵和内容哈希缺失；R2 已在 `~/.lingxi` 与 `~/.lingxi-dev` 内建立真实哨兵并逐文件摘要，但首次完整运行中 `~/.lingxi` 完整摘要改变，随后一次摘要稳定时 Lingxi.app 仍运行。前一次变化的写入者未知，不能据此断言隔离服务改写了用户数据，也不能用后一次稳定抵消。R2 还确认哨兵创建中途失败的遗留文件，以及候选清单覆盖不全。**环境更新：**本次会话中用户已授权暂时关闭 Lingxi.app；总控代理使用应用正常退出命令关闭，并确认原主进程 PID 29489 及其 helper/server 子进程均已退出。A01 安静窗口复测因此现可执行，但运行前仍须重新核实两个目录没有其他写者；该更新不追认历史失败，也不修改原验收要求。修复代理不得覆盖真实用户数据。

原规格见 `Lingxi_Rust_Tauri_Taskbooks_2026-09-23/R00_基线、功能保护与验收地图.md` 的 R00-T01，以及 `task-catalog.json`、`acceptance-catalog.json` 中 R00-A01/A02：A01 必须在真实目录设只读哨兵、另建空测试目录，启动和关闭后生产目录哈希及哨兵不变，数据与日志只出现在测试目录。`01_通用执行约束.md` 第 52、62 行和 `05_验收与性能协议.md` 第 110—112 行要求结果绑定实际环境、命令、退出码和证据；失败与全部重试必须保留。

## 一次覆盖式修复矩阵

| 项 | 根因与现有证据 | 修复代理应做的事 | 可观察完成条件 |
|---|---|---|---|
| 1. 真实目录安静窗口，**历史外部阻碍，现可复测** | `startup-probe-r2-attempt1.json` 中生产目录完整摘要 `5d573013… → d1208845…`，内容摘要相同；`startup-probe-r2.json` 的单次摘要稳定，但两次前后 Lingxi.app 主进程均为 PID 29489。`startup_probe_r2.py:145-148,180,265,282` 的 PID 前后相同只说明进程仍在，不证明没有写入，也未覆盖其他服务/帮助进程。目录扫描不是原子快照。现已获用户授权暂时关闭应用，且总控代理确认原进程树退出。 | 修好取证脚本后，在现有窗口重新检查两个真实目录没有其他写者，记录应用及实际数据写入进程状态、窗口时间、两目录完整与内容摘要、哨兵、沙盒、服务参数、启动/退出和清理。发现任何并发活动或摘要变化即记录并保持 BLOCKED，不继续重试到出现绿灯。无需再次请求已取得的关闭授权，也不要自行重新启动 Lingxi.app。 | 在可归因的同一安静窗口完成一次完整 A01；两目录扫描无错误且前后完整摘要相同，哨兵身份/权限/内容不变并安全清理，服务数据和日志位于隔离目录，真实目录拒写、服务正常关闭；全部尝试和异常一并留存。若窗口失效，A01 继续 BLOCKED。 |
| 2. 哨兵创建的事务边界，**确定缺陷** | `startup_probe_r2.py:114-123` 先 `O_EXCL` 建文件，再写入、`fchmod`、`fsync`、读回哈希；调用方直到 `:187-189` 函数返回才登记清理。R2 在 `/tmp` 注入 `fsync` 的 `ENOSPC` 后观察到权限 `0400` 的遗留哨兵。相同漏洞覆盖第一目录、第二目录，以及写入/短写、权限、同步、`fstat`、关闭和 `sha_file` 失败。 | 首次成功创建后立即登记路径与文件身份，在创建函数内部为后续每一步提供失败清理；完整写入要处理短写并验证预期内容。清理只针对能确认由本次排他创建、未被替换或更改的文件；不能确认时不删除，明确留下需人工处理的异常和 BLOCKED 结果。第二目录失败时两个目录都按各自身份清理。 | 在临时目录逐点注入写入、短写、`fchmod`、`fsync`、`fstat`/读回、关闭和第二目录失败：自有未变哨兵全部清理，原有同名文件绝不覆盖，被替换/改动的路径绝不误删且明确报错；每条路径的退出码、清理状态和目录前后状态可复查。 |
| 3. 扫描和清理的路径身份，**同类风险** | `tree_digest` 在 `startup_probe_r2.py:50-101` 先按路径扫描，再以 `entry.path` 打开或递归；目录若在这两步之间被替换成链接，`visit(entry.path)` 可能沿新链接走出目标树。`sha_file(path)` 与 `sentinel_state(path)` 在 `:31-35,126-129` 也按路径再次打开；`delete_owned_sentinel` 在 `:132-136` 核对后再按路径删除，存在替换窗口。这些是代码可达的竞态风险，**未观察到真实数据被误删或越界读取**。 | 将真实目录巡检限制在已打开且核对身份的目录/文件对象上，不跟随新出现的链接；哨兵读回、核对和清理尽量用同一身份锚定。无法安全确认时失败封闭并留下诊断，不能以路径名相同为理由删除。安静窗口仍是 A01 的必要外部条件，代码防护不代替它。 | 临时目录的链接替换、目录替换、哨兵替换/改动故障注入均不能读出目标树外的数据或删除非本次文件；失败被标记，身份不确定时没有“清理成功”或 PASS。 |
| 4. 候选清单完整性，**确定缺陷** | `build_candidate_summary_r2.py:13-42` 的固定 `FILES` 只收 28 项；R2 审查前两个目录共有 47 项，遗漏 19 项。R2 审查报告写入后为 48 项，新增本分析报告后为 49 项，旧清单仍只列 28 项。遗漏包括旧探针 `startup_probe.py`、旧沙盒证据、首次日志、`worktree-after.json` 与旧测试输出。`verify_candidate_r2.py:21-34` 只查清单内哈希，不比较实际目录文件集合，所以 `ok: true` 不能说明候选完整。 | 从明确定义的 R00-T01 候选目录动态枚举实际文件，按路径与内容摘要建立清单；明确说明自引用的摘要文件、验收报告等排除规则，并检查实际集合与声明集合完全相等。每次尝试产物和历史失败证据也应归档覆盖。校验器不能把旧的 `BLOCKED` 状态硬编码为永久预期：修后应验证结果与证据图及门禁规则一致。 | 随机增、删、替换任一纳入范围的文件，校验都失败；仅显式排除的自引用/审查文件不参与自身摘要，排除列表可审阅。所有 A01/A02 结果引用、候选清单、最终工作树和 HEAD 在同一快照下互相一致。 |
| 5. 单次探针与场景结论、尝试留存，**取证流程缺陷** | `startup_probe_r2.py:284-286` 仅按本次布尔检查将探针写为 `PASS`，未纳入“无其他写者”条件；因此第二次原始探针 `PASS` 与总体 `R00-A01.result.json` 的 `BLOCKED` 同时存在。探针和日志使用固定 `startup-probe-r2.json`、`server-stdout-r2.txt`、`server-stderr-r2.txt`，直接重跑会覆盖上一次原始结果；首次失败是人为改名后才保存。`R00-A01.result.json` 保留两次退出码是正确方向。 | 把“单次观察值”与“REQUIRED 场景能否放行”分开：原始探针记录检查是否稳定，不在缺少安静窗口归因时宣称场景 PASS。每次尝试使用唯一 ID、不可覆盖的原始文件；场景结果聚合全部尝试、阻碍和安全清理结果，失败不能被后续成功覆盖。更新报告、摘要和校验规则以同一结论为准。 | 模拟第一次摘要变化、第二次稳定，聚合结论仍 BLOCKED；所有原始退出码和日志可检索，文件哈希可验证；缺窗口、未清理、扫描错误或证据缺失时无法进入 PASS。 |
| 6. A02 工作树辅助校验，**预防性补强** | R2 独立审查已核对原有 17 个删除与 33 个未跟踪文件，A02 的 PASS 有依据。但 `worktree_verify_r2.py:34` 仅用 `git diff --name-status` 检查其他已跟踪差异，辅助脚本本身没有检查暂存区；原快照的 `status_short` 含全局状态。此点不是本轮 A02 失败证据。 | 修复取证脚本时将已暂存变更和原有未跟踪文件集合一并核对，防止以后把新增暂存改动误判为干净；保留原始工作树，不做 reset。 | 测试性增加暂存变更或改动原有任务书文件时辅助校验失败；在现有工作树，17 个删除与 33 个文件仍逐项相符，原有内容无损。 |

## 调用链与影响范围

真实桌面开发态的服务链是 `desktop/main.cjs → server/bootstrap.ts → server/main-full.ts → server/index.ts:startServer → ensureFirstRun/LingxiEngine.init`。现探针直接调用相同的服务组合入口；它证明的是**独立服务的数据路径**，不能扩写成已验收图形桌面或安装包。`scripts/launch.js → scripts/dev-env.js` 会覆盖传入的 `LINGXI_HOME` 为 `~/.lingxi-dev`，所以 R00-T01 不能改用该启动器而只看设置过的环境变量；必须继续检查实际服务 home、参数及隔离目录写入。R2 的沙盒仅约束被其启动的服务进程与同配置负向探针，不约束用户已运行的 Lingxi.app。`tree_digest`、哨兵读回与清理、结果聚合、候选摘要和工作树核对是本次系统搜索找到的同类调用路径；未见产品源码本身被本次候选改动。

## 修后完整重跑集合与放行规则

1. **先在 `/tmp` 完成取证脚本故障注入回归**：现有 `test_startup_probe_r2.py` 的等长内容改写、排他创建、已变更哨兵不清理，加上矩阵第 2、3、5 项的全部失败点、第二目录、链接/路径替换、尝试不覆盖和失败后聚合仍 BLOCKED。真实目录不用来做故障注入。
2. **再运行启动与路径回归**：`vitest run tests/startup-contract.test.ts tests/hana-runtime-paths.test.ts tests/server-composition-boundary.test.ts`。记录完整测试数量、退出码、隔离 `HOME`/`TMPDIR`/`LINGXI_HOME` 和真实服务入口；skipped 不算通过。若实际修改了生产代码，再按受影响范围扩大门禁。
3. **在当前已获授权的安静窗口完整重跑 A01**：先核实原应用及其相关服务仍已退出，两个真实目录没有其他写者；再核对两目录只读哨兵、前后完整及内容摘要、真实服务启动与关闭、OS 拒写、实际 home/日志/数据、进程参数、退出码、哨兵身份与清理。先完成安全的脚本修复及临时目录验证。任何失败、并发变化或不能归因，A01 保持 BLOCKED/FAIL，保留所有尝试。
4. **重跑 A02 与证据封存**：对照首次工作树快照验证 17 个删除与 33 个未跟踪文件及暂存区；重算 A01/A02 每条证据引用；动态枚举候选目录并验证文件集合及 SHA-256；把最终结果绑定实际 HEAD、工作树、工具链和本次探针版本。只有 REQUIRED A01 与 A02 都有真实 PASS、清理安全且独立复审认可时，R00-T01 才可申请放行。本地结果不代表其他平台、图形桌面、安装包或 R00 阶段整体通过。

本次分析没有运行会写真实用户目录的探针，没有停止应用，没有修改源码、测试、探针、任务书或总控账本。
