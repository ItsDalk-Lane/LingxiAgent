# R00-T02 第七版修复说明（回应第六轮 FAIL）

本文件记录修复者对 R6-F01/F02/F03 的处理，不给 R00-A03/A04 判 PASS。分支 `codex/rust-tauri-migration`，HEAD/Task Base 为 `16aeb380d58d68ff1a38bb46f5cc5d18f985f084`。产品源码、用户原文件、总控账本、原任务书、R1—R6 独立审阅和根因报告均未由本次修复改写；没有提交或推送。

## R6-F01：新选择分支不能靠同步摘要绕过

`ask_user_ast_gate.cjs` 从 `const decision = await promise` 对应的 `decision?.action` 出发，用 TypeScript AST 追踪别名、常量集合、本地纯函数、条件和提前返回；对严格相等、反向比较、不等于、`!`/`&&`/`||`、数组 `includes`、常量 `Set.has`、`switch` 及默认分支逐值求结果。动态集合、被改动的集合、动态 case、无法确定的条件或结果会列 `MANUAL`，使自动检查非零。同名但无关的 `action` 对象或内层同名变量不会被当作来源。

发现器将源码值、条件摘要和每条结果路径摘要，与人工分支契约双向比较。结果路径摘要包含返回的消息、标志、答案形成语句等，不会因新返回与旧兜底的布尔标志相同而放过。仅调整等价写法时，人工裁决并同步条件摘要后可通过；若改了结果文案，旧契约不能通过。人工契约补齐 `/confirm`、`/reject` 命令的生产入口证据。

内存反例覆盖新增 `skipped` 的九种条件写法、常量别名、同标志不同消息、动态集合/动态 `switch` 的 `MANUAL`、无关同名变量、等价改写正例与结果依赖改动。完整 `build()` 在逐叶和统一审查摘要同步、人工契约不变的情况下，对 `includes(skipped)` 及“同标志不同消息”的新增分支仍按预期非零。

## R6-F02/F03：按实际控件和结果重写 UI 场景

独立 UI 源码审查从原矩阵查出 75 个动作结果需修、10 个真实入口漏项及 10 个不可达错挂。现矩阵为 26 页、170 个动作、313 个控件/效果变体。每个动作挂本页四态结果场景 `ui_result_scenario_id`，每个变体仍挂真正的效果叶及其补充场景。矩阵动作已逐字段回写 `FEATURE_INVENTORY.json` 与 `FEATURE_STAGE_ACCEPTANCE.json`，两份生成清单同矩阵的动作/变体差集和内容漂移均为零。UI 独立审查按矩阵 SHA `ec4943e76fd2d5bf0eff358d635660ab67851ae8232c5ffe897b96037c83989f` 逐项复核，170/313 项均标 `CORRECT`，其结论限于现役源码静态路径，不代表迁移运行验收。

主要修正包括：供应商摘要加载失败被静默吞掉；选择供应商与临时草稿只改本地状态，真正保存另走配置请求；聊天模型与媒体模型增删各用不同路径；自动化允许创建空提示词、停用状态的草稿，后来尝试启用才阻止；HTTP 和网络失败的 toast 按各按钮实际处理区分；用量页是十张指标卡，四张图各自加载。聊天、频道、地图、Bridge、记忆、OAuth、快捷键等页也逐动作校准了加载、保存、失败、假成功、乐观更新与不回滚的结果，删去不可达的支持边并补齐真实可见动作。新增的记忆健康提示 `agent#18` 在本页验收场景独立断言健康条显示和读取失败；连接器 OAuth 取消后仍可能弹保存成功的现役行为也写入了场景。

## 已验证与边界

- `python3 -B docs/rust-tauri/R00/r00_t02_inventory.py --write --negative-checks`：退出 0；旧反例与新增 AST 反例按预期检出或明确 `MANUAL`。
- `python3 -B docs/rust-tauri/R00/r00_t02_inventory.py`、Python 编译检查和 `node --check docs/rust-tauri/R00/ask_user_ast_gate.cjs`：退出 0。
- 登记 832、保留叶 736（HTTP 491、非 HTTP 245）、补充子场景 736，结构差集为空。736 个迁移子场景仍为 `SPECIFIED_NOT_EXECUTED`；五项 UI 动态边界与统一审查的外部/动态人工边界仍按原归属留待后续执行。现役源码静态核对不代替 Rust/Tauri 实装、跨平台打包、真实供应商和平台测试。

证据见 `R00-T02_AST_CONDITION_AUDIT.json`、`R00-T02_UI_ORACLE_REVIEW.json`、当前矩阵与生成清单。AST 独立审查记录了修复期间的漏检样例；上述新增负例及最终门禁执行由修复者补齐。后续仍须由未参与修复者按本版候选作 R00-A03/A04 独立裁决。
