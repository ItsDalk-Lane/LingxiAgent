# R00-T01 固定基线与隔离工作区：R2 findings 修正后候选

**实施状态：READY_FOR_REVIEW。R00-A01：本机复测 PASS；R00-A02：PASS。** 本报告只覆盖 R00-T01，尚待独立复审，不代表 R00 阶段通过。候选位于 `codex/rust-tauri-migration`，已提交 HEAD 仍为 `7d1a0c6bc28062ff455adcf8f68c3a80b117e90d`；本次未提交、推送或发布。

## 根因与修复

R1 缺少真实目录哨兵和完整内容摘要。R2 补齐取证后，真实应用仍在运行，首次完整目录摘要发生变化；随后单次稳定不能抵消那次失败。R2 独立审查又发现哨兵创建中途失败可能遗留、候选清单固定漏项。历史原始文件、R1/R2 审查结论和根因分析均保留。

本轮先在 `/tmp` 重现 `fsync` 失败导致的残留，再修复哨兵事务：排他创建、完整写入与读回、身份核对、失败清理和第二目录失败时的外层清理。只有身份、权限和内容能证明仍属本次创建才删除；不确定时保留并阻断通过。目录扫描使用已打开的目录/文件对象，不跟随中途换上的链接。每次真实探针用独立文件名保存原始结果和日志；单次结果标为观察值，场景结果另行聚合。候选摘要现动态枚举本任务两个目录，并核对文件集合与每项哈希；A02 校验加入暂存区和未跟踪集合。

## 本轮实测

| 场景 | 结果与证据 |
|---|---|
| R00-A01 失败路径与安全清理 | `/tmp` 故障注入 10 项通过：写入、短写、权限、同步、读回、关闭、身份查询、第二目录、目录、普通文件与哨兵替换及同环境失败聚合。`probe-regression-r3-final.txt`。 |
| R00-A01 第一次真实复测 | 退出码 1，原始 `startup-probe-r3-b0e1de4a10026986fc615e34.json` 与两份唯一日志保留。真实目录前后完整及内容摘要稳定、哨兵未变且安全清理、OS 拒写、服务健康和退出、安静窗口均通过；唯一失败是新探针误查 `server-info.json` 中不存在的 `lingxiHome` 字段。源码证实该文件不包含此字段，已改为核对隔离目录中的服务信息文件、启动日志及实际数据目录。 |
| R00-A01 修正后的真实复测 | 退出码 0，`startup-probe-r3-81810c93302dd6e4dbd26aef.json`。应用与相关服务进程、两个目录的打开句柄在前后检查均为空；两处只读哨兵身份、权限和内容不变并安全清理；两处完整与内容摘要前后相等，扫描无错误；OS 对两处写入均拒绝；真实 `server/bootstrap.ts → main-full.ts` 服务健康返回 200/ok、退出码 0；HOME、TMPDIR、LINGXI_HOME 和实际服务信息、数据、日志均落在 `/tmp` 隔离目录。此结果不抹去首次失败及 R2 历史变化。 |
| R00-A01 脚本摘要复测 | 退出码 0，`startup-probe-r3-e0d8c213225442074a0373b0.json`；前述检查再次成立。之后最终核查发现普通文件首次身份检查与打开之间仍有替换窗口，随即补上并保留这次原始记录。 |
| R00-A01 定稿脚本复测 | 退出码 0，`startup-probe-r3-5cc673966167ef10c4b0332f.json`；前述所有检查成立，且原始记录中的脚本摘要与当前交付脚本完全相同。前三次 R3 原始记录均保留。 |
| R00-A01 启动与路径回归 | 隔离 HOME/TMPDIR/LINGXI_HOME 下，指定 3 个文件、26 项测试通过，退出码 0，见 `targeted-tests-r3.txt`。 |
| R00-A02 | 原有 17 个已跟踪删除、33 个未跟踪任务书文件逐项一致；暂存区和其他已跟踪差异为空。新增的 `/tmp` 反例对暂存变化、原文件变化均会拒绝，见 `worktree-preservation-r2.json` 与 `worktree-regression-r3.txt`。 |
| 候选完整性 | 从本任务文档和证据目录动态枚举；`test_candidate_r3.py` 在 `/tmp` 副本上分别加入、删除、改写文件，校验均拒绝；原副本通过，见 `candidate-negative-r3.txt`。只有候选摘要自身和独立校验输出被明确排除。 |

A01 的原始两次 R2 尝试与本轮四次尝试均在 `R00-A01.result.json` 中列出；同一验证版本的失败不能靠后续稳定尝试抹去。本轮第一次因取证断言与实际服务文件格式不符退出 1；后续每次脚本修正均保留原始记录并重新跑完整链路。定稿版本的脚本摘要与最后一次运行一致。当前本机独立服务场景满足原 A01 条件，等待独立审阅判定。真实用户内容、密钥和文件名未写入新证据；目录证据仅保留摘要、数量与合成哨兵名。

## 边界

本轮没有改产品源码，没有验证图形桌面、安装包、其他平台或真实外部供应商。真实目录扫描不是原子快照；本机通过应用退出、相关进程和打开句柄前后为空，以及完整摘要稳定建立本次安静窗口证据。旧 R2 的并发变化写入者仍未知。候选摘要与证据哈希由 `candidate-summary-r2.json` 和 `verify_candidate_r2.py` 核对，最终以两份结果文件及完整候选清单为准。
