# 本地模型子系统执行日志

## 2026-09-03：Phase 8

- 最终证据索引包含 209 文件、49,277,530 字节；当前适配器及本轮核心源码摘要复核一致。最终进程清单筛选无本实验语音/识字原生进程残留（筛选无匹配返回 1，不是测试失败）。

- 最终门禁完成：13055 通过、7 跳过、0 失败；1295 文件有通过用例，1 文件全跳过，81.70 秒。三段类型、范围代码规范（0 错误/7 警告）、开放边界和差异格式通过。
- 最终源码独立重跑两项各六轮回收：识别 99.171%–99.995%，朗读 99.487%–99.986%；各五次取消总范围 5.439–6.981 ms，返回前进程已退出且重载成功。最终长音频 1,501,240 字节，空闲物理卸载通过。新报告独立保存，不替换 Phase 6/7 历史失败。
- 本阶段已完成，没有提交、推送、发布、重打安装包或改日常配置。剩余产品范围（包括朗读吞吐、真流式、录音格式、默认并发预算、其他模型与外部平台）明确保留。

- 四能力同时调用、合成再转写、嵌入/识字交替十轮通过；大档活动进程最大 1，排队取消 0.422 ms，测试使用小档 4096 MiB / 大档空闲 100 ms，不冒充默认并发预算。宿主 SIGKILL 实验：识别/朗读子进程分别 27.32/44.89 ms 自动退出。测试只杀本次创建的隔离宿主。
- 持久化扫描仍 70 stores/809 sites，日志协议源码变更后指纹按 compatible 更新为 sha256:d9772463928b11be8a2d9d3b40aa7cea22f21b8a7d263787f6095fb5462bd68e；没有数据迁移。CLI 闭包仍 10687 文件，开放边界与差异格式通过，最终全量回归进行中。

- 两项真实语音在系统禁止全部网络、仅允许本机管道的沙箱中通过；沙箱内无法采样子进程内存，相关数值明确仅宿主，不作为内存证据。宿主意外退出探针首跑因嵌套脚本换行转义错误未执行到模型，补充标准错误取证后改用普通日志输出；失败证据保留。

- 严格六轮真实回收、各五次取消/进程退出/重载、31 秒长音频和空闲物理卸载全部通过；4 文件 24 项目标回归通过。查找持久化扫描脚本时误用了不存在的旧名称，改用文件清单定位，不重复猜测。

- 已实现独立语音标准管道包装、同实例串行、取消/超时/错误等待进程终止、重载后实时进程编号、音频响应上限。首批 3 文件 18 项通过；三段类型检查通过，范围代码规范 0 错误。
- 隔离运行包升级到 1.13.7-local2（33 文件，kind=sidecar），模型权重身份不变，local1 历史包保留。真实识别冷/热/卸载/重载通过，进程树峰值 968.9 MiB，释放后 275.4 MiB（基线 259.4），正在执行严格六轮回收与取消复用。

- 用户确认允许两项语音改为独立进程。恢复脚本仍返回无关历史，不操作外部配置。两次计划补丁因进度标题锚点错误整体失败，现按实际标题更正。
- 现有旁路取消在进程停止前返回，语音需要增加串行请求与退出确认，避免取消当前请求误伤排队请求。运行包必须明确标记 sidecar，保留 small 档和模型权重身份。

## 2026-09-02

- 完整读取用户附件（408 行），确认这是双代理、跨层、多平台的大型实现任务。
- 完整读取并启用 `openai-docs`、内置浏览器控制、文件化计划三项技能。
- 按官方 OpenAI 文件上传说明核对协作通道限制。
- 运行 session catchup；发现的旧会话内容与本任务无关，未触碰。
- 检测到根目录已有其他任务计划和 `PROGRESS.md`，改在本目录建立独立计划，避免覆盖与大小写冲突。
- 当前状态：Phase 0 进行中；尚未打包、尚未上传、尚未修改产品代码、尚未提交或推送。
- 完成 Git 与运行时冻结：`feat/pending-sep02` / `3eab8589...` / Node 24.16.0 / npm 11.13.0；未发现用户已有未提交源码改动。
- 读取项目规则、README 和完整包配置；根目录不存在 `CLAUDE.md`，已如实记录而非假设缺失内容。
- 完成第一轮调用链还原：确认一次性本地命令包装不等于长驻 sidecar；本地语音适配器、文字识别执行链和通用模型下载器均尚不存在；代理与观测边界可复用。
- 临时打包方案第一次因包含删除动作被安全规则拒绝，未产生或改动任何包；已切换为纯白名单打包。
- 第二次候选包发现一个中文路径被 Git 转义后漏包，未上传；第三次改用原始路径清单，3,245/3,245 条目一致，摘要与完整性检查通过。
- 已连接 Codex 内置浏览器并读取上传流程；当前无已打开标签页，下一步在同一浏览器中新建 ChatGPT 页面确认登录状态。
- 已打开 ChatGPT 新对话并确认 `Charles Pro` 登录有效、多文件上传入口可用；尚未发送任务。
- 第一次网页上传尝试点中了错误的隐藏文件输入并超时；没有上传或发送，页面保留，下一步改走可见附件菜单。
- 已通过可见附件菜单把安全源码 ZIP 与完整原始需求 TXT 暂存到 `Charles Pro` 的新对话；消息尚未发送，等待浏览器外发操作的当下确认。
- 用户已在当下确认发送。任务已发至 ChatGPT Pro，对话链接 `https://chatgpt.com/c/6a97bd95-6680-83ea-a74b-2f27de0a29b8`；页面处于 `Pro 思考中`，未催促或中断。
- 独立查阅上游官方资料：small 的 SenseVoice/Kokoro Node 通道有直接证据；多项 large/OCR/TTS 的原生 q4 sidecar 与三平台分发仍未闭环，已列为重点审查风险。
- Pro 已进入源码检查。回读上传 ZIP 发现忽略文件 `AGENTS.md` 未入包；待当前长任务完成后补发并要求按规则复核，不在思考中途打断。
- 改动前门禁：工作区包构建退出 0；三段 TypeScript 检查退出 0。
- 改动前相关回归：16 个测试文件、185 项通过，退出 0。
- Pro 当前已从调用链检查进入“创建本地模型基础文件”阶段，仍在运行；未催促、未中断、尚无可下载交付。
- 本地补充核对路由安全、持久化、开放边界、CLI 闭包与壳表面门禁，列为后续补丁审查必查项。
- 用户已确认继续双代理协作。ChatGPT Pro 已完成“本地模型及实例管理”阶段，当前进入“本地模型下载与安全解压”阶段，仍在运行；尚未收到最终报告或下载产物。
- 外部长任务持续有进展：完成下载/注册/Sidecar 审查与类型/清单调整后，已进入改动状态复核；页面与账号连接正常，未催促或中断。
- 待补发的 `AGENTS.md` 为 21,337 字节，命中敏感词扫描的仅为普通技术词 `token`，文件本身由项目规则构成；仍需在首轮产物完成后补发并要求复核。
- 外部工程师已完成其环境的依赖安装阶段并继续修补：新增路由安全、语音服务适配和文字转语音路由后，开始搜索文档抽取真实调用点。其 Node 22 自检结果仍不计入本项目 Node 24 门禁。
- 复核本地 Git 状态：仍仅有 `docs/local-models-implementation/` 三份本任务记录，产品源码未改，分支与 HEAD 未漂移。
- ChatGPT Pro 在其类型检查阶段遇到一次页面级“出了点问题”错误；用户再次确认继续后，已对同一回复执行一次可恢复重试。恢复请求仍显示 `Pro 思考中`，账号、对话和已上传附件均正常；没有重复点击或中断，避免破坏其已有工作区。
- 恢复重试经过持续分段观察仍无任何可见工具步骤或文字；只读副本被站点“请稍候”保护页拦下并已立即关闭，原页未刷新。按用户预授权的故障恢复规则，停止卡住的生成过程，没有删除对话或外部工作区。
- 已补发安全检查过的仓库规则 `AGENTS.md`，并明确仓库原本没有 `CLAUDE.md`；要求 Pro 从其现有已修改工作区继续修类型错误、诚实区分 Node 22 与 Node 24 证据，并实际生成四份报告、完整源码 ZIP、补丁及摘要。新回复已进入 `Pro 思考中`。
- 用户指出需要重启浏览器。关闭唯一标签页后重新打开同一正式对话，账号和附件均恢复；重启后的服务端对话只保留最初任务，显示原始 Pro 回复仍在运行，先前页面错误、重试、补发与抢救追问均未持久化。由此确认此前主要是浏览器前端状态异常；后续只监控重启后的原始任务，不重复发送或停止。
- 重启后原始 Pro 任务最终完成（页面记录耗时 81 分 56 秒）。用户已下载五份 Markdown 报告；另从同一最终回复下载完整源码 ZIP 与补丁。
- 完整源码 ZIP：`/Users/study_superior/Downloads/LingxiAgent-local-models-implemented-3eab8589.zip`，52,572,908 字节，SHA-256 `9bf75cd637e20bd4616f786b6f4f8e07637510aa41de7725fcc8c03d5abf77b6`，ZIP 完整性检查通过。
- 补丁：`/Users/study_superior/Downloads/LingxiAgent-local-models-3eab8589.patch`，2,691,432 字节，SHA-256 `4356378be2d4a2c2396d221e8f7a4eac6f440587f5ef43349ae6b215a2491449`，`git apply --check` 通过；五份报告的摘要均与交付校验表一致。
- 补丁事实：101 个新增文件、20,960 行新增、0 个既有文件修改、0 个删除；除四份根目录报告和大量测试日志外，仅新增 `packages/local-models/`，没有接入现有服务组合根、权限表、引擎、知识/记忆、文档、语音、设置、五语言或打包配置。
- 外部报告内部自相矛盾：一处写明核心类型检查/构建/测试闭环为 False、测试未执行，另一处却把多条业务链和桌面/打包状态写成 True。验收以补丁和本机实测为准，首轮交付未通过。
- 在独立临时副本中复用当前工作区依赖做 Node 24 验证：包内 TypeScript 检查在下载器两处报 `TS2367`，退出 2；编译仍输出产物后，包内 20 项测试通过，但不能覆盖编译失败，也不能证明产品集成。
- 已向同一 ChatGPT Pro 对话发送返工意见，列出产品零接线、清单/模型身份错误、安全解压与符号链接风险、实例泄漏、sidecar 环境泄密风险、后端顺序、下载和测试证据等问题；要求提交纠正补丁及诚实状态说明。
- 当前产品源码仍未修改，未提交、未推送、未部署。下一步只筛选可用核心代码并逐项修正，不直接套用整包补丁。
- 补充改动前门禁：`npm run lint:boundary` 退出 0；对预期接线文件执行 ESLint 退出 0，但已有 151 条 warning（主要是既存 `no-explicit-any`），后续验收按“零新增错误/警告”比较，不把旧警告算成本任务回归。
- ChatGPT Pro 第二轮附件下载入口失效。经对话沙箱、本机 Downloads、附件缓存、废纸篓和系统索引复核，第二轮真实文件均无法恢复；没有生成首轮改名包或空占位冒充交付。
- 用户明确要求后续不再由 ChatGPT Pro 执行，改由 Codex 独立完成整个任务。已停止外部协作路线，重新回到当前提交 `3eab8589...` 做本地事实审计、实现、测试和交付。
- 复用并更新既有 `docs/local-models-implementation/` 三份任务记录；删除本轮误建的三份根目录重复记录，未触碰任何产品文件或旧任务文档。
- 重新搜索当前源码，确认不存在任何本地模型实现残留；Phase 2 从零建立。确认下载可复用 `undici` 逐 URL 代理 dispatcher，语音服务已有统一观测宿主，文档抽取可通过可选依赖注入扩展。
- Phase 2 第一小步：新增本地模型结果/调用/身份契约、结构化错误、默认配置归一化、八个目录条目和严格远端 manifest 解析器。目录条目不含伪造 URL/hash，只有通过 manifest 校验后才允许提升为可下载。
- 新增核心契约后运行 `npx tsc --noEmit -p tsconfig.node.json --pretty false`，退出 0。
- 新增 LargeSlot、MemoryGovernor 与 BackendProbe：大档按模型共享、跨模型排队、交互优先、Abort 清队列；内存按 1.25 系数与 small 总预算拒绝加载；内存压力按 large→small、LRU、零引用顺序驱逐；后端按平台要求逐项真实初始化验证。
- 定向测试 2 文件、17 项通过；Node 源码与测试 TypeScript 检查均退出 0。首次类型检查暴露冻结数组推导错误，已精确标注后复跑通过。
- 新增 InstanceManager：冷加载共享、独立等待者 Abort、最后等待者取消时中止底层加载、晚到实例确定性清理、引用计数、small/large TTL、可取消卸载、显式预热/立即卸载、RSS 回落事件与 dispose 合并。
- 生命周期定向验证累计 3 个文件、23 项通过；`tsconfig.node.json` 与 `tsconfig.test.json` 两段检查均退出 0。大档切换测试实测最大同时驻留计数为 1。
- 新增模型专用流式 ZIP 解压器：只接受 manifest 声明文件，拒绝路径逃逸、symlink、大小写重复、未声明文件、高压缩比、单文件/总量超限；逐块校验字节数与 CRC32，Abort 或失败会清理调用方专属 staging 目录。
- 安全解压测试 6/6 通过，Node 源码与测试类型检查均退出 0。首跑的高压缩普通夹具被安全阈值正确拒绝，改成随机字节后复验通过，炸弹样本仍保持拒绝。
- 新增断点续传下载器初版：强制 HTTPS、逐地址复用项目代理、下载前双倍磁盘余量检查、HEAD 探测范围请求、最多四片并发、分片状态持久化、流式合并与 SHA-256 双次校验、暂停/取消/中断状态和可选镜像重写。
- 下载器首轮源码类型检查退出 0；审阅发现并修正两个未进入测试的生命周期缺口：已完成产物必须复核哈希后直接复用，多个调用者共享同一任务时单个调用者中止不能误伤其余等待者。
- 下载器定向运行测试 6/6 通过，覆盖四片并发与代理透传、完成产物复用、共享等待者独立取消、暂停后按下一字节续传、单次哈希失败重试、双次失败清理、HTTPS 与磁盘余量拒绝。随后测试类型检查发现夹具把 Node 缓冲区直接作为浏览器响应体的声明不兼容，已改成标准字节数组，待复跑确认。
- 下载器夹具类型兼容修正后，6 项运行测试、Node 源码类型检查与测试类型检查全部退出 0。
- 新增严格安装注册表与手动导入底座：固定 `<分类>/<模型>@<量化>` 目录，严格 `model.json` 字段与身份匹配，逐文件普通文件/大小/哈希复核，拒绝符号链接、额外文件、大小写重复路径和目录逃逸；手动导入先复制到专属 staging 并复核，再原子改名，已安装目标不覆盖。源码类型检查退出 0，定向测试待补。
- 注册表定向测试 5/5 通过，覆盖原子导入与来源改写、哈希篡改清理、符号链接拒绝、已安装目录篡改隔离、禁止覆盖和精确删除；Node 源码与测试类型检查均退出 0。
- 侧车进程审计确认可参考现有跨平台进程树终止逻辑，但不能直接复用沙箱命令包装：本地模型侧车需要健康握手、会话令牌、受控环境变量、日志轮转、重启预算与“确认退出后才释放大模型槽位”的专用生命周期。
- 新增专用 SidecarManager：绝对路径启动、环境白名单、随机会话令牌与协议/运行时身份握手、每请求编号/超时/取消、16 MiB 请求和 1 MiB 协议行上限、启动失败最多重启一次、令牌脱敏轮转日志、优雅关闭后跨平台进程树强杀。源码类型检查退出 0；测试前进一步改为强杀后必须等到真实退出确认，否则显式失败。
- 侧车首轮定向测试 7 项中 6 项通过；“第一次失败、第二次启动成功”夹具因首次直接读取不存在的计数文件而自行退出，已修正为不存在时从 0 开始。其余握手/调用、环境隔离与日志脱敏、双次失败封闭、请求超时与取消、拒不退出时强杀确认、环境键拒绝均已通过。
- 侧车夹具修正后 7/7 定向测试通过，Node 源码与测试 TypeScript 检查全部退出 0。Phase 2 的生命周期、下载、解压、注册表和侧车底座已经具备真实合同测试；下一步补 manifest 拉取/缓存、远端安装协调与统一推理门面，再接服务端入口。
- 按原始需求补齐 manifest 客户端：仅 HTTPS、项目代理 dispatcher、ETag/304、5 MiB 流式上限、原子缓存；远端失败或新内容解析失败时保留并使用上次合法缓存，无合法缓存时回到八模型内置目录并带显式 warning，不伪造任何下载地址。
- 安装元数据已与需求目录约定对齐：`model.json` 现在必须持久化 `version`、`bytes`、`sha256Manifest`，并校验总字节数等于逐文件清单；注册表 5 项回归和两段类型检查继续通过。
- Manifest 客户端 5/5 定向测试通过，Node 源码与测试类型检查均退出 0。为远端安装增加受控注册入口，并收紧同一量化档每个平台最多一个资产包，防止选择歧义。
- 新增远端安装协调器：按当前平台选择模型与运行时资产，先校验/补齐共享运行时，再下载与流式解压模型；逐文件生成安装清单，携带远端 manifest 摘要，经注册表二次复制复核后才原子登记；成功后清理下载临时任务。当前 `zip` 可执行，`tar.zst` 只识别并显式报不支持，未冒充完成。源码类型检查退出 0，定向测试待补。
- 远端安装与注册表联合测试 9/9 通过，覆盖运行时先行、原子模型登记、完成后临时清理、重复调用复用、十路并发去重、共享运行时篡改拒绝和平台缺包零网络；两段类型检查退出 0。
- 新增统一 LocalModelRuntimeService，四能力共用已安装身份解析、实例申请/释放、TTL、大档槽位、内存治理和无内容观测。运行测试 3/3 通过；测试类型检查仅发现假后端字面量被推宽，已收紧夹具，待复跑。
- 统一运行门面测试夹具修正后，Node 源码与测试类型检查均退出 0。为真实运行时装载补齐持久身份：manifest/runtime 现在区分进程内与 sidecar、可声明平台入口文件；`model.json` 保存运行参数模板与能力元数据，供重启后加载，不依赖仅驻内存的远端 manifest。
- 新增 SidecarInstanceFactory：加载前复核 runtime.json 与入口文件哈希；后端候选逐个以真实启动握手验证并缓存；运行参数仅替换 modelDir/backend/threads/mmap/mlock 白名单占位符，直接传 argv；四种请求使用结构化 JSON 协议并严格校验返回形状，音频/图片只在本机协议内用 base64 传递。
- Sidecar 工厂 3/3 定向测试通过，Node 源码与测试类型检查均退出 0。BackendProbe 缓存键增加运行时身份，防止不同原生引擎错误复用探测结论。
- 新增进程内运行时工厂：只允许 STT/TTS，加载已校验运行时包的模块入口，优先调用模块的真实后端探测，否则用短生命周期会话验证；创建正式会话后由 InstanceManager 管理销毁和 RSS。该通道为 SenseVoice/Kokoro 的 sherpa 包装契约，不包含权重或伪造原生资产。
- 进程内工厂 3/3 定向测试通过，Node 源码与测试类型检查均退出 0；已验证后端逐项回退、正式会话创建/销毁、RSS 和不同运行时探测缓存隔离。
- `model.json` 与注册表新增持久化 runtimeKind，统一复合工厂按明确的 in-process/sidecar 身份选路，卸载和 RSS 也沿原工厂返回；不再根据模型名称临时猜测。
- 本地模型专项回归当前累计 12 个测试文件、65 项全部通过；Node 源码与测试 TypeScript 检查均退出 0。
- 服务端接线初查确认应用在 `server/index.ts` 创建唯一 Engine，并通过 open/full composition root 注册路由；本地模型属于产品闭集能力，应挂 full root，同时在 route-security 明确登记 local-owner 权限，不能依赖未知 `/api/*` 的默认策略。
- 进一步读取组合根职责后修正上一条：本地模型不含闭源产品内容，应挂开放组合根，使 full/open 启动入口一致可用；仍需在 route-security 明确限制为 local-owner，不允许远程 studio 或插件表面管理本机模型文件与进程。
- 路由注册发生在 Engine 完成初始化、代理运行时已注入之后，因此本地模型子系统可在同一启动段创建并挂到 Engine，再由 open-root 路由读取；这样 manifest/资产下载会复用已经生效的网络代理配置。
- Engine 已暴露统一 preferences 读写与全局事件发送边界；本地模型配置将存入 preferences 的独立 `localModels` 字段，事件只发送状态/进度/后端/内存等元数据，不发送文本、图片、音频或本机绝对路径。
- 新增 LocalModelsSubsystem：统一持有目录、缓存、下载、安装、注册、运行时与配置；状态响应去掉下载 URL/hash、安装绝对路径和文件名；配置变更写回现有 preferences，活动下载时拒绝热换管理器；无已验证 manifest 时安装请求明确失败，不触网。
- 子系统定向测试 2/2 通过，覆盖空资产启动、八项诚实目录、配置归一与持久化、手动导入、脱敏状态/事件、卸载后删除，以及无 manifest 零下载；Node 源码与测试类型检查均退出 0。
- 服务器退出链已定位：停止新请求后、Hub/Engine 清理前是本地模型确定性卸载窗口；将在这里等待 runtime.dispose，确保 sidecar 和进程内会话先退出，再关闭其余宿主资源。
- 本地模型管理路由将同时使用全局 route-security 的显式 LOCAL_ONLY 分类与路由内 local-owner 复核；读状态也不开放给 Studio Owner，因为其中包含本机模型、下载和进程状态。
- 新增 `/api/local-models` 本机管理路由：状态、配置、manifest 刷新、安装、手动导入、暂停/取消下载和卸载删除；所有输入做固定类别/安全 id/任务 id 校验，返回不含绝对路径，变更写安全审计。open-root 已挂载，route-security 显式 LOCAL_ONLY，路由内再次核验 local-owner。
- 服务器在代理配置生效后初始化 LocalModelsSubsystem 并挂到 Engine；优雅退出时在 Hub/Engine 清理前等待本地运行时全部释放。服务端源码类型检查退出 0，路由合同测试待补。
- 本地模型路由与 HTTP 权限运行测试合计 32/32 通过：本机所有者可读、远程 Studio Owner 仍拒绝、非法模型/任务 id 不下发、结构化冲突映射正确、八个管理端点均显式 LOCAL_ONLY。测试类型检查仅发现 Hono 夹具未声明 authPrincipal 自定义字段，已按仓库既有测试做局部桥接，待复跑。
- 路由夹具修正后两段类型检查退出 0。首次 `npm run lint:boundary` 退出 1，准确报告 open-root→新路由、server/index→新 lib 两条“未分类”边；未把它们塞进基线债务，下一步按项目规则将本地模型生产文件登记为开放导出并重算 CLI 运行闭包。
- 首次把 `lib/local-models/` 作为目录加入 export manifest 后门禁仍失败，因为目录项按设计只展开 Git 已跟踪文件，而本任务尚未提交且用户禁止提交。已改为逐文件精确白名单，保持未提交状态也能由边界门禁审计；没有修改基线债务。
- 逐文件开放白名单后 `lint:boundary` 退出 0。首次重算 CLI 闭包被两条未登记动态调用阻断：已验证运行时 sidecar 的 `spawn(executable)` 与进程内模块动态 import；已按“外部下载资产、非仓库源码边”写入精确调用点白名单，待重算。
- 动态边界登记后 CLI 运行闭包重算成功：10,677 个文件（source graph 777、runtime asset 11、依赖跟踪 9,889），开放边界基线仍为原有 1 条 evidence-needed，没有新增 provisional 或债务。
- 持久化盘点确认至少需登记四类独立所有权：模型安装目录、可恢复下载/安装 staging、manifest 缓存、平台运行时目录；本地模型日志由既有日志树是否覆盖仍待扫描验证。模型权重和手动导入资产不能随数据 epoch 复制成巨型隐式备份，需在登记中明确恢复策略。
- 首次运行持久化扫描在下载任务目录创建点退出 1，证明新写入点尚未被任何 store descriptor 接管；未加宽全局豁免。计划按真实恢复语义登记：模型/运行时资产不进入 epoch 巨型快照，下载分片可续传或取消，manifest 缓存可重拉，sidecar 日志只作诊断轮转。
- 新增 4 个持久化 store descriptor：模型+运行时资产、断点下载、manifest 缓存、sidecar 轮转日志；逐模块限定允许的写入种类，并明确 epoch/checkpoint/恢复策略。重跑扫描成功：70 个 stores、808 个写入/打开 sites，生成 inventory 与 startup receipt。
- 类型检查发现 sidecar 日志的初始格式名不属于仓库枚举，已改用既有 `append-only-log`，随后重新生成 inventory、startup receipt 和 schema fingerprint；最终指纹为 `sha256:7a8e56a42ba57bd3f35b52c9c0b7e81069055b57a5f31789133e978ca4c379ff`。DATA_EPOCH 未提升，兼容理由明确限定为四类隔离的新存储。
- 持久化专项 4 文件、77 项通过；Node 源码与测试 TypeScript 检查均退出 0；开放边界门禁继续退出 0，仍只有仓库既有 1 条受控债务。
- Phase 3 首批业务接线完成：稳定 `local:<id>@<quant>@<manifestVersion>` 身份可解析；知识摄入/查询在 Engine 共用嵌入边界直接调用本地运行时并把精确身份传给向量变体，云端解析路径保持原样；记忆查询与事实回填复用同一本地嵌入通道。
- 新增 `local-offline-asr` 离线语音适配器并注册到既有语音服务。本地已安装 STT 会进入原模型列表，转写继续复用会话文件状态、语义观测和用量外壳，但不解析云端凭据、不调用 fetch；旧的同名测试供应商兼容路径保留。
- 嵌入/语音业务接线与知识/记忆回归共 6 文件、48 项通过；Node 源码和测试 TypeScript 检查均退出 0。首轮测试发现旧 payload 套件默认把所有 adapter 当 HTTP adapter，现已明确只对远端 adapter 跑四层 HTTP 捕获，本地零网络由独立合同覆盖。
- OCR 接线前核对确认：Office PDF 阅读器已用 `unpdf` 提取文字，而同一依赖原生提供逐页渲染接口；Node 渲染明确要求 `@napi-rs/canvas`。已把仓库锁中已有版本 `0.1.69` 提升为根 optionalDependency，保持三平台原生包可选分发，不引入 Python；后续会以页数和单页像素双上限调用。
- 本地配置契约新增 OCR 默认模型/页数/像素上限，以及 TTS 默认模型/音色字段；缺省仍不选择任何模型，不会启动或预热。本地子系统新增经过已安装注册表复核的 OCR 调用门面，非法/未安装身份显式报错。
- OCR 已接入通用文档抽取与内置 Office 阅读工具：png/jpg/webp/tiff 直接识别；普通文档仍走原转换器；扫描 PDF 才逐页渲染后识别；Office 透传启用开关、单次模型、语言、页数和像素上限。成功结果带 `ocr:<modelId>` warning，缺模型/渲染失败返回显式 `ocr-unavailable`。
- 首版 PDF 渲染实测发现 Node Buffer 与标准 Uint8Array 差异、Node legacy 构建要求，以及旧 `unpdf`/新安全版 PDF 引擎的 canvas 类型不兼容。已升级到当前 `unpdf@1.8.1`、`pdfjs-dist@6.3.289`、`@napi-rs/canvas@1.0.8`；高危 PDF.js 审计项随之消失，审计回到仓库既有 1 low + 7 moderate。
- OCR 集成 4 项通过，其中真实 `sample-scanned.pdf` 经实际 PDF 页面渲染成有界 PNG 后送入假 OCR（渲染约 607ms）；另有普通文档/File 工具/Office 回归 23 项通过。真实 OCR 权重推理仍为 `NOT_EXECUTED`。
- 新增对称的 TextToSpeechService 与 `local-offline-tts` 适配器：从已安装 TTS 注册表列模型/音色，严格限制 20,000 字输入与 64 MiB 输出，支持 Abort/分块回调，调用本地运行时并写入同一模型观测外壳；缺模型不自动转云端。
- 新增 `/api/media/tts/providers` 与 `/api/media/tts/synthesize`，后者用显式 base64 编码返回本地音频；统一媒体 providers 入口支持 speech_generation/speech_synthesis/tts 别名，HTTP 权限表把读取与提交分别归入既有媒体读/写策略。
- TTS 服务、媒体路由与权限回归共 3 文件、40 项通过；Node 源码和测试 TypeScript 检查均退出 0。真实 Kokoro/IndexTTS 音频仍为 `NOT_EXECUTED`，桌面播放/暂停/流式句级队列尚待接线。
- 恢复桌面设置页工作时确认上一轮五语言补丁未落盘；已重新以各语言 `settings.localModels` 独立命名空间补齐管理页所需文案，下一步先做 JSON/类型验证，再补组件测试。
- 五语言 JSON 解析与项目三段 TypeScript 检查均退出 0。继续核对时发现首版安装按钮会一直等待同步安装响应，期间组件不刷新，因而用户看不到任务 id，也无法真正点暂停/取消；下载器本身已有实时速率/剩余时间事件和断点状态，但管理页尚未消费，需先修正这条交互链再写测试。
- 下载实时指标已从运行中的下载器安全并入本机状态响应；设置页安装期间每 500ms 刷新，展示单项/全局速率、剩余时间和字节进度，暂停/取消不再被长安装请求禁用，暂停后再次点击同模型会从持久分片继续。新增镜像地址入口并修复 GB 容量显示。
- 管理页、父模型页、子系统与路由目标组共 4 文件、18 项通过；项目三段 TypeScript 检查退出 0。测试覆盖八个稳定目录、目录占位资产禁下载、实时指标、暂停/取消、配置保存与删除二次确认。
- 助手消息页脚新增本地“朗读”动作：提取消息纯文本后按句、每段最多 600 字顺序合成，优先播放第一句；同一时间只允许一条消息播放，并支持暂停、继续、停止、请求中止、组件卸载清理与 Object URL 回收。`pcm_s16le` 会在前端封装为单声道 WAV 后播放，原生 WAV 直接播放。
- 朗读/TTS 服务/媒体路由目标组共 3 文件、18 项通过；三段 TypeScript 检查退出 0。真实离线模型音频仍为 `NOT_EXECUTED`，流式回复尚未在文字到达过程中自动排句，当前是回复完成后用户触发的逐句低首句延迟播放。
- Bridge 入站音频在写入受管会话文件缓存并获得稳定会话身份后、送入主模型前调用同一语音识别服务；仅在用户显式启用且配置默认模型时执行，成功以 `[语音转写]` 追加文本，失败只追加人话提示并保留原附件。飞书/QQ 原始音频可走此链，微信未下发原音频时继续使用平台已有转文字。
- Bridge 会话与本地语音目标组共 2 文件、48 项通过；三段 TypeScript 检查退出 0。真实 Bridge 平台+真实本地权重的离线转写仍为 `NOT_EXECUTED`。
- 本地 TTS 配置新增默认关闭的 Bridge 语音回复开关。开启且平台声明支持内存音频上传时，文字回复始终先送达，再附加本地 WAV；PCM 输出会封装为 WAV，大小受平台上限约束。合成或上传失败保持已送达文本并只写脱敏诊断；QQ 等拒绝裸内存的平台不创建临时文件绕过归属边界，直接保持文本。
- Bridge 入站/出站、TTS 与设置目标组共 4 文件、107 项通过；修正测试替身签名后项目三段 TypeScript 检查退出 0。真实平台语音发送仍为 `NOT_EXECUTED`。
## 2026-09-02 运行时配置语义

- 已核查：内存预算、空闲回收时间、预热开关当前均未在保存配置后真实作用于运行时。
- 正在修复：运行时热更新预算与回收时间，并为已安装的小模型补上有界、可取消的预热入口。
- 已完成：内存预算会立即约束后续加载；空闲时间变化会重新排程已闲置实例；加载方式变化会卸载闲置实例，但不强退正在使用的实例。
- 已完成：小模型预热改为后台逐个执行，可取消、单项失败隔离，并在启动、启用设置和新导入小模型后触发。
- 验证：运行时配置目标组 3 个文件、20 项通过；项目三段 TypeScript 检查通过。
- 设置页增强进行中：固定打开模型目录、目录拖放导入、大小模型空闲时间、线程/mmap/mlock、内存预留、大模型槽位、实际运行后端、被拒绝目录和向量数据保留说明已接线，待五语言与测试验证。
- 设置页增强已完成五语言和目标验证：语言包 JSON、桌面主进程语法检查通过；设置页与运行时配置 4 个文件、24 项通过；项目三段 TypeScript 检查通过。
- 无 `model.json` 手动导入进行中：注册表已能安全识别 ONNX/GGUF/Transformers 文件形态并流式生成逐文件摘要；服务端新增只读检查与候选确认两步；候选契约只取已验证 manifest，且导入前强制复核本机对应运行时包。
- 无元数据手动导入已完成目标验证：注册表、管理路由、本机权限和设置页 4 个文件、45 项通过；五语言 JSON 与项目三段 TypeScript 检查通过。源目录未被写入，生成元数据只出现在受管安装副本中。
- 生产打包合同第一步完成：独立 server 构建在包含原生画布时会用目标 Node 真创建画布并编码 PNG；Electron 收集规则测试锁定不包含 `models` 或 `runtime/local-inference`。构建合同 2 个文件、19 项通过，类型检查通过。
- 零外网代码边界和基准工具初版已加入：推理编排源禁止网络客户端，sidecar 固定为随机令牌 stdio；端到端清单覆盖管理、四业务链、生命周期、断网和四平台打包。安全/构建目标组 3 文件、21 项通过。
- 基准工具首次启动失败：Node 的 strip-only TypeScript 不支持依赖模块里的参数属性语法；该次没有运行任何模型，需换用仓库已有的完整 TypeScript 执行入口后复验。
- 基准工具已改用仓库现有 `jiti` 完整 TypeScript 执行器；未设置 `LINGXI_HOME` 时退出码 2，并只输出明确的人话提示，不猜测或创建真实用户目录。
- 许可证闭环进行中：远端资产许可证文件名进入安装元数据，注册表提供 1 MiB 有界纯文本读取，管理路由与设置页提供查看；手动包未声明许可证时按钮明确不可用。首轮测试类型检查暴露旧测试替身兼容问题，已收窄修正，待复验。
- 许可证闭环已完成目标验证：许可证、路由、本机权限、安装和设置页 5 个文件、52 项通过；五语言 JSON 与项目三段 TypeScript 检查通过。发布级真实资产许可证复核仍为 `NOT_EXECUTED`。
- 基准工具用独立空 `LINGXI_HOME` 实跑成功，四能力均输出 `NOT_EXECUTED` 而非虚假耗时；本次仅验证工具状态语义，没有真实模型性能结果。
- `git diff --check` 退出 0；生成物仍是业务接线前版本，下一步重算运行闭包和持久化盘点。
- 第一次重算在持久化盘点首步被正确阻断：桌面固定打开模型目录会创建 `models` 根目录，该写点尚未归属；后续指纹与运行闭包没有执行。
- 将桌面固定模型根目录创建点归入既有 `local-model-artifacts` 后，生成链通过：70 个 stores、809 个 sites；持久化指纹 `sha256:1f76b6eca57ea340d933b79d74933e71fdf2991fa1885044cdb9f7df99b99d05`；CLI 运行闭包 10,686 文件（source 779、runtime asset 11、依赖 9,896），开放边界仍为原有 1 条受控证据项、0 provisional。
- 总门禁第一次在 `lint:boundary` 首步退出：新语音合成服务尚未进入开放导出清单；后续测试未执行，未误记为通过。
- 语音合成服务与适配器精确加入开放导出清单并重算闭包；`lint:boundary` 通过，仍只有原有 1 条受控债务。
- 持久化、CLI 闭包、本地模型全部模块、朗读、Bridge、媒体和模型选择目标组共 29 个文件、334 项通过，耗时 68.31 秒。
- 仓库三段 TypeScript 检查通过。全量 ESLint 首跑退出 1，共 19 个 error/9,193 个 warning；18 个 error 属于新测试夹具，1 个在未修改的 `lib/security/injection-scan.ts`，需修正前者并保留后者为基线噪声。
- 本任务范围 ESLint 退出 0。全量 Vitest 首跑：1,293 文件中 1,286 通过、1 跳过；13,041 项中 13,014 通过、7 跳过、20 失败。17 项为同一个 Node 直载语法根因，另有 3 个明确合同差距，正在修复，当前全量状态仍为失败。
- 四个失败根因已修复：复合工厂改为 Node 直载可解析语法；组合路由黄金清单加入本地模型路由；远端语音观察测试不再把零网络本地 adapter 当 HTTP adapter；设置页间距/颜色全部改用现有样式令牌。
- 原 6 个失败测试文件单独复跑 47/47 通过，三段 TypeScript 检查再次通过；需重跑全量确认没有方差回归。
- 全量 Vitest 第二轮通过：1,292 个文件通过、1 跳过；13,033 项通过、7 跳过；耗时 83.96 秒。仓库级测试状态已由失败提升为通过。
- 全量构建首轮：packages、桌面主进程、预加载、渲染器、启动页和主题均通过；server 构建在解析 `@napi-rs/canvas` 的 macOS 原生绑定时退出 1。根因是该依赖有目标 Node smoke，却漏进 Vite 外置依赖清单；已按同类原生模块边界补齐，待重跑验证。
- server 构建第二轮已完成源码 bundle，但独立依赖安装后正确报告 `@napi-rs/canvas` 缺失并退出 1；收集器只读普通依赖、未读根清单可选依赖。已修为两类根依赖合并派生，目标 server 仍把选中的画布钉为必需项，待第三轮验证。
- server 构建第三轮已通过源码 bundle、依赖安装、裁剪以及四项目标 Node 原生 smoke（数据库、分词、文档转换、画布）；最后在仓库强制种子签名门禁因未设置密钥退出 1。这不是功能或原生依赖失败；下一轮按脚本说明使用工作树外的一次性测试密钥验证签名与种子，不接触发布密钥。
- 使用 `/tmp` 一次性 Ed25519 测试密钥完成 server 第四轮：四项原生 smoke 再次通过，12 个 Mach-O 文件完成本地 ad-hoc 签名，server/renderer 种子包与清单签名生成；`verify:seed-kit` 退出 0。临时私钥、公钥和 keyset 已逐文件删除，未写入工作树，也未接触正式发布密钥。
- 完整 `npm run pack` 已走完 helper、客户端、server、种子复核和 Electron 应用目录生成，本地 ad-hoc 重签也通过；末尾因传入 `SKIP_NOTARIZE=1` 而脚本只接受字面值 `true`，仍尝试 Apple 公证并退出 1。应用目录存在但整条命令仍记失败；下一步用正确开关只重跑 Electron 目录打包。
- 使用正确的 `SKIP_NOTARIZE=true` 单独重跑 Electron 目录打包退出 0：`dist/mac-arm64/Lingxi.app` 生成，种子资源完整性检查、全应用 ad-hoc 重签和签名复核通过；Apple Developer ID 正式签名与公证未执行。
- 最终产物核查：应用目录和 server 种子归档的模型权重扩展名命中均为 0，八模型受管目录与 `runtime/local-inference` 命中均为 0；画布包及 `skia.darwin-arm64.node` 在种子中存在，应用 `codesign --verify --deep --strict` 退出 0。
- 最终门禁复核：三段 TypeScript、持久化指纹、`git diff --check` 和开放边界均通过；全仓 ESLint `--quiet` 只剩未修改 `lib/security/injection-scan.ts:24:31` 的 1 条既有错误；生产依赖审计为 7 个 moderate 传递依赖，强修会造成破坏性版本降级。
- 基准权威入口已对齐 `scripts/bench/local-models.mts`，空临时 home 实跑退出 0，四类均诚实输出 `NOT_EXECUTED`。
- 四份最终报告已写入独立任务目录：实施、测试、源码审计、剩余工作；下一步生成安全源码包和校验和报告。
- 安全源码包首轮扫描命中 2 个测试文件，逐行确认均为脱敏测试专用的公开假私钥头/GitHub/AWS 示例；无未解释命中。源码包 3,318 个文件，ZIP 完整性通过；最终收口后重打包并以校验和报告中的摘要为准。
- Phase 5 本机范围完成：代码与报告保留在当前未提交工作区；未提交、未推送、未部署、未迁移数据。真实模型、正式许可证、其他平台、性能与发布链均保留为 `NOT_EXECUTED`。
- 最终全量测试复跑首轮出现 1 个新失败：1,291 文件通过、1 跳过，13,032 项通过、7 跳过；失败是质量合同要求 `@napi-rs/canvas` 属于普通生产依赖。已将画布从根可选依赖移到生产依赖，并撤回收集器的双重依赖来源；待目标与全量复验，当前不能沿用此前全量 PASS。
- 质量合同、构建依赖和启动合同目标组 3 文件、39 项通过，三段 TypeScript 再次通过；最终全量 Vitest 复跑通过：1,292 文件通过、1 跳过，13,033 项通过、7 跳过，耗时 82.65 秒。
- 用户授权 Phase 6：允许必要资源下载且无流量限制，四条真实能力全部在本阶段范围内。本机 Node v24.16.0、96 GiB 内存、28 核、691 GiB 可用磁盘；Git 坐标与上一阶段一致，保留全部已有改动。技能恢复脚本再次返回无关 Claude 配置历史，不读取该配置。首个计划补丁因错误锚点未应用，已改用已知段落修正。
- 官方资产下载已启动：SenseVoice int8、Kokoro 多语言 fp32、Qwen3 嵌入 Q8、GLM-OCR f16（待真实转换 q4）及视觉投影。llama.cpp v0.3.0 官方发布指向 b10621，macOS arm64 运行包官方 SHA256 为 `429c8270608600188035e5e92f7d78dffb7900904fe7dd7e6a84f48068cd13cf`。原生 sherpa 1.13.7 安装到隔离目录，未改根依赖。
- 实际运行时适配涉及用户输入与进程边界，已完整读取 security-review 技能，将输入上限、取消与进程清理列为本阶段验证项。
- 已新增随运行包安装的原生适配源码：语音使用进程内工作线程，嵌入/识字使用标准输入协议桥接仅本机回环、随机令牌鉴权、强制离线的 llama.cpp。尚未真实模型验收，不把这些源码写成运行成功。macOS 进程级断网工具可用，可在不影响整机联网的情况下验证离线推理。
- SenseVoice、Kokoro 两份官方压缩包的完整 SHA256 已核验通过；隔离注册表可安全导入 SenseVoice（约 240.5 MB，manual 身份）。平台原生包的 npm 显式安装遭遇连接重置，改用官方 registry 精确 tarball 下载并对照发布完整性值，未改换未知来源。
- sherpa macOS 原生包官方 SHA512 校验通过，Node 24 实际加载返回 sherpa 1.13.7 / ONNX Runtime 1.28.1；Kokoro 已导入隔离目录。Qwen 嵌入完整文件与官方 SHA256 一致。首轮断网测试在模型调用前被系统沙箱阻止创建监测子进程，属于沙箱策略配置失败，不记模型失败或离线通过；正在收窄为禁止外网但允许本机进程通信。
- 语音两类已通过真实模型输出，且在系统禁止外网的进程沙箱中再次成功；外网负对照返回 EPERM。发现卸载后 RSS 不及时回落、Kokoro 热合成约 1.2 秒/4.2 秒音频，不能宣称性能或完整回收达标。嵌入首次运行因原生进程启动健康检查未通过而失败，正在核对实际启动耗时与日志。
- Qwen3 Q8 嵌入已真实成功：1024 维，同义句相似度 0.8005、无关句 0.1350；Metal 首次调用 3268 ms、热调用约 34 ms，卸载后进程树从峰值 1553 MiB 回到 271 MiB。原生进程约 1188 MiB，因此本机 q8 按 large 登记，不能沿用目录默认 small。类型检查与新增适配器代码规范已通过。
- 修复真实进程退出发现的启动计时器残留：健康检查成功后仍有 60 秒计时器拖住宿主退出，现已清除并增加真实子进程自然退出回归测试。相关 4 文件、17 项通过。
- Qwen 在禁止外网、只允许本机通信的系统沙箱中通过，自动模式实际选 Metal；内存缺测明确省略而不误记零。完整知识业务管线也通过：Markdown/TXT/CSV 导入、解析、向量入库（3 个 ready 变体，均 1024 维）及三问检索全部成功，包括中文查英文远程办公政策。第二精度真实权重切换仍未执行。
- 生命周期实测发现关键故障：SenseVoice 约 56 秒音频取消等待 902 ms，随后业务服务仍可用；但六轮加载/卸载 RSS 继续上升，不能算物理回收通过。Kokoro 长句生成期间强制终止工作线程触发原生 Napi 异常，宿主退出 134；必须改为原生协作取消和等待安全退出，不保留会崩溃的取消实现。
- 原生取消方式修复后，Kokoro 六轮加载、长句取消和取消后再次合成均无崩溃；取消耗时 1569.6 ms，明确未达到 <1 秒要求。当前采用安全协作取消，不通过提前返回或假卸载来伪造达标。新增静态安全边界测试只守实现约束，不代替真实模型验收。
- 本轮全量测试首跑：1,292 文件通过、1 文件失败、1 跳过；13,033 项通过、4 项失败、7 跳过，83.50 秒。四项均为修改旁路日志所属源码后持久化源码指纹未更新；无数据库结构变更，正在按 compatible 说明重算，不将该轮写成通过。
- GLM-OCR f16 文件已完整到达且与官方摘要一致，正在用固定版本 llama.cpp 真正量化为 Q4_K_M；视觉投影还在下载。资源均来自公开官方仓库；SenseVoice 许可证为 FunASR 自定义模型许可，不能标作 Apache 或默认允许发布。
- OCR 首轮真实推理与图片/扫描 PDF 业务路由均返回结果，但视觉检查发现测试图片使用未正确加载的中文字体，部分字在输入中本来就是方框；仓库扫描 PDF 夹具也只有色块，不能据此宣称中文识别准确。正在换显式中文字体，并构造有真实文字图像、无文字层的扫描 PDF 后重验。
- 源码指纹已按无持久化格式变化的 compatible 理由更新到 `sha256:bab3e4c4166c894edc8469596611720987bd5276f843044071a19f12350b81c6`；70 stores/809 sites、CLI 闭包 10686 文件不变。指纹和新增适配器目标组 2 文件、17 项通过；需最终全量复跑。
- 中文图片已显式加载系统黑体；新扫描 PDF 经提取确认没有文字层，并渲染回图片核对五行中英文均完整。使用 PDF 技能只生成验证夹具，Python 不进入产品运行时。GLM 上游代码库 LICENSE 为 Apache，与模型卡的 MIT 权重声明不同；隔离模型不再把代码许可证冒充权重许可证，发布许可闭环仍保留。
- 正确夹具的 OCR 实测与断网复测均通过：图片和扫描 PDF 五行文字完整命中；Metal 首次端到端 1855 ms，同图缓存热调用 195/183 ms，扫描页推理 3257 ms。独立 100 ms 进程树采样峰值 1938 MiB，释放后 305 MiB。纯 CPU 对照正在执行，已见视觉计算缓冲超过 4.6 GiB、进程超过 8 GiB，不能沿用 Metal 结果宣称 CPU 性能达标。
- OCR 强制 CPU 完整输出通过，但性能未通过：首次图片 22.3 秒，新扫描页 110.5 秒，进程树峰值 9275 MiB（完成时原生进程 8750 MiB），释放后 536 MiB；此轮 120 秒请求上限内完成，没有冒充性能合格。随后全局进程检查未见本实验 llama-server 残留。
- 实际嵌入/识字并发十轮通过，40 ms 采样最大大模型原生进程数 1、活动大模型记录数 1，排队可见且排队取消后未观测加载，释放后进程 0。隔离测试使用大模型空闲 100 ms、小模型预算 4096 MiB，不能冒充默认配置验收。四能力同时调用通过，合成 WAV 再转写得到原句。开始最后全量回归。
- 最后全量回归退出 0：13037 通过、7 跳过、0 失败；1293 文件有通过用例、1 个 Windows 手工文件全跳过，报告时间跨度约 84.33 秒。三段类型检查、范围 ESLint（0 错误/7 警告）、开放边界与差异格式均通过。最终未见本实验原生进程残留。
- Phase 6 首轮真实验证收口：新增真实报告与 130 份原始文件摘要索引，旧报告加历史快照说明；四类均执行、失败项独立保留。模型和测试数据约 7.9 GiB 留在 Downloads 隔离目录，未进入仓库或旧安装包。没有提交、推送、发布或修改日常数据。下一工作应按真实失败项修复，而不是把源码全绿误作完整产品验收。
- 用户授权 Phase 7 修复上述实测问题。继续使用既有隔离目录和未提交工作区；技能恢复仍只返回无关外部配置历史，不读取该配置。首次查找上游包装文件用了猜测文件名而失败，已用真实目录清单定位为 non-streaming-asr/tts.js；未重复错误命令。
- 识字第一次修复真实复测：开启省内存计算并禁用跨请求缓存后，进程树峰值 9275→2852 MiB，扫描页 110.5→55.9 秒，内容仍完整；速度尚未达标。进一步只移除 PDF 外围完全纯白区域，保留所有非白像素、原始字号与 16 像素边距，不压缩/缩放输入；新增浅色文字与页边内容保护测试。
- 关闭语音工作内存池后，六轮识别宿主驻留由不断上涨改善到约 675 MiB 平台，但仍不回到基线。上游终结器确实释放对象，正在用隔离原生探针验证 macOS 分配器缓存是否滞留，尚未把探针作为产品依赖。
- 精确保留全部非白像素后，CPU 扫描页识字降至 2352 ms，进程树峰值 2455 MiB，五项内容完整；裁剪目标测试 2 文件 9 项通过。系统分配器压力释放探针返回 0，主动垃圾回收实验也未让语音 RSS 回落，不采用这些无效探针为产品修复；继续核对原生后端持久缓存与内存分区。
- 新取消检查点实测：8 秒上限且优先停顿切分的转写，约 56 秒输入取消 32.66 ms；朗读完整保留原文、最多 12 字短段，取消 608.87 ms；两者随后复用通过。新增原生协作错误归一化，取消不再误记为运行故障，且单测证明必须等待原生结束才释放引用。
- macOS vmmap 独立检查发现 RSS 包含大量已可回收空页，不能全部视为物理泄漏；STT 最终 footprint 133.6 MiB 对基线 135.5 MiB，但 TTS 六轮后 footprint 501.3 MiB 对基线 155.4 MiB，仍未回收。已向用户异步请求允许把语音改为受控独立进程；在获准前保留原“进程内”边界，不发布或冒充完全修好。
- Phase 7 本轮回归完成：13049 通过、7 跳过、0 失败；1295 个文件有执行通过、1 个 Windows 文件全跳过，JSON 时间跨度 78.40 秒。三段类型检查、范围 ESLint（0 错误/3 警告）、开放边界与差异格式通过；CLI 闭包 10687 文件，指纹不变。新增修复报告及独立 170 文件证据索引，不覆盖 Phase 6 原始失败证据。
- 实验辅助原生探针未进入应用/受管运行包，未采用强制 GC 或分配器压力回收作为无效产品补丁。取消及 CPU 识字修复已完成，但原语音驻留回收指标仍未通过；任务停在需要变更原硬要求的用户确认点，未擅自换架构，未提交/推送/发布。
