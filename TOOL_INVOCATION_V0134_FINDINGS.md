# 契约执行路径修复 v0.1.34 迁移事实

## 已确认基线偏移

- 原固定基线：`4fefe66ec3b4f6b23c78a09869a607886585740e`。
- 正式 v0.1.34 提交：`60d910b84572c525a7c9c49216fb9206623bf7a4`。
- 签名标签对象：`8c2e80e7e00b993a260a3e9273a85be1678c3b94`；不能代替源码提交。
- v0.1.34 合并时间早于原任务 P0 执行时间约 80 分钟，原基线在执行开始时已经过期。
- 原基线到 v0.1.34 共改动 303 个文件；与原任务 106 个改动文件重叠 18 个；三方现场预演有 11 个真实冲突文件。
- P12 的版本领先和旧封印失败都是基线偏移的直接症状。

## 迁移原则

- 不删除原分支，不把旧提交当垃圾；它是实现和红灯证据来源。
- 新分支基于正式发布树，迁移后重新生成统计、指纹、运行闭包和审计坐标。
- v0.1.34 已重构知识链，旧 P8 只能按语义迁移，不能覆盖回旧结构。
- 先前 10 个冲突的口头统计漏掉了生产 legacy 文件被搬为测试夹具形成的重命名冲突；已用现场 `merge-tree` 结果更正为 11 个。

## P0-01 新基线事实

- v0.1.34 上任务书指定的 11 个测试文件原样通过，共 `253` 项。
- 全量套件的唯一失败不是 v0.1.34 产品回归，而是 P0 记录位于旧已验证源码坐标之后；封印仍按设计关闭通道。
- 正式发布提交相对旧已验证源码只变化六份既有审计文件，均在发布时 allowlist 内；新增的五份任务记录是当前差集。
- 服务端构建在没有签名私钥时拒绝，使用一次性匹配密钥可完整通过，说明 P0 原始构建失败属于环境输入缺失。

## P0-02 迁移分界

- P1–P2 和 P6–P7 的实现路径与 v0.1.34 无直接重叠，可原样迁移后复测。
- P3–P5 的契约主体可复用，但引擎工具装配发生过发布差异，必须逐连接点适配。
- P8 仍缺完整重排策略与缓存身份，但正式版已经删除生产 legacy 入口；只在正式搜索链抽取共享语义，历史实现留在测试夹具。
- P9–P10 的实现和测试主体可复用；运行闭包、持久化指纹等生成物必须从新树重新生成。
- P11 的说明可参考旧稿，所有数量、状态和提交坐标必须重新生成。

## P4 Bridge 适配事实

- P4-01 的目录与桥接文件可按原哈希迁移；引擎只移植目录条目构造片段，v0.1.34 其它区域保持不动。
- P4-02 红灯在 v0.1.34 基线上暴露 `20` 项失败，比旧分支记录多；校正版保留本次原始数字，不复制旧证据。
- Bridge 现只接收目录、统一执行入口和日志；权限解析、完整参数校验、执行与能力归属复核均由规范目标链负责。
- P4-03 后，连接器工具的展示、目录登记、直接调用和延迟调用都读取同一实时可用性结论；富结果和来源信息不再经过第二套转换。
- 引擎和桥接层不再直接调用底层连接器；仓库剩余调用点只位于连接器适配层与协议客户端。

## P7 媒体执行目标事实

- 图片、视频、语音识别和后台图片任务已统一在真正执行前解析运行供应商与凭证供应商。
- 下游适配器只消费解析结果；旧的统一错误压平短语已从媒体执行边界清除，多凭证通道失败后不再暗中改试另一通道。

## P8 正式知识链适配事实

- v0.1.34 的核心引擎已经没有旧的编译范围兼容检索分支，生产 legacy 知识注入器也已退役；恢复它们会倒退正式架构，因此本项只迁移共同语义。
- 正式快速管线仍只做本地全文检索，并显式携带禁用重排的完整策略；详细知识搜索工具按混合/本地通道显式选择启用或禁用策略。
- 编译范围重排和查询核心重排现在进入同一个执行器，统一处理门控阈值、期限、输入上限、失败降级与主动跳过留痕。
- 结果缓存不再用一个布尔值区分语义，而是使用完整且规范化的策略摘要；改变通道、是否启用、门控阈值、期限或文档上限都会隔离缓存。

## P9 错误因果事实

- 调用失败不再统一压成普通错误或成功文本：找不到目标、目标不可见、被禁用、已撤销和权限拒绝各自保留独立原因。
- 参数校验错误现在携带稳定的问题位置；能力错配同时给出声明能力与注册能力事实，便于定位而不依赖文案匹配。
- 媒体凭证缺失与刷新/传输失败不再混为一种；取消继续作为取消传播，不被包装成传输故障。
- 面向模型的错误内容会遮蔽本机路径和常见密钥片段；内部诊断只记录路由、来源、目标、代次和稳定错误码等安全字段。
- 静态边界门禁使用语法树而不是文本猜测，当前扫描 2129 个生产源码文件为 0 违规；三类底层执行入口均采用精确文件白名单，并对桥接层和核心引擎的退役旁路单独设规则。

## P10 路径等价事实

- 对同一主体、会话、目标、规范参数、策略快照和代次，只改变直接、延迟或插件开发聊天路线，审批和真实执行语义保持一致。
- 宿主文件边界产生的规范参数在权限包装与统一入口之间显式绑定，不再因路线不同被重复解析或误当成模型原始参数。
- 本地开发者 HTTP 路线保留自己的主体类型，但继续复用相同的参数校验、生命周期、目标身份和规范执行器。
- 完整配置矩阵在补入测试后首次即绿，说明授权变化、审批后撤销、取消与流式更新等组合已由前序修复覆盖；本项没有为制造红灯而改坏生产代码。

## P11 架构说明事实

- 架构说明把“不随路线变化”写成可检查的九部分契约：身份分工、完整执行顺序、四路线同入口、绑定内容、底层来源边界、错误表、新工具清单和禁止做法都与现有门禁对应。
- 文档没有恢复 v0.1.34 已退役的知识旧路线，也没有把展示名称或目录名称重新当成执行身份。
- 修复报告、测试报告、剩余事项和机器事实均使用 v0.1.34 发布提交与校正版分支坐标；旧分支材料只作结构参考，旧基线失败和旧阻塞没有复制进新结论。
- 机器事实中的源码候选与封印字段按任务书固定为 `null`，防止提交自引用；真实坐标将在允许更新的 `PROGRESS.md` 与最终回复中记录。

## P12 首次验证事实

- 首个源码候选的功能契约门禁全绿，但开放发行清单没有登记 6 个新增规范模块，导致它们产生的 26 条连接被视为新增跨界；这是清单/生成闭包遗漏，不是把整个目录放开的理由。
- 当前修复只加入 5 个精确文件和 1 个本次新增的小目录；重新计算后仍保留原有 1 条已知债务，没有把新连接写成既有债务来绕过门禁。
- 第二个候选的全量测试确认生产存储格式没有变化，但四个参与持久化契约指纹的源码哈希随本任务变化；以兼容变更理由重钉当前生成值，不更改数据纪元。
- 两个媒体可观测性夹具仍按旧接口直接调用适配器；补入统一解析后的执行目标后恢复通过，生产端继续拒绝缺失凭证来源。
- 审计封印测试在 P12-04 前必然读取旧坐标；该项没有被跳过或改成警告，顺序冲突继续以真实失败保留。
