# Task Plan: Codex Model Observatory Phase 10.1 最终修复

## Goal

在当前工作树、当前提交状态中，严格依照用户提供的 Phase 10.1 任务书修复数据真实性、查询语义、Blob 原子性、动态生命周期和发布证据闭合问题，并完成重新验收。

## Current Phase

Phase 5：交付复核

## Phases

### Phase 1：需求与基线事实

- [x] 完整读取任务书并提取白名单、禁止项、交付物和验收命令
- [x] 检查当前提交、工作区改动、项目规则与既有实现
- [x] 记录当前失败基线和未闭合事实
- **Status:** complete

### Phase 2：调用链与根因还原

- [x] 按任务书逐项定位生产入口、存储路径、查询路径和发布链
- [x] 为每个问题建立可复现证据和最小修复方案
- [x] 明确兼容性、迁移和历史数据约束
- **Status:** complete

### Phase 3：精准实现与回归测试

- [x] 只修改任务书允许范围内的必要代码
- [x] 先补或确认失败测试，再实施修复
- [x] 分项运行相关测试并记录结果
- **Status:** complete

### Phase 4：完整验收与证据闭合

- [x] 执行本机可完成的静态、单元、集成、构建和打包验收
- [x] 核对产物、清单、运行时与历史数据行为
- [x] 对未执行的远端跨平台 CI 与源码封印保留真实阻断证据
- **Status:** complete（本地）；remote CI / seal 为 NOT_EXECUTED

### Phase 5：交付复核

- [x] 审查最终差异，确认没有创建或切换工作树/分支
- [x] 更新工作记录、历史对账和 V2 交付证据
- [x] 汇总完成项、验证结果及剩余阻塞
- **Status:** complete（等待用户决定是否另行授权提交/推送）

## Key Questions

1. 任务书对允许修改路径、必须交付文件及验收门槛的精确定义是什么？
2. 当前代码与任务书描述的审查发现是否一致，真实根因分别在哪里？
3. 哪些历史数据、运行时版本和发布证据必须保持兼容或原样？

## Decisions Made

| Decision | Rationale |
|----------|-----------|
| 不创建或切换工作树、分支 | 用户明确要求原地执行；当前状态为 detached HEAD，仍保持原状 |
| 完整读完任务书和基线后再修改生产代码 | 防止误读白名单、禁止项或验收条件 |
| 保留原始失败、退出码与统计 | 确保最终结论反映真实状态 |
| 不以“全绿”代替语义验收 | 最终成功取决于各类缺失、损坏和不可用状态能否贯穿全部产品层而不失真 |
| 数据库版本只在正式列确有必要时升级 | 先检查当前版本与兼容机制；若升级则必须完成单事务迁移、回滚、版本兼容、指纹和导出闭合 |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| macOS 大小写不敏感导致计划文件 `progress.md` 覆盖已跟踪的 `PROGRESS.md` | 1 | 初始状态已确认干净；将本轮记录迁入 `OBSERVABILITY_REMEDIATION_PROGRESS.md`，再用当前提交内容精确恢复 `PROGRESS.md` |
| 首次记录上述错误的多文件补丁因上下文不匹配未应用 | 1 | 改为先精确定位文本，再使用小范围补丁；没有部分写入 |
| 相关测试基线未启动：工作树缺少本地 Vitest，`npm exec` 临时下载 4.1.11 后 fsevents node-gyp 失败，配置仍找不到 `vitest/config` | 1 | 保留原始失败；确认依赖目录后按锁文件执行标准 `npm ci`，再用本地固定版本重跑 |
| 记录 Query RED 结果的多文件补丁上下文不匹配 | 1 | 拆为逐文件小补丁，避免跨文件上下文耦合 |
| 只读 `rg` 正则中的反引号被 shell 解释为命令替换 | 1 | 无文件写入；后续用单引号固定 shell 正则 |

## Notes

- 每次重大决定前重读本文件。
- 每完成一个阶段立即更新状态。
- 所有错误都写入记录，避免用跳过、弱断言或吞掉退出码制造假绿。
- 最终报告矩阵只允许 `PASS`、`BOUNDED`、`FAIL`、`NOT_EXECUTED`，不得把未执行或有边界的结果写成完整通过。
- 因仓库已有 `PROGRESS.md` 且文件系统不区分大小写，本轮以任务书指定的 `OBSERVABILITY_REMEDIATION_PROGRESS.md` 代替技能默认的 `progress.md`。
