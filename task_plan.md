# Knowledge 功能实施计划

## 目标

以用户附件为权威规格，在不破坏现有数据、模型凭证边界和发布门禁的前提下，分阶段交付 Notebook-first 的原生 Knowledge 功能，并用可复跑测试证明引用、范围冻结、全文覆盖与恢复语义真实成立。

## 当前阶段

Phase 10：逐条完成度审计与缺口修复（本机闭环已完成）

## 阶段

### Phase 0：事实核对、基线冻结与架构契约

- [x] 核对工作区、Node 24、测试与构建命令、审计 seal 状态
- [x] 还原现有持久化、文件导入、文档解析、检索、模型调用、后台任务、API 与一级页面边界
- [x] 把 Notebook、快照、解析、引用和 Owner 边界写成可执行契约测试
- [x] 完成 Embedding/Rerank 真实适配代码与协议契约测试；因本机无供应商密钥，live smoke 明确保留为 `NOT_EXECUTED`
- [x] 明确首个可交付垂直闭环及其白名单
- **状态：** completed

### Phase 1：Knowledge 持久化骨架

- [x] 独立数据库、迁移、持久化登记与原始内容托管存储
- [x] Notebook、Source、成员关系、内容快照及重启恢复
- [x] 接入核心引擎但不堆入门面类
- **状态：** completed

### Phase 2：解析与可信引用闭环

- [x] 来源适配、解析产物、顺序块、定位锚点、引用校验与来源查看器
- [x] 首批 citation-grade 格式和扫描 PDF 明示失败语义
- [x] 历史快照在外部原文件变化或删除后仍可打开
- **状态：** completed

### Phase 3：原生 Knowledge 一级页面

- [x] 一级入口、Notebook 管理、来源管理、解析状态与查看器
- [x] 查询只允许选择一个或多个 Notebook，不暴露查询级文件勾选
- **状态：** completed

### Phase 4：FTS Quick Answer

- [x] 不可变范围快照、中文全文检索、上下文构建、统一生成调用与引用校验
- [x] 未选择 Notebook 或任一来源未准备好时明确拒绝
- [x] UI 明示“相关内容检索”，不声称全文已读
- **状态：** completed

### Phase 5：模型操作与混合检索

- [x] knowledge 语义模型角色及统一非流式协议覆盖
- [x] embedding/rerank 操作能力、统一凭证/用量/观测边界与可替换向量索引
- [x] FTS-only、FTS+Vector、FTS+Vector+Rerank 三种状态闭环
- **状态：** completed

### Phase 6：可恢复全文研究执行器

- [x] 研究规格、冻结范围、覆盖清单、分析单位、执行批次、实际尝试与后台任务挂接
- [x] 只按覆盖单位记账，检索只改变顺序；重启后不重复已完成工作
- **状态：** completed

### Phase 7：证据、结论与完整矛盾检查

- [x] 后端校验证据、证据账本、结论关系、支持状态与认识依据
- [x] 完整矛盾检查清单；未达到 100% 必须标为部分完成
- **状态：** completed

### Phase 8：受约束综合与研究界面

- [x] 最终综合只能读取已验证证据与覆盖信息
- [x] 报告、覆盖、冲突、限制、引用跳转、取消和恢复界面
- **状态：** completed

### Phase 9：稳定性、跨平台与回归

- [x] 用自动化契约覆盖重启、崩溃恢复、迁移、索引重建、删除、凭证轮换与模型切换
- [x] Node 24 下完成目标测试、类型检查、构建、真实浏览器运行验收和完整回归
- [ ] 在 macOS arm64/x64、Windows x64、Linux x64 的正式 CI/打包矩阵执行验证
  - 工作流已就绪：四个明确宿主均有架构断言、源级恢复烟测、种子验签与真实归档重启烟测；等待获准提交后远端执行。
- [ ] 使用真实供应商密钥完成付费 Embedding/Rerank live smoke
- [ ] 获得提交授权并形成提交坐标后推进 audit seal
- **状态：** bounded；本地实现与验证完成，三项外部证据为 `NOT_EXECUTED`

### Phase 10：逐条完成度审计与缺口修复

- [x] 把附件 75 个主题章节、A–M 核心场景和全部明确禁项映射到源码与测试证据
- [x] 逐项判定 `PROVED`、`CONTRADICTED`、`WEAK`、`MISSING` 或 `EXTERNAL`
- [x] 修复所有可在当前工作区闭合的实现与验证缺口，并复跑受影响门禁
- [x] 将只依赖真实平台、供应商密钥或提交坐标的项目保持为外部证据，不冒充完成
- **状态：** completed_local；本机可闭合项全部完成，跨平台 CI、真实付费模型 live smoke、提交后 audit seal 仍为 `EXTERNAL / NOT_EXECUTED`

## 关键问题

1. 当前源码对附件中的架构事实有无漂移，哪些现成模块可复用？
2. 首批 citation-grade 格式怎样在现有解析基础上提供稳定定位？
3. 哪个向量后端能通过四平台 Electron 打包验证；若没有，如何保持 FTS-only 正式可用？
4. 现有模型统一调用边界缺哪些文本协议与非生成式操作能力？
5. 当前测试与 audit seal 的真实基线是多少，新增公共模块需要同步哪些清单？

## 决策

| 决策 | 理由 |
|------|------|
| 在独立工作区和 `codex/knowledge-notebook` 分支实施 | 主工作区有 11 个与本任务无关的用户删除，必须隔离保护 |
| 附件是产品语义权威，源码是现状权威 | 避免把旧架构判断当成当前事实，同时不擅自改写产品约束 |
| 按垂直闭环推进，不先做空壳式全层铺开 | 每个阶段都能用真实用户结果和可复跑测试判定 |
| FTS-only 是正式基础能力，向量与重排是增强项 | 附件明确禁止把未验证的原生向量依赖设为系统前置条件 |

## 错误记录

| 错误 | 次数 | 处理 |
|------|------|------|
| 手工创建 goal 失败：任务已由 `/goal` 自动建立 | 1 | 读取现有 goal 后继续，不再重复创建 |
| 大小写不敏感文件系统把 `progress.md` 映射到已有 `PROGRESS.md` | 1 | 从 HEAD 完整恢复原文，仅追加本目标摘要；改用唯一文件 `knowledge_progress.md` |
| 生产依赖审计发现 7 个既有中等级别问题 | 1 | 保留退出码与依赖链证据；不做无关破坏性自动修复，确保本功能不新增使用 |
| 首次完整测试缺少工作区构建产物，37 个文件无法解析 `@lingxi/plugin-*` | 1 | 保留 1169/1207 文件通过与原始退出码；官方构建成功且 4 个代表性首错文件已恢复，现于不同前置状态重跑全量基线 |
| 首次生产服务构建缺少必需的签名密钥 | 1 | 使用只存在于临时目录的抛弃式密钥完成真实签名构建，验证后精确销毁，未写入仓库或聊天 |
| 源码直接运行暴露两处 Node strip-only 不支持的参数属性语法 | 1 | 改为普通字段；随后服务启动和真实浏览器验收均通过 |
| 最终完整回归暴露两处旧固定集合数量与一份新页面样式令牌违例 | 1 | 把第七个语义角色和第九种观测输入形状纳入闭集；页面全部改用样式令牌，定向与完整回归均通过 |
| v5 持久化门禁仍写死 Knowledge v4 | 1 | 只同步真实运行时版本断言；复跑 54/54 通过 |
| 临时签名目录的递归删除命令被系统安全规则拒绝 | 1 | 未执行被拒命令；改为逐一删除两个已知文件并移除空目录，回读确认路径不存在 |
| 四平台流水线新契约首轮按预期失败 | 1 | 3 个文件红灯；下一步实现明确宿主矩阵、架构断言、源级恢复烟测与包内重启烟测后复跑 |
| 四平台组合回归触发旧 Node 矩阵形状断言 | 1 | 把单轴断言升级为四个平台明细行全部固定 Node 24.15.0；复跑 48/48 通过 |
| 快速重装箱内联模块转义失败 | 1 | 未触及归档；改用临时普通模块文件执行，完成后与密钥一起删除 |
| 最新全仓回归出现 Dream 修订页面单项时序失败 | 1 | 12270 项通过、1 项失败；先单文件复现并审查调用时序，禁止直接把它当偶发波动忽略 |

## 执行纪律

- 每个阶段完成后更新本文件、`findings.md`、`knowledge_progress.md`，并同步项目的 `PROGRESS.md` / `BLOCKED.md`。
- 所有 SQL 使用参数绑定；所有外部输入先校验；日志不记录正文、完整提示、密钥或用户绝对路径。
- 不修改测试判据、跳过测试、放宽断言、伪造模型/存储对象或用失败吞掉命令来换取绿灯。
- 同一验收连续失败三次时切换任务并记录；结果比基线差则撤回本任务造成的改动并如实说明。
### 2026-08-25 追加复验

- [x] 复核最新全量测试中唯一的旧界面失败，确认是恢复提示与后续刷新之间的测试竞态。
- [x] 只补测试等待，不修改产品行为；单文件首次及连续 10 轮均通过。
- [x] 重跑四平台契约、知识稳定性集合与静态门禁：28/28、85/85，TypeScript/代码规范/边界/补丁空白均通过。
- [x] 重跑全量测试：1222 files passed、1 skipped；12274 tests passed、7 skipped；130.80s。
- [ ] 在四种真实托管主机上执行新矩阵并保存远程证据（当前改动未提交，且没有 commit/push/远程触发授权，因此仍为 `NOT_EXECUTED`）。

### 2026-08-26 连续阻塞审计

- [x] 重新完整读取原始任务书并核对当前源码、测试、工作流、归档和远端状态。
- [x] 当前复跑四平台统一稳定性集合 85/85、工作流契约 28/28、本机真实归档烟测，退出码均为 0。
- [x] 确认远端没有本分支且没有本分支运行记录，不存在可回读的跨平台证据。
- [ ] 获得明确 commit/push/远程工作流触发授权后，才能产生附件 Phase 9 要求的四种真实宿主证据。
- **状态：** blocked_external；同一授权缺口已连续超过三轮，且本机不能替代真实宿主执行。

## 2026-09-02 安全双件套

### 目标
外部证据在进入模型前完成净化检测、风险提示与边界标记；工具循环在重复、连续失败和同片重读时机械提醒并最终阻断。

### 阶段
- [x] Phase 1：共享扫描引擎与独立单测（16/16 通过）。
- [x] Phase 2：普通/滚动知识证据接线与原文回归（扫描+知识定向 104/104 通过）。
- [x] Phase 3：跑飞守卫扩展、服务注册与扩展单测（20/20 通过）。
- [x] Phase 4：开放边界生成物、定向测试和三段类型检查（边界 74/74；typecheck 三段绿）。
- [x] Phase 5：两条反向验证、全仓测试、PROGRESS 收口（12927 passed / 7 baseline skipped）。

### 错误记录
| 错误 | 次数 | 处理 |
|---|---:|---|
| 首次追加补丁错误复用另一个文件的尾部上下文，校验前失败且未写入 | 1 | 分别读取三个文件真实尾行后再追加，不原样重试 |
| 安全双件套首次全仓回归：5 项失败（旧证据预算未计边界开销；`server/index.ts` 触发持久化指纹门禁） | 1 | 保留生产安全开销，显式校准该旧测试预算；按 `compatible` 口径重钉指纹后定向复验 |
| 指纹生成脚本不支持无值 `--help`，参数解析先于帮助输出而退出 | 1 | 读取脚本参数契约，改用明确的分类与兼容理由，不原样重试 |
