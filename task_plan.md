# LingxiAgent 版本事故修复执行计划

## 目标

查清并修复 `0.1.3` 版本倒退导致的运行时未切换风险，验证下游功能完整性，补齐运行时可观测性与发布硬门禁，并用自动化升级场景证明受影响用户可无损自愈。

## 硬边界

- 源码、Git 历史、现有测试和真实发布链路优先于任务书中的事故假设。
- 不删除用户配置、整个 Artifact 目录或历史回滚数据。
- 不使用事故版本特判；修复必须覆盖正常升级、重装、旧安装包、旧指针、OTA、损坏指针和崩溃回退。
- 不顺手修改范围外代码；无法安全裁决的事项写入 `BLOCKED.md` 后继续其他阶段。
- 每个阶段都记录可复跑命令与真实结果，不用跳过或弱断言掩盖失败。

## 阶段

1. **已完成：建立事实与事故复现**
   - 读取完整任务书、现有进度和仓库规则。
   - 记录分支、提交、工作区、版本、标签、SDK 与发布历史。
   - 建立版本传播图，定位所有版本比较与指针决策。
   - 用现有逻辑复现 `current 0.1.22 + seed 0.1.3`。
   - 验证：形成从输入、决策、指针到启动目录的证据链。
2. **已完成：下游功能与上游同步审计**
   - 建立同步前 downstream、上游目标、当前 main 的三方比较。
   - 覆盖模型、辅助模型、语义槽位、审批、网关、提供方、Ollama、技能、记忆、通信、持久化和更新系统。
   - 验证：生成 `UPSTREAM_SYNC_AUDIT.md`，每项有文件、测试或 Git 证据。
3. **已完成：版本与 Artifact 选择修复**
   - 确定实际最高正式版本，选择严格递增的修复版本。
   - 分离产品展示版本与发布先后顺序，保留 OTA 和崩溃回退语义。
   - 验证：覆盖任务书列出的 seed 选择性质与五个事故迁移场景。
4. **已完成：运行时可观测性与兼容保护**
   - 接入现有日志或诊断，报告 Shell、renderer、server、来源、train 与发布来历。
   - 对明显危险组合显式告警或进入已有恢复路径。
   - 验证：正向、负向测试均能观察到确定结果。
5. **已完成：发布防复发门禁**
   - 正式版本倒退、关键 Artifact 或升级 smoke、seed 校验和发布完整性失败时阻断所有正式发布与指针更新路径。
   - 验证：本地执行等价 CI 命令，负向夹具确实失败且阻断链路可证。
6. **已完成：全量验证与交付（发布摘要外部阻塞已单列）**
   - 运行定向测试、全量测试、类型检查、构建或打包相关校验、差异与白名单审计。
   - 更新 `PROGRESS.md`、`BLOCKED.md` 和必要的版本规范文档。
   - 验证：每项成功标准有命令、退出码和产物证据；未完成项明确阻塞原因。
7. **已完成：解除发布摘要阻塞并复核发布输入**
   - 已完成来源提交和 GitHub 输入复核；按用户指定把生成路径改为 `DEEPSEEK_API_KEY`、`deepseek-v4-flash` 和 DeepSeek Responses API。
   - 先以官方文档和真实契约确认端点/请求/响应，再以负向测试证明缺密钥、错误响应、非法摘要和版本不匹配均失败关闭。
   - 摘要必须经过仓库生成器和两份校验，不手改、不绕过；任何密钥只从环境读取且不得写入文件或日志。
   - 验证：摘要校验退出 0；预检、差异检查和关键测试保持通过。
8. **已完成：提交并推送 main**
   - 只暂存本事故修复、测试、生成清单和交付报告，提交后回读 SHA 与远端状态。
   - 验证：`origin/main` 指向新提交，工作区干净。
9. **已完成：创建并推送 tag，监督正式 Release 链**
   - `v0.1.23` 已不可变地指向 `1f24a039` 并触发 Build；远端 Linux 质量门禁暴露两组平台依赖测试夹具，Release 尚未创建。
   - 不移动、不删除、不强推既有 tag；修复跨平台测试后使用 `v0.1.24`、发布世代 2 继续正式发布。
   - `v0.1.24` 已精确指向 `480603cb`；GitHub Release 非草稿、非预发布、成为 Latest，18 个资产通过工作流硬门禁与独立复核。
   - stable train 3、beta train 6 的签名、来源提交、发布世代、版本、文件大小和摘要均已独立复核通过。
   - AtomGit 外部镜像连续在首个 442 MB 文件上传回执处超时，不影响 GitHub single source of truth 与 signed Train；平台阻塞和两次真实运行已单列到 `BLOCKED.md`，没有伪装成成功。

## 初始假设的最终裁决

- 已证实：`0.1.3` 的 Git 和已发布归档都含新功能，实际运行 seed 因标准版本小于 `0.1.22` 而未激活。
- 已证实：原有 train 只在单频道内单调，manifest 没有跨安装包/频道的发布世代；因此新增最小 `releaseGeneration`，并复用既有签名与激活链。
- 已定案：实际历史最高产品版本为 `0.1.22`；首次修复候选 `0.1.23` 因 Linux 测试夹具问题只留下失败 tag、没有 Release，最终正式修复版本为严格更大的 `0.1.24`、发布世代 2；摘要已通过双校验。

## 错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 首次补丁无法匹配 `PROGRESS.md` 结尾，未产生部分写入 | 1 | 分开创建计划文件，再按实际结尾追加旧进度 |
| `gh release list` 使用了不支持的 `url` JSON 字段 | 1 | 保留成功的远程标签证据，下一次改用工具列出的字段重查 Release |
| 首次全仓 Artifact 关键字检索误扫 `desktop/dist-renderer`，输出被截断 | 1 | 已保留文件入口清单；后续只检索源码扩展名并显式排除所有生成目录 |
| 首次事故复现脚本调用了未导出的内部版本比较函数 | 1 | 改为只调用模块公开的启动决策入口，成功得到 `boot 0.1.22` 结果 |
| zsh 循环的 A/B/C Git 对象分类把所有路径误判为 missing | 1 | 结果已作废；改用显式 `bash` 和逐对象检查重跑，并先用 `package.json` 做阳性校验 |
| GitHub `0.1.3` 发布资产下载遇到临时链接 EOF，后续命令因未启用 fail-fast 仍返回 0 | 1 | 整次产物抽检作废；改为单资产下载并启用 `set -e`，失败时不执行解包和结论 |
| 首次 `publish-train.mjs` 组合补丁上下文不匹配，未产生部分写入 | 1 | 按真实 import、组装和入口位置拆成小补丁完成 |
| 版本预检负向脚本把 zsh 只读变量 `status` 当作退出码变量 | 1 | 结果未用于结论；改名 `preflight_code` 后确认负向退出码为 1 |
| 三包版本读取直接 `require(package/package.json)`，其中一包禁止导出该子路径 | 1 | 改为只读实际安装目录中的 `package.json`，三包均确认 0.84.1 |
| 首次类型检查被 `node_modules/@types/* 2` 重名目录污染，未进入源码检查 | 1 | 按 lockfile 执行 Node 24 `npm ci`，随后原命令 0 error |
| release digest 生成器需要 `OPENAI_API_KEY`，当前环境未配置且改动尚未提交 | 1 | 遵守规则不手改摘要；记录到 `BLOCKED.md`，让发布校验继续阻断 tag |
| 首次本地 seed 构建命令因包含 shell `rm -rf` 清理被执行器安全规则拒绝，未启动构建 | 1 | 改为只删除经 `/tmp/lingxi-seed-key.*` 前缀校验的临时目录；构建、验签通过并确认私钥目录已移除 |
| 最终秘密扫描把计划中记录的临时目录命名规则误报为秘密，检查脚本退出 1 | 1 | 去掉非秘密的目录模式，只扫描真实私钥头和一次性 key id 后重跑 |
| `npm audit` 与 `npm audit --omit=dev` 因发现风险退出 1 | 1 | 不掩盖退出码；逐项确认可兼容升级的直接依赖并更新，回归后重新审计 |
| `npm outdated --json` 因存在可更新包退出 1 | 1 | 结果仅作升级范围取证，不当成测试失败；只选择修复 critical/high 所需的兼容版本 |
| `npm audit fix --package-lock-only --ignore-scripts` 因仍有中低风险退出 1 | 1 | 安全更新已写入 lockfile；拒绝 `--force` 提供的降级/主版本跳跃，最终残余风险单独留档 |
| 安全升级后的首次全量测试出现大量装载失败，类型检查也提示缺少 `yauzl` | 1 | 最小样本确认根因是生产代码借用了打包工具的传递依赖；改为直接声明 `yauzl` 与类型包，81 项相关用例恢复通过 |
| Electron 补丁升级后 shell 表面清单仍声明旧版本，守卫 1/41 失败 | 1 | 同步受守卫管理的版本字段；不修改守卫断言，等待全量回归确认 |
| v2 摘要复核误传不存在的 `--schema` 参数，退出 1 | 1 | 结果只证明参数错误，不用于门禁结论；读取校验器后按文件内容自动识别的真实接口重跑 |
| 正式摘要生成在来源采集成功后因缺少 `OPENAI_API_KEY` 退出 1 | 2 | 源码提交 `cf0be5bc` 与来源包已就绪；停止推送/tag/Release，等待用户把任务所需凭据配置到当前环境 |
| planning-with-files session catchup 命中无关的旧技能/MCP 问答 | 1 | 与当前仓库、摘要和发布无关，不合并进计划或代码事实 |
| 首次官方 Responses 页面正文提取为空 | 1 | HTML 解析器错误地按所有闭合标签递减 article 深度；未形成契约结论，改为只跟踪 article 起止后重跑 |
| 修正解析器后正文仍为空 | 2 | 不再重复解析假设；改查 HTTP 状态、重定向和实际响应字节，再从 Docusaurus 页面数据或官方参考提取契约 |
| DeepSeek 迁移测试首次运行 7 项失败 | 1 | 失败均准确命中旧 OpenAI 默认值、旧导出和缺失的安全检查；保留为红灯证据后实现迁移 |
| DeepSeek 改动后首次 lint 报 959 errors | 1 | 全部来自被忽略的 5.7 MB 构建临时副本 `build/.cli-closure-nft-scratch-nft-server-bundle 2.mjs`；先核对出生时间、忽略状态和生成入口，再只移除该临时文件重跑 |
| 临时终端首次静默读取把下一条 export 命令当成输入 | 1 | 没有使用错误值发请求；改为分两次写入，先等待静默输入状态，再单独提交密钥 |
| DeepSeek 首次真实生成返回完成状态但本地 JSON 解析失败 | 1 | 响应同时含思考过程和最终正文，旧提取器拿了首个任意 text；摘要未落盘，先补思考项在前的复现测试，再只提取 `output_text` |
| 最终全量测试的 JSON reporter 未生成指定报告文件 | 1 | 该次没有最终统计，不能当作通过证据；改用仓库原始 `npm test`、日志文件和显式退出码重跑 |
| `v0.1.23` 远端 quality-gate 在 Linux 失败 8 项 | 1 | 6 项路由测试默认使用宿主平台、2 项引擎测试只在本机 macOS 成立；保留 Linux 不支持的生产行为，改为在正向夹具中显式指定 darwin |
| `v0.1.24` 事实约束摘要首次生成 incomplete | 1 | DeepSeek 在 4000 输出额度内耗尽并返回 `max_output_tokens`；生成器按设计未落盘，增加请求额度契约测试后提升到 8000 |
| 额度提升到 8000 后事实约束摘要仍 incomplete | 2 | 官方 Responses 契约确认默认启用思考且额度同时计算推理/正文；摘要任务改为 `reasoning.effort=none`，保留 8000 正文上限 |

# 供应商配置与模型目录状态漂移修复（2026-08-09）

## 目标与边界

- 以现有供应商配置目录和运行时模型目录为权威源，修复设置左栏、删除生命周期、辅助模型选择器之间的状态漂移。
- 保持内置供应商可重新选择、自定义供应商既有删除语义、视觉能力过滤和完整模型引用不变。
- 不新增第二套持久化列表或事件，不用浏览器缓存、轮询、延迟刷新、远端模型发现掩盖内部同步问题。
- 所有写操作失败关闭；只修改彻底修复与回归测试必需的代码，不升级依赖、不改版本、不发布。

## 阶段

1. **已完成：建立基线与状态流审计**
   - 记录提交、分支、工作区、Node/npm；运行供应商设置、模型同步、应用事件、辅助模型和类型检查基线。
   - 追踪供应商成员、当前选择、临时草稿、配置写入、运行时模型投影和前端订阅的完整数据流。
   - 验证：在 `findings.md` 和 `PROGRESS.md` 写清 BUG 1/2/3 的具体断点，改代码前先形成裁决。
2. **已完成：用失败测试固定正确契约**
   - 覆盖持久供应商重载、最后选择不定义成员、草稿增删、已配置删除、内置项回候选、写失败关闭。
   - 覆盖辅助模型共享来源、已挂载刷新、删除刷新、视觉过滤、同名模型跨供应商区分。
   - 验证：新增断言在旧实现上命中真实缺陷，保留红灯证据。
3. **已完成：修复供应商成员与删除状态模型**
   - 从权威摘要恢复持久项；把页面临时草稿与持久项分离；成功删除后由权威状态收敛，失败不假删。
   - 验证：P1-P6、D1-D5 对应定向测试通过。
4. **已完成：收口运行时模型状态与写操作错误处理**
   - 让辅助模型复用统一前端模型状态；保证配置变更到运行时刷新和事件传播失败关闭。
   - 审计所有本任务涉及的供应商/模型写路径，只有服务端成功后才刷新和提示成功。
   - 验证：M1-M6、T7-T12 与服务端路由测试通过。
5. **已完成：全量验证与对抗复审**
   - 运行定向测试、类型检查、全量测试、差异检查和白名单审计。
   - 复查生命周期、快速连续操作、权威源边界，以及远端模型发现只承担 discovery。
   - 验证：对比基线无新增回归；阶段记录、阻塞和交付清单完整。
6. **已完成：真实界面点击复现与二次修复**
   - 启动真实服务和渲染界面，用 Playwright 实际点击供应商模型与辅助模型选择器，记录网络、事件、控制台和截图证据。
   - 先在当前实现上复现用户报告，再沿真实窗口通信链定位单元测试遗漏的断点，只做最小修复。
   - 验证：隔离 Electron 中实际点击新增、删除、重新添加并选择辅助模型；无需重开设置或手动“读取模型”即可更新已挂载选择器。定向、类型、lint、低并发全量和差异检查均通过；默认并发全量的既有发布预检超时另行保留真实退出码。

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 首次绿灯定向运行剩 2 项供应商测试失败 | 1 | 两项旧夹具仍假设“草稿没有移除按钮”和“仅设置 selected 就产生成员”；按已确认的新契约改为期待草稿删除入口，并通过 Picker 创建草稿，不修改实现来迁就旧假设 |
| 首次写路径检索命令因双引号内含反引号导致 zsh 报 unmatched quote | 1 | 该检索没有产生结论；改用不含 shell 反引号的固定字符串查询重跑 |
| 扩大定向回归后 AgentTab 旧测试仍用组件私有 `/api/models` mock 喂数据 | 1 | 生产组件已改读设置窗口共享目录；测试改为直接布置同一共享状态，验证文案和 provider 图标契约不变 |
| 状态源复核命令包含不存在的历史路径 `server/routes/agent-config.ts` | 1 | 该路径未参与结论；从实际 `settings-snapshot.ts` 与 `agents.ts` 入口确认配置快照确实投影 Provider Catalog |
| Onboarding 供应商步骤直接点击“下一步”的首个 Playwright 命令未返回页面输出 | 1 | 不重复盲点；重新侦察当前页面和窗口，确认是表单校验、窗口切换还是命令等待，再选择最小完成路径 |
| 选择本地 Ollama 后继续的组合脚本再次超过短等待窗口 | 2 | 服务日志只显示上一阶段记忆配置写入；拆成单次点击与只读页面检查，不再在一个命令里等待整段 onboarding 流程 |
| 触发最小 onboarding 完成后旧页面等待报 `TargetClosedError` | 1 | 这是 onboarding 窗口按设计关闭造成的目标切换；不当作产品失败，重新连接调试端口获取新主窗口 |
| 首次“添加模型后打开辅助下拉”的长组合脚本未在工具等待窗口内结束 | 1 | 页面已实际推进到“配置模型”，拆开核对持久目录、运行时接口和当前辅助列表；后续正式用例使用分段断言与无缓冲输出 |
| 分段点击“添加模型”后 portal 未在 3 秒内保持可见 | 1 | 后端核对证明此前模型未写入；先显式滚动控件并等待布局稳定，再点击并立即断言 portal，区分自动滚动关闭与产品点击失败 |
| 修复后界面脚本等待供应商文本触发 Playwright strict violation | 1 | 页面同时有左栏名称和右栏标题，产品写入已 200 成功；选择语义明确的右栏 heading，不放宽成模糊匹配 |
| 改用 heading 后命中一个隐藏的同名标题并超时 | 2 | 页面详情和“添加模型”按钮实际可见；不再等待不参与操作的标题，直接以可见操作按钮为前置条件 |
| 修复后首次 portal 等待仍因打开态翻转而超时 | 2 | 每次操作前先检查 portal 当前状态，关闭则点击一次，随后立即断言；不再假设前一脚本结束时一定关闭 |
| 首个热刷新回归用例使用可访问形态的 HTTPS 地址时错误转绿 | 1 | 改为界面复现所用的不可访问本地地址后稳定命中空目录；证明地址条件会放大竞态，保留真实条件作为测试夹具 |
| 并行重复红灯测试的失败差异展开了上千个内置模型，输出被截断 | 1 | 结论只取退出码与缺失目标模型；修复后使用点状 reporter 重跑 8 份，全部退出 0 |
| 界面脚本用相对 `/api/models` 诊断时读到 Vite 的 HTML 并 JSON 解析失败 | 1 | 不把它当产品接口结果；改为捕获产品自身带连接信息发出的 `/api/models` 请求，确认返回目标模型 |
| 首次用原始模型 id 检查下拉文本得到假阴性 | 1 | 下拉按设计显示格式化名称而不是原始 id；改为同时检查供应商分组和格式化名称，旧实现的空下拉仍能明确区分 |
| 关闭辅助下拉时 `Escape` 同时关闭了设置弹层 | 1 | 不再依赖全局 Escape 切页；通过触发按钮切换 portal，必要时重新打开设置并重新建立监听前置条件 |
| 删除后重新添加时，滚动后立即点击“添加模型”未保持 portal 打开 | 1 | 先等待滚动完成，再单独点击并立刻读取 portal；该失败只属于自动化节奏，未产生写请求或产品结论 |
| 全量默认并发两次均有发布预检用例超过自身 10 秒上限 | 2 | 两次均为 11040 通过、7 跳过、仅 1 项超时；该文件单跑 6/6 通过且耗时 4.28 秒，低并发完整测试集 11041/11041 通过。不修改测试上限或伪装默认命令退出码 |
| 首次差异秘密扫描把新增测试中的显式假密钥夹具判为命中 | 1 | 命中值只存在测试且明确为虚构数据；改为扫描真实界面测试标识和临时目录标识，结果为空，`git diff --check` 单独退出 0 |

# skillBadge 消息持久化修复（2026-08-10）

## 目标与边界

- 修复带技能卡片的用户消息在会话重载、应用重启、重试与分叉后丢失技能展示信息的问题。
- 只补齐现有消息展示记录的写入、历史投影和重试/分叉还原；不改提示词拼接、实时事件和前端渲染。
- 保留工作区中已有的供应商配置相关未提交修改，不回滚、不格式化、不顺手清理。

## 阶段

1. **已完成：建立事实与现有测试基线**
   - 核对三处断点、类型定义、展示记录配对逻辑和相关测试入口。
   - 验证：改代码前形成可定位的源码与测试证据。
2. **已完成：用失败测试固定持久化契约**
   - 覆盖展示记录写入、历史消息投影、重试/分叉还原技能列表。
   - 验证：旧实现上的失败与报告的三处断点一致。
3. **已完成：最小实现修复**
   - 三处按现有结构化展示字段的惯例补齐技能列表。
   - 验证：失败测试转绿，不改变无技能消息行为。
4. **已完成：回归与差异审计**
   - 运行定向测试、三段类型检查、差异检查和修改白名单审计。
   - 验证：真实退出码为 0；范围只包含本 Bug 必需文件及计划记录。

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| `planning-with-files` 会话恢复命中无关旧问答 | 1 | 不纳入本 Bug 事实；保留当前工作区已有改动，另起独立计划小节 |
| 新增纯技能守卫测试沿用邻近夹具的 `as any`，目标文件 lint warning 从 182 增至 183 | 1 | 改用对象合并挂载测试替身，避免本任务新增静态检查警告 |
| 低并发全量测试经 Node 24 调用 npm 后，npm 子进程仍按系统路径使用 Node 22，155 项失败 | 1 | 失败统计保留但不用于源码结论；不重建 Node 24 原生依赖，改为用 Node 24 直接启动 Vitest |

# 只读终端输出查看器（2026-08-13）

## 目标与硬边界

- 在桌面端右侧工作区实现当前会话的只读终端输出查看器，覆盖实时输出、状态、断线恢复、后端重启恢复、多终端和有界历史读取。
- 终端会话管理器是唯一生命周期事实源；不经过活动中心，不给现有子代理活动状态增加终端分支。
- V1 不支持输入、终止、尺寸调整、完整屏幕模拟或移动端；远程客户端不得获得终端状态和输出。
- 只改正确性、测试和五语言文案所需文件；性能实测若证明同步持久化是独立瓶颈，只记录后续优化，不在本任务顺手重构。

## 当前阶段

- **Phase 7：全量门禁与差异审计（已完成）**

## 阶段与验证

1. **已完成：建立事实与基线**
   - 核对终端事件、持久化格式、会话身份解析、消息权限、前端状态组合、右侧栏和测试惯例。
   - 验证：形成当前数据流、缺口、可复用入口和修改白名单；记录 HEAD、工作树、Node/npm 与相关基线测试。
2. **已完成：共享契约与终端会话管理器**
   - 先补失败测试，再实现完整生命周期快照和有界、会话隔离的历史尾部读取；保持现有模型工具读取语义不变。
   - 验证：成功、失败、终止、失效、大小/块数边界和跨会话拒绝全部有回归。
3. **已完成：服务端消息协议、输出合并与权限**
   - 实现状态转发、按时间/大小双阈值的有界合并、结束前强制清空、快照/尾部请求和本机所有者权限。
   - 验证：序号不丢、顺序正确、会话隔离、身份补齐、远程拒绝和重连恢复测试转绿。
4. **已完成：前端状态与高频输出通道**
   - 新增低频会话状态、无全局缓存的高频订阅流和客户端请求；处理尾部与实时输出竞态、重复和缺口。
   - 验证：路径迁移、会话隔离、无订阅即丢弃、去重、缺口恢复和折叠时无高频全局状态更新。
5. **已完成：卡片、输出预览、ANSI 安全和五语言**
   - 实现展示限制、排序、状态推导、标签优先、按需展开、粘底滚动和跨块 ANSI 状态；挂载右侧栏并补齐五份文案。
   - 验证：空态不渲染、五状态、多终端、展开收起、跨块颜色、危险 HTML/链接不可执行、用户上滚后停止自动滚动。
6. **已完成：真实集成与性能基准**
   - 用真实伪终端覆盖启动、实时输出、结束、关闭、断线重连、后端重启和多会话；记录 10 MB、小块高频和多终端数据。
   - 验证：卡片出现时延、每秒消息数、事件循环延迟、处理器消耗、磁盘写入和界面提交次数有可复核结果。
7. **已完成：全量门禁与差异审计**
   - 运行定向测试、三段类型检查、全量测试、界面构建、差异检查和白名单审计。
   - 验证：保留真实退出码与精确统计；禁止文件无改动，所有 19 项验收逐项有证据或明确阻塞。

## 本任务决策

| 决策 | 理由 |
| --- | --- |
| 先调查和写失败测试，再改产品代码 | 任务横跨持久化、权限、实时协议和界面竞态，先固定真实契约可避免按任务书猜实现 |
| 沿用仓库根目录现有三份计划文件并新增独立小节 | 避免覆盖历史记录，同时满足技能的断点恢复要求 |
| 不复用旧任务的测试结论 | 当前目标与历史任务不同，所有门禁都要在本轮现场重跑 |
| 尾部读取无起点时取最近窗口，带起点时优先补起点之后的窗口 | 首次展开需要最近历史；缺口恢复需要从已知序号继续，同时两者都必须受硬上限约束 |
| 尾部响应显式标注截断 | 有界读取无法假装返回完整历史，前端需要据此区分完整补齐和只能重置到最近窗口 |
| 同步 metadata 持久化性能另立优化，不与本功能混改 | 现场基准已证明突发写入会阻塞事件循环；目标明确要求先交付正确性并单独处理该瓶颈 |

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| `/goal` 已自动登记活动目标，手动再次登记被拒绝 | 1 | 读取现有目标后沿用，不再重复创建 |
| 文件化计划恢复命中 2026-08-12 的旧任务上下文 | 1 | 已确认工作树当前无短状态输出；旧结论只作历史记录，不当成本任务证据 |
| 终端消息路由测试首次绿灯复跑被稳定身份门禁拒绝 | 1 | 产品代码正确失败关闭；补齐测试引擎的 manifest 当前路径，不放宽身份校验 |
| Phase 4 首次类型检查报 11 个测试夹具字面量被拓宽 | 1 | 给消息夹具声明共享契约返回类型并补齐公开条目字段，不改生产类型来迁就测试 |
| TerminalCard 收起测试立即断言卸载，未等待既有退场动画 | 1 | 改为等待动画完成后断言；产品折叠语义不变 |
| 首次全量测试 10 项失败 | 1 | 失败均为仓库门禁准确发现新增开放模块、兼容持久化字段、生成闭包和样式硬编码尚未登记；按真实规则补清单、声明兼容性、重生成受控产物并改用设计变量，先复跑 75/75 再全量复跑转绿 |

# 终端与子助手卡片交互重构（2026-08-13）

## 目标与边界

- 右侧栏只展示当前会话中仍在运行的终端和子助手，每项固定两行：标题；运行时长与停止按钮。不在右侧栏展开详情。
- 所有终端命令和子助手记录继续保留在对话中，对话卡可展开为内嵌详情界面；停止后历史记录不消失。
- 点击右侧标题要滚动到对应对话卡并自动展开。停止操作必须走既有会话身份与所有权边界，不能借编号跨会话终止。
- 保留上一轮未提交的终端查看器改动及用户其他工作，不重置、不清理范围外文件。

## 当前阶段

- **Phase 1：建立真实关联链与基线（已完成）**
- **Phase 2：用失败测试固定新交互和权限契约（已完成）**
- **Phase 3：实现稳定关联、安全停止和历史/实时输出投影（已完成）**
- **Phase 4：重构右栏与对话卡片样式和跳转（已完成）**
- **Phase 5：真实界面点击验收（已完成）**
- **Phase 6：定向、类型、全量、构建和差异门禁（已完成）**

## 阶段与验证

1. **已完成：建立真实关联链与基线**
   - 核对两张参考图、终端工具卡、子助手卡、右侧活动卡、停止入口、消息持久化与定位能力。
   - 验证：明确右侧条目如何稳定关联对话卡，记录相关测试基线。
2. **已完成：用失败测试固定新交互**
   - 覆盖右侧只显示运行项、无折叠详情、时长、停止、点击定位展开，以及两类对话卡展开详情。
   - 验证：旧实现上的失败只命中本次需求。
3. **已完成：实现安全停止与稳定关联**
   - 复用或补齐终端/子助手停止通道，验证当前会话所有权和运行状态；补充对话卡关联信息。
   - 验证：合法停止成功，跨会话、已结束、远程越权失败关闭。
4. **已完成：重构右侧栏和对话卡**
   - 右侧终端/子助手改为两行控制项；对话内终端/子助手详情改成统一内嵌展开形态；实现跳转并展开。
   - 验证：内容、样式、交互、五语言和无障碍测试通过。
5. **已完成：真实界面验收**
   - 启动隔离 Electron、真实 server 和当前 Vite renderer；在实际桌面布局中注入只存在于该隔离进程内的运行夹具，点击终端和子助手右栏标题。
   - 验证：两项右栏均为固定两行且无折叠详情；点击后对应聊天卡 `aria-expanded=true`，终端内嵌输出和子助手正文均真实挂载，并保存收起/展开截图。停止的后端所有权和前端请求闭环由集成测试覆盖，不用假任务编号向真实 server 伪造成功。
6. **已完成：完整门禁与边界审计**
   - 运行定向、类型、lint、全量测试、renderer 构建、差异与安全审计。
   - 验证：全量 1103 文件通过、1 文件跳过，11184 项通过、7 项跳过；三段类型、renderer 构建、源码 lint、开放边界、差异格式和敏感信息扫描均通过，范围外改动为零。

## 当前假设

| 假设 | 处理 |
| --- | --- |
| “只有持续运行的终端进程出现在右侧”也适用于子助手 | 右侧两类都只列运行中；完成/停止后仅保留对话历史卡 |
| 停止按钮需要立即反映状态 | 点击后先保持真实后端状态为准，不做假停止；成功事件到达后从右侧移除 |
| 跳转目标可能尚未渲染 | 优先复用会话消息稳定标识与现有定位能力；若目标未加载则显式补载，不按标题模糊匹配 |

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 文件化计划恢复命中 2026-08-12 的旧协议块任务 | 1 | 已核对当前 diff 和计划文件；旧记录不参与本次交互结论 |
| 生成持久化指纹时清单尚未同步 | 1 | 先运行权威 store 扫描，再以“可选关联字段、旧记录可读”的兼容理由重新生成指纹；持久化测试 14/14 通过 |
| 新增后台运行状态判断首次补到普通工具行 | 1 | 新回归测试当场暴露 `terminal` 未定义；把判断移到终端命令卡，原样重跑 20/20 通过 |
| 调用了仓库不存在的多语言脚本 | 1 | 改用现有 `tests/i18n-locale-parity.test.ts`，与组件测试合计 20/20 通过 |
| 全库 lint 扫到被 `.gitignore` 排除的闭包临时副本 | 1 | 精确确认该 5.5 MB 文件未纳管，再检查本次源码与开放边界；不修改规则、不拿临时产物错误冒充源码失败 |
| 最终补强复用了任务状态权威集合，首次全量命中 2 项运行闭包差异 | 1 | 不复制状态常量；使用仓库权威生成器更新真实依赖来源，闭包 21/21 和原样全量复跑均通过 |

# 技能调用消息流卡片（2026-08-13）

## 目标与边界

- 模型每次调用技能时，在这次调用实际发生的消息流位置插入一张可展开卡片，不固定到消息顶部或底部。
- 收起态显示“已运行技能”和技能名；展开态显示技能名、调用参数与技能返回内容，视觉层级参考用户截图并沿用现有主题变量。
- 实时流与历史重载必须同构；未知旧记录保持可读，不猜造缺失内容。
- 保留本工作树中尚未提交的终端、子助手和全宽卡片改动，只修改技能调用展示必需的链路。

## 当前阶段

- **Phase 1：建立技能工具真实事件链与基线（已完成）**
- **Phase 2：用失败测试固定时序、内容和展开契约（已完成）**
- **Phase 3：实现有界结果投影和技能调用卡片（已完成）**
- **Phase 4：真实界面验证消息顺序与展开交互（已完成）**
- **Phase 5：定向、类型、构建、全量和差异门禁（已完成）**

## 阶段与验证

1. **已完成：建立事实与基线**
   - 追踪技能工具名、参数、返回内容、隐藏规则、实时块构建和历史投影。
   - 验证：明确旧界面为何完全不显示，以及消息流顺序由哪一层决定。
2. **已完成：红灯测试**
   - 覆盖技能调用不被隐藏、正文前后顺序、收起摘要、展开详情、实时/历史一致和大结果截断。
   - 验证：旧实现失败只命中本次需求。
3. **已完成：最小实现**
   - 复用现有工具消息块和通用资源卡，只新增技能专用展示与白名单结果投影。
   - 验证：技能参数和内容作为纯文本安全显示，其他工具行为不变。
4. **已完成：真实界面验收**
   - 在实际 renderer 中渲染“正文前 → 技能调用 → 正文后”，点击展开并保存截图。
   - 验证：DOM 顺序与视觉位置一致，详情可展开且卡片占满消息区可用宽度。
5. **已完成：完整门禁**
   - 运行定向测试、三段类型、renderer 构建、低并发全量、源码静态检查与差异审计。
   - 验证：定向 295 项、低并发全量 1104 文件/11200 项、三段类型、renderer 构建、修改源码与排除既有临时打包副本后的全库 lint、差异格式和新增敏感信息扫描全部通过；不跳过、不弱化断言。

## 当前假设

| 假设 | 处理 |
| --- | --- |
| 技能是普通工具调用但被界面隐藏 | 先从工具可见性规则和真实工具定义取证，不直接删除过滤条件 |
| 技能返回内容可能很大 | 复用共享结果投影的有界策略，界面不持有无界原始结果 |
| “调用位置”是正文块之间的真实先后顺序 | 用块构建测试和真实 DOM 顺序双重验证，不仅检查卡片存在 |

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 文件化计划恢复命中 2026-08-12 的旧协议块任务 | 1 | 已核对当前差异和三份计划文件；旧上下文与技能调用卡片无关，不并入本次事实 |
| 首次同时追加三份计划文件时，`findings.md` 的结尾锚点不匹配 | 1 | 补丁未产生部分写入；改为按每个文件的真实末行分别追加 |
| 扩展断线快照后，类型检查未从数组查找回调自动收窄消息条目 | 1 | 在读取消息块前增加显式条目类型判断，不放宽共享类型；295 项定向测试已通过，随后原命令复跑 |
| 更新阶段记录时发现 `PROGRESS.md` 已被另一个并行任务改成“用量统计”进度 | 1 | 不覆盖、不追加到对方的任务记录；本任务后续事实只写独立的 `task_plan.md` 与 `findings.md` 小节 |
| 全库 lint 扫到被忽略的 `.cli-closure-nft-scratch-*` 合并临时副本并报 2895 项运行环境全局量错误 | 1 | 不删除、不修改该范围外临时产物；精确排除它后对其余全库源码执行同一 lint，退出 0，同时本任务修改文件的独立 lint 也退出 0 |

# 技能卡参数语义修正（2026-08-13）

## 目标与边界

- 技能卡展开区的“参数”展示触发本次技能调用的配套用户提示词，不展示内部读取 `SKILL.md` 的文件路径。
- 保持技能卡仍处于真实调用位置，实时、断线恢复和历史重载语义一致。
- 不凭技能路径或当前页面状态猜提示词；从所属用户轮次的权威消息内容建立关联。
- 保留工作树内终端、子助手、用量统计及其他并行修改，不覆盖 `PROGRESS.md`。

## 当前阶段

- **Phase 1：核对参考图与用户轮次关联链（已完成）**
- **Phase 2：用失败测试固定提示词语义（已完成）**
- **Phase 3：实现实时/历史一致的提示词投影（已完成）**
- **Phase 4：定向、类型、构建、全量和真实界面验证（已完成）**

## 已确认的参考图语义

- 图中“参数”内容是用户提出的完整任务要求，而不是技能文件路径或读文件工具参数。
- 展开区的信息关系是：技能正文相当于本次调用采用的规则说明，用户提示词相当于套用这些规则要处理的具体任务。
- 底层路径属于实现细节，只用于识别技能名和取得技能正文，不应出现在面向用户的“参数”行。

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 文件化计划恢复再次命中 2026-08-12 的旧协议块任务 | 1 | 已通过当前 diff 和本任务计划确认无关；不继承旧结论，也不改写并行任务的 `PROGRESS.md` |
| 旧实现红灯测试 2 文件 30 项中 3 项失败 | 1 | 27 项原有测试通过；3 项只命中“直渲染仍显示路径、真实轮次未传提示词、隐藏输入仍暴露路径”，符合本次修正目标 |
| 首次绿灯复跑剩 1 项组件测试失败 | 1 | 产品时间线两项已通过；检查发现测试提示词属性误加到相邻 bash 用例，首次补丁锚点也未命中真实上下文，随后按用例标题精确移动，不改产品实现 |
| 查询主题变量时 `rg` 把以 `--` 开头的模式误当成参数 | 1 | 增加 `--` 结束参数解析后重跑，确认真实验收入口使用现有 midnight 主题，不改主题源码 |
| 最终原样全量测试 1 项样式纪律失败 | 1 | 11203 项通过、7 项跳过；唯一失败来自并行“用量统计”设置样式新增的 `120ms` 裸时长，文件与本次技能参数链路无关。保留真实失败，不改范围外代码、不更新基线、不跳过门禁 |

# 聊天消息渲染架构重构（2026-08-16）

## 目标与硬边界

- 将聊天体系从“消息数组加渲染时猜测”渐进升级为“统一助手语义事件、当前回合实时状态、确定性回合投影”的增量架构。
- 过程、答案、结果、待用户操作四种界面语义必须由显式数据决定；不按文本长度、最后一段文字或数组位置猜测。
- 实时生成完成后的结构必须与刷新重载后的结构完全一致；普通流式增量不能重建整条时间线或重渲染无关消息。
- Markdown 原文是权威数据，富文本只是派生缓存；冻结内容不重复解析或重挂载。
- 保留用户数据、旧会话兼容、终端恢复/所有权、文字选区、查找、时间线、锚点与现有结果/确认卡行为；不大爆炸重写状态仓库。
- 每个阶段先有失败测试或基线证据，再做最小实现；不使用跳过、弱断言、改超时或伪造绝对性能数字。
- 所有本任务改动和提交只落在当前 worktree 的 `chat-rendering-restucturing` 分支；不切换或合并其他分支，不访问或修改主工作区 `/Users/study_superior/Desktop/Code/LingxiAgent`，不删除任何 worktree 或分支。

## 当前阶段

- **阶段 4：结构状态与实时状态双平面（进行中）**

## 十个交付阶段与验收

1. **已完成：基线与不变量**
   - 审计真实数据流、历史路径、流式发布、渲染边界、Markdown 和终端归属。
   - 新增语义与性能测试设施，不改变产品行为；先记录当前数值，再决定最终预算。
2. **已完成：统一语义类型与稳定内容标识**
   - 为内容补充稳定标识、语义阶段、界面角色和生命周期，保留旧字段兼容。
   - 验证：长过程文字仍是过程，短最终答复仍是答案；新数据不依赖数组位置。
3. **已完成：服务端事件归一化**
   - 把不同模型来源统一成分段开始、增量、结束、工具状态和回合结束语义。
   - 验证：未决文字先进入过程区，结束后按真实阶段收口；无阶段来源不做自然语言猜测。
4. **进行中：结构状态与实时状态双平面**
   - 首次内容只添加一次助手占位，后续增量只更新当前回合；结束时一次提交。
   - 验证：1000 个普通增量期间结构列表引用稳定，无关消息渲染次数为零。
5. **待开始：回合投影与确定性收口**
   - 增量维护过程、答案、结果、控制四类内容；结束时封口、解析未决阶段并生成稳定过程组。
   - 验证：实时与重载投影深度相等；无最终答案、失败、中止、等待确认都有明确结果。
6. **待开始：过程折叠第二版与终端运行时解耦**
   - 移除长度、数量和“最后文本保护”启发式；命令和技能过程可安全折叠，结果与待操作卡保持外显。
   - 验证：折叠卡卸载不终止命令监听；右侧终端可展开所属过程组并精确定位。
7. **待开始：原文优先与增量 Markdown**
   - 新文本只保存原文；解析器给出冻结边界，已完成块保持缓存和页面节点身份，流式与收口采用不同富渲染策略。
   - 验证：冻结块不再解析，只有活动尾部更新；文字选区测试继续通过。
8. **待开始：语义感知发布策略**
   - 普通增量按页面刷新节奏合并，阶段边界和回合结束立即发布，并限制最高发布频率。
   - 验证：同一帧最多一个待发布任务，记账事件不触发消息渲染。
9. **待开始：历史重载减负与渲染窗口**
   - 思考、大工具结果和内联媒体改为预览与按需读取；先实现稳定锚点的可增量渲染窗口，不直接上完整虚拟化。
   - 验证：初始历史载荷和页面节点数有实测下降，向上加载、选区、查找、定位不回退。
10. **待开始：类型化渲染注册表、兼容收口与全量验收**
    - 收敛中央分支，短期功能开关和双投影对照稳定后删除旧启发式与新数据旧字段。
    - 验证：真实回归夹具、定向测试、三段类型、构建、低并发全量、差异与敏感信息检查全部有真实退出码。

## 已知约束与决策

| 决策 | 原因 |
| --- | --- |
| 不覆盖仓库原有计划历史，所有本任务记录追加独立章节 | 工作区含多次已完成任务的事实记录，清理会破坏交接语义 |
| 本任务进度使用 `chat_rendering_progress.md` | 当前文件系统不区分大小写，技能默认 `progress.md` 会碰撞已有 `PROGRESS.md` |
| 先复核当前源码，再把 2026-08-13 技能卡和终端结论纳入设计 | 这些记忆能给出不能回退的产品边界，但文件位置与实现可能已经变化 |
| 阶段 1 观测器默认关闭、测试显式启用 | 既能给后续阶段提供同口径数据，又不改变产品状态或显示行为 |

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 新建目标失败：当前任务已有未完成目标 | 1 | 读取目标后确认它就是附件任务，直接续跑 |
| `progress.md` 在当前文件系统上覆盖了已有 `PROGRESS.md` | 1 | 对照基线完整恢复原 37 行文件，改用不会撞名的 `chat_rendering_progress.md`；恢复后该文件差异为空 |
| 历史常用的 `/opt/homebrew/opt/node@24/bin/*` 路径不存在 | 1 | 当前 PATH 已提供满足仓库约束的 Node 24.16.0，不再探测该旧路径 |
| 首次读取流式 Markdown 组件误用 `components/StreamingMarkdownContent.tsx` | 1 | 以检索结果改读真实的 `components/chat/StreamingMarkdownContent.tsx`，不重复错误路径 |
| 读取不存在的概念文件 `terminal-runtime-slice.ts` | 1 | 当前真实文件为 `stores/terminal-slice.ts`，后续以源码为准，不把任务书建议名称当成现状 |
| 首次相关基线测试在配置加载阶段失败：找不到 `vitest` 包 | 1 | 退出发生在任何测试执行前，不作为产品基线；先核对当前 worktree 依赖目录与锁文件，再按现有锁文件恢复依赖 |
| 阶段 2 首轮定向测试有 3 项旧精确形状断言失败 | 1 | 实际差异只包含新 `id/lifecycle/semanticPhase/surfaceRole`；更新断言为完整新契约，不删除或放宽原有字段检查 |
| `AssistantMessage.interlude` 套件加载时找不到 `@lingxi/plugin-protocol` 入口 | 1 | 测试未执行；单独核对工作区包声明与标准构建命令，不把加载失败当成产品结果 |
| 阶段 2 首次类型检查提示插件卡详情没有通用 `id` | 1 | 不扩张或猜造插件协议；读取真实详情类型，使用现有稳定字段组合，缺失时回退同类型序号 |
| 阶段 3 首轮契约测试因统一事件模块尚未存在而在加载阶段失败 | 1 | 作为先测后改的红灯证据；实现独立归一化层后原样重跑，6 项纯逻辑契约全部通过 |
| 新统一分段事件进入续传序列后，旧会话改名用例仍假设正文增量序号连续为 1、2 | 1 | 不隐藏新事件；强断言完整分段与旧正文交错序号、共享流标识和改名后路径，相关 50 项路由回归通过 |
