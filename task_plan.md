# LingxiAgent 版本事故修复执行计划

## 目标

查清并修复 `0.1.3` 版本倒退导致的运行时未切换风险，验证下游功能完整性，补齐运行时可观测性与发布硬门禁，并用自动化升级场景证明受影响用户可无损自愈。

## 硬边界

- 源码、Git 历史、现有测试和真实发布链路优先于任务书中的事故假设。
- 不删除用户配置、整个 Artifact 目录或历史回滚数据。
- 不使用事故版本特判；修复必须覆盖正常升级、重装、旧安装包、旧指针、OTA、损坏指针和崩溃回退。
- 不顺手修改范围外代码；无法安全裁决的事项写入 `BLOCKED.md` 后继续其他阶段。
- 每个阶段都记录可复跑命令与真实结果，不用跳过或弱断言掩盖失败。

## 阶段

1. **已完成：建立事实与事故复现**
   - 读取完整任务书、现有进度和仓库规则。
   - 记录分支、提交、工作区、版本、标签、SDK 与发布历史。
   - 建立版本传播图，定位所有版本比较与指针决策。
   - 用现有逻辑复现 `current 0.1.22 + seed 0.1.3`。
   - 验证：形成从输入、决策、指针到启动目录的证据链。
2. **已完成：下游功能与上游同步审计**
   - 建立同步前 downstream、上游目标、当前 main 的三方比较。
   - 覆盖模型、辅助模型、语义槽位、审批、网关、提供方、Ollama、技能、记忆、通信、持久化和更新系统。
   - 验证：生成 `UPSTREAM_SYNC_AUDIT.md`，每项有文件、测试或 Git 证据。
3. **已完成：版本与 Artifact 选择修复**
   - 确定实际最高正式版本，选择严格递增的修复版本。
   - 分离产品展示版本与发布先后顺序，保留 OTA 和崩溃回退语义。
   - 验证：覆盖任务书列出的 seed 选择性质与五个事故迁移场景。
4. **已完成：运行时可观测性与兼容保护**
   - 接入现有日志或诊断，报告 Shell、renderer、server、来源、train 与发布来历。
   - 对明显危险组合显式告警或进入已有恢复路径。
   - 验证：正向、负向测试均能观察到确定结果。
5. **已完成：发布防复发门禁**
   - 正式版本倒退、关键 Artifact 或升级 smoke、seed 校验和发布完整性失败时阻断所有正式发布与指针更新路径。
   - 验证：本地执行等价 CI 命令，负向夹具确实失败且阻断链路可证。
6. **已完成：全量验证与交付（发布摘要外部阻塞已单列）**
   - 运行定向测试、全量测试、类型检查、构建或打包相关校验、差异与白名单审计。
   - 更新 `PROGRESS.md`、`BLOCKED.md` 和必要的版本规范文档。
   - 验证：每项成功标准有命令、退出码和产物证据；未完成项明确阻塞原因。
7. **已完成：解除发布摘要阻塞并复核发布输入**
   - 已完成来源提交和 GitHub 输入复核；按用户指定把生成路径改为 `DEEPSEEK_API_KEY`、`deepseek-v4-flash` 和 DeepSeek Responses API。
   - 先以官方文档和真实契约确认端点/请求/响应，再以负向测试证明缺密钥、错误响应、非法摘要和版本不匹配均失败关闭。
   - 摘要必须经过仓库生成器和两份校验，不手改、不绕过；任何密钥只从环境读取且不得写入文件或日志。
   - 验证：摘要校验退出 0；预检、差异检查和关键测试保持通过。
8. **已完成：提交并推送 main**
   - 只暂存本事故修复、测试、生成清单和交付报告，提交后回读 SHA 与远端状态。
   - 验证：`origin/main` 指向新提交，工作区干净。
9. **已完成：创建并推送 tag，监督正式 Release 链**
   - `v0.1.23` 已不可变地指向 `1f24a039` 并触发 Build；远端 Linux 质量门禁暴露两组平台依赖测试夹具，Release 尚未创建。
   - 不移动、不删除、不强推既有 tag；修复跨平台测试后使用 `v0.1.24`、发布世代 2 继续正式发布。
   - `v0.1.24` 已精确指向 `480603cb`；GitHub Release 非草稿、非预发布、成为 Latest，18 个资产通过工作流硬门禁与独立复核。
   - stable train 3、beta train 6 的签名、来源提交、发布世代、版本、文件大小和摘要均已独立复核通过。
   - AtomGit 外部镜像连续在首个 442 MB 文件上传回执处超时，不影响 GitHub single source of truth 与 signed Train；平台阻塞和两次真实运行已单列到 `BLOCKED.md`，没有伪装成成功。

## 初始假设的最终裁决

- 已证实：`0.1.3` 的 Git 和已发布归档都含新功能，实际运行 seed 因标准版本小于 `0.1.22` 而未激活。
- 已证实：原有 train 只在单频道内单调，manifest 没有跨安装包/频道的发布世代；因此新增最小 `releaseGeneration`，并复用既有签名与激活链。
- 已定案：实际历史最高产品版本为 `0.1.22`；首次修复候选 `0.1.23` 因 Linux 测试夹具问题只留下失败 tag、没有 Release，最终正式修复版本为严格更大的 `0.1.24`、发布世代 2；摘要已通过双校验。

## 错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 首次补丁无法匹配 `PROGRESS.md` 结尾，未产生部分写入 | 1 | 分开创建计划文件，再按实际结尾追加旧进度 |
| `gh release list` 使用了不支持的 `url` JSON 字段 | 1 | 保留成功的远程标签证据，下一次改用工具列出的字段重查 Release |
| 首次全仓 Artifact 关键字检索误扫 `desktop/dist-renderer`，输出被截断 | 1 | 已保留文件入口清单；后续只检索源码扩展名并显式排除所有生成目录 |
| 首次事故复现脚本调用了未导出的内部版本比较函数 | 1 | 改为只调用模块公开的启动决策入口，成功得到 `boot 0.1.22` 结果 |
| zsh 循环的 A/B/C Git 对象分类把所有路径误判为 missing | 1 | 结果已作废；改用显式 `bash` 和逐对象检查重跑，并先用 `package.json` 做阳性校验 |
| GitHub `0.1.3` 发布资产下载遇到临时链接 EOF，后续命令因未启用 fail-fast 仍返回 0 | 1 | 整次产物抽检作废；改为单资产下载并启用 `set -e`，失败时不执行解包和结论 |
| 首次 `publish-train.mjs` 组合补丁上下文不匹配，未产生部分写入 | 1 | 按真实 import、组装和入口位置拆成小补丁完成 |
| 版本预检负向脚本把 zsh 只读变量 `status` 当作退出码变量 | 1 | 结果未用于结论；改名 `preflight_code` 后确认负向退出码为 1 |
| 三包版本读取直接 `require(package/package.json)`，其中一包禁止导出该子路径 | 1 | 改为只读实际安装目录中的 `package.json`，三包均确认 0.84.1 |
| 首次类型检查被 `node_modules/@types/* 2` 重名目录污染，未进入源码检查 | 1 | 按 lockfile 执行 Node 24 `npm ci`，随后原命令 0 error |
| release digest 生成器需要 `OPENAI_API_KEY`，当前环境未配置且改动尚未提交 | 1 | 遵守规则不手改摘要；记录到 `BLOCKED.md`，让发布校验继续阻断 tag |
| 首次本地 seed 构建命令因包含 shell `rm -rf` 清理被执行器安全规则拒绝，未启动构建 | 1 | 改为只删除经 `/tmp/lingxi-seed-key.*` 前缀校验的临时目录；构建、验签通过并确认私钥目录已移除 |
| 最终秘密扫描把计划中记录的临时目录命名规则误报为秘密，检查脚本退出 1 | 1 | 去掉非秘密的目录模式，只扫描真实私钥头和一次性 key id 后重跑 |
| `npm audit` 与 `npm audit --omit=dev` 因发现风险退出 1 | 1 | 不掩盖退出码；逐项确认可兼容升级的直接依赖并更新，回归后重新审计 |
| `npm outdated --json` 因存在可更新包退出 1 | 1 | 结果仅作升级范围取证，不当成测试失败；只选择修复 critical/high 所需的兼容版本 |
| `npm audit fix --package-lock-only --ignore-scripts` 因仍有中低风险退出 1 | 1 | 安全更新已写入 lockfile；拒绝 `--force` 提供的降级/主版本跳跃，最终残余风险单独留档 |
| 安全升级后的首次全量测试出现大量装载失败，类型检查也提示缺少 `yauzl` | 1 | 最小样本确认根因是生产代码借用了打包工具的传递依赖；改为直接声明 `yauzl` 与类型包，81 项相关用例恢复通过 |
| Electron 补丁升级后 shell 表面清单仍声明旧版本，守卫 1/41 失败 | 1 | 同步受守卫管理的版本字段；不修改守卫断言，等待全量回归确认 |
| v2 摘要复核误传不存在的 `--schema` 参数，退出 1 | 1 | 结果只证明参数错误，不用于门禁结论；读取校验器后按文件内容自动识别的真实接口重跑 |
| 正式摘要生成在来源采集成功后因缺少 `OPENAI_API_KEY` 退出 1 | 2 | 源码提交 `cf0be5bc` 与来源包已就绪；停止推送/tag/Release，等待用户把任务所需凭据配置到当前环境 |
| planning-with-files session catchup 命中无关的旧技能/MCP 问答 | 1 | 与当前仓库、摘要和发布无关，不合并进计划或代码事实 |
| 首次官方 Responses 页面正文提取为空 | 1 | HTML 解析器错误地按所有闭合标签递减 article 深度；未形成契约结论，改为只跟踪 article 起止后重跑 |
| 修正解析器后正文仍为空 | 2 | 不再重复解析假设；改查 HTTP 状态、重定向和实际响应字节，再从 Docusaurus 页面数据或官方参考提取契约 |
| DeepSeek 迁移测试首次运行 7 项失败 | 1 | 失败均准确命中旧 OpenAI 默认值、旧导出和缺失的安全检查；保留为红灯证据后实现迁移 |
| DeepSeek 改动后首次 lint 报 959 errors | 1 | 全部来自被忽略的 5.7 MB 构建临时副本 `build/.cli-closure-nft-scratch-nft-server-bundle 2.mjs`；先核对出生时间、忽略状态和生成入口，再只移除该临时文件重跑 |
| 临时终端首次静默读取把下一条 export 命令当成输入 | 1 | 没有使用错误值发请求；改为分两次写入，先等待静默输入状态，再单独提交密钥 |
| DeepSeek 首次真实生成返回完成状态但本地 JSON 解析失败 | 1 | 响应同时含思考过程和最终正文，旧提取器拿了首个任意 text；摘要未落盘，先补思考项在前的复现测试，再只提取 `output_text` |
| 最终全量测试的 JSON reporter 未生成指定报告文件 | 1 | 该次没有最终统计，不能当作通过证据；改用仓库原始 `npm test`、日志文件和显式退出码重跑 |
| `v0.1.23` 远端 quality-gate 在 Linux 失败 8 项 | 1 | 6 项路由测试默认使用宿主平台、2 项引擎测试只在本机 macOS 成立；保留 Linux 不支持的生产行为，改为在正向夹具中显式指定 darwin |
| `v0.1.24` 事实约束摘要首次生成 incomplete | 1 | DeepSeek 在 4000 输出额度内耗尽并返回 `max_output_tokens`；生成器按设计未落盘，增加请求额度契约测试后提升到 8000 |
| 额度提升到 8000 后事实约束摘要仍 incomplete | 2 | 官方 Responses 契约确认默认启用思考且额度同时计算推理/正文；摘要任务改为 `reasoning.effort=none`，保留 8000 正文上限 |

# 供应商配置与模型目录状态漂移修复（2026-08-09）

## 目标与边界

- 以现有供应商配置目录和运行时模型目录为权威源，修复设置左栏、删除生命周期、辅助模型选择器之间的状态漂移。
- 保持内置供应商可重新选择、自定义供应商既有删除语义、视觉能力过滤和完整模型引用不变。
- 不新增第二套持久化列表或事件，不用浏览器缓存、轮询、延迟刷新、远端模型发现掩盖内部同步问题。
- 所有写操作失败关闭；只修改彻底修复与回归测试必需的代码，不升级依赖、不改版本、不发布。

## 阶段

1. **已完成：建立基线与状态流审计**
   - 记录提交、分支、工作区、Node/npm；运行供应商设置、模型同步、应用事件、辅助模型和类型检查基线。
   - 追踪供应商成员、当前选择、临时草稿、配置写入、运行时模型投影和前端订阅的完整数据流。
   - 验证：在 `findings.md` 和 `PROGRESS.md` 写清 BUG 1/2/3 的具体断点，改代码前先形成裁决。
2. **已完成：用失败测试固定正确契约**
   - 覆盖持久供应商重载、最后选择不定义成员、草稿增删、已配置删除、内置项回候选、写失败关闭。
   - 覆盖辅助模型共享来源、已挂载刷新、删除刷新、视觉过滤、同名模型跨供应商区分。
   - 验证：新增断言在旧实现上命中真实缺陷，保留红灯证据。
3. **已完成：修复供应商成员与删除状态模型**
   - 从权威摘要恢复持久项；把页面临时草稿与持久项分离；成功删除后由权威状态收敛，失败不假删。
   - 验证：P1-P6、D1-D5 对应定向测试通过。
4. **已完成：收口运行时模型状态与写操作错误处理**
   - 让辅助模型复用统一前端模型状态；保证配置变更到运行时刷新和事件传播失败关闭。
   - 审计所有本任务涉及的供应商/模型写路径，只有服务端成功后才刷新和提示成功。
   - 验证：M1-M6、T7-T12 与服务端路由测试通过。
5. **已完成：全量验证与对抗复审**
   - 运行定向测试、类型检查、全量测试、差异检查和白名单审计。
   - 复查生命周期、快速连续操作、权威源边界，以及远端模型发现只承担 discovery。
   - 验证：对比基线无新增回归；阶段记录、阻塞和交付清单完整。
6. **已完成：真实界面点击复现与二次修复**
   - 启动真实服务和渲染界面，用 Playwright 实际点击供应商模型与辅助模型选择器，记录网络、事件、控制台和截图证据。
   - 先在当前实现上复现用户报告，再沿真实窗口通信链定位单元测试遗漏的断点，只做最小修复。
   - 验证：隔离 Electron 中实际点击新增、删除、重新添加并选择辅助模型；无需重开设置或手动“读取模型”即可更新已挂载选择器。定向、类型、lint、低并发全量和差异检查均通过；默认并发全量的既有发布预检超时另行保留真实退出码。

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| 首次绿灯定向运行剩 2 项供应商测试失败 | 1 | 两项旧夹具仍假设“草稿没有移除按钮”和“仅设置 selected 就产生成员”；按已确认的新契约改为期待草稿删除入口，并通过 Picker 创建草稿，不修改实现来迁就旧假设 |
| 首次写路径检索命令因双引号内含反引号导致 zsh 报 unmatched quote | 1 | 该检索没有产生结论；改用不含 shell 反引号的固定字符串查询重跑 |
| 扩大定向回归后 AgentTab 旧测试仍用组件私有 `/api/models` mock 喂数据 | 1 | 生产组件已改读设置窗口共享目录；测试改为直接布置同一共享状态，验证文案和 provider 图标契约不变 |
| 状态源复核命令包含不存在的历史路径 `server/routes/agent-config.ts` | 1 | 该路径未参与结论；从实际 `settings-snapshot.ts` 与 `agents.ts` 入口确认配置快照确实投影 Provider Catalog |
| Onboarding 供应商步骤直接点击“下一步”的首个 Playwright 命令未返回页面输出 | 1 | 不重复盲点；重新侦察当前页面和窗口，确认是表单校验、窗口切换还是命令等待，再选择最小完成路径 |
| 选择本地 Ollama 后继续的组合脚本再次超过短等待窗口 | 2 | 服务日志只显示上一阶段记忆配置写入；拆成单次点击与只读页面检查，不再在一个命令里等待整段 onboarding 流程 |
| 触发最小 onboarding 完成后旧页面等待报 `TargetClosedError` | 1 | 这是 onboarding 窗口按设计关闭造成的目标切换；不当作产品失败，重新连接调试端口获取新主窗口 |
| 首次“添加模型后打开辅助下拉”的长组合脚本未在工具等待窗口内结束 | 1 | 页面已实际推进到“配置模型”，拆开核对持久目录、运行时接口和当前辅助列表；后续正式用例使用分段断言与无缓冲输出 |
| 分段点击“添加模型”后 portal 未在 3 秒内保持可见 | 1 | 后端核对证明此前模型未写入；先显式滚动控件并等待布局稳定，再点击并立即断言 portal，区分自动滚动关闭与产品点击失败 |
| 修复后界面脚本等待供应商文本触发 Playwright strict violation | 1 | 页面同时有左栏名称和右栏标题，产品写入已 200 成功；选择语义明确的右栏 heading，不放宽成模糊匹配 |
| 改用 heading 后命中一个隐藏的同名标题并超时 | 2 | 页面详情和“添加模型”按钮实际可见；不再等待不参与操作的标题，直接以可见操作按钮为前置条件 |
| 修复后首次 portal 等待仍因打开态翻转而超时 | 2 | 每次操作前先检查 portal 当前状态，关闭则点击一次，随后立即断言；不再假设前一脚本结束时一定关闭 |
| 首个热刷新回归用例使用可访问形态的 HTTPS 地址时错误转绿 | 1 | 改为界面复现所用的不可访问本地地址后稳定命中空目录；证明地址条件会放大竞态，保留真实条件作为测试夹具 |
| 并行重复红灯测试的失败差异展开了上千个内置模型，输出被截断 | 1 | 结论只取退出码与缺失目标模型；修复后使用点状 reporter 重跑 8 份，全部退出 0 |
| 界面脚本用相对 `/api/models` 诊断时读到 Vite 的 HTML 并 JSON 解析失败 | 1 | 不把它当产品接口结果；改为捕获产品自身带连接信息发出的 `/api/models` 请求，确认返回目标模型 |
| 首次用原始模型 id 检查下拉文本得到假阴性 | 1 | 下拉按设计显示格式化名称而不是原始 id；改为同时检查供应商分组和格式化名称，旧实现的空下拉仍能明确区分 |
| 关闭辅助下拉时 `Escape` 同时关闭了设置弹层 | 1 | 不再依赖全局 Escape 切页；通过触发按钮切换 portal，必要时重新打开设置并重新建立监听前置条件 |
| 删除后重新添加时，滚动后立即点击“添加模型”未保持 portal 打开 | 1 | 先等待滚动完成，再单独点击并立刻读取 portal；该失败只属于自动化节奏，未产生写请求或产品结论 |
| 全量默认并发两次均有发布预检用例超过自身 10 秒上限 | 2 | 两次均为 11040 通过、7 跳过、仅 1 项超时；该文件单跑 6/6 通过且耗时 4.28 秒，低并发完整测试集 11041/11041 通过。不修改测试上限或伪装默认命令退出码 |
| 首次差异秘密扫描把新增测试中的显式假密钥夹具判为命中 | 1 | 命中值只存在测试且明确为虚构数据；改为扫描真实界面测试标识和临时目录标识，结果为空，`git diff --check` 单独退出 0 |

# skillBadge 消息持久化修复（2026-08-10）

## 目标与边界

- 修复带技能卡片的用户消息在会话重载、应用重启、重试与分叉后丢失技能展示信息的问题。
- 只补齐现有消息展示记录的写入、历史投影和重试/分叉还原；不改提示词拼接、实时事件和前端渲染。
- 保留工作区中已有的供应商配置相关未提交修改，不回滚、不格式化、不顺手清理。

## 阶段

1. **已完成：建立事实与现有测试基线**
   - 核对三处断点、类型定义、展示记录配对逻辑和相关测试入口。
   - 验证：改代码前形成可定位的源码与测试证据。
2. **已完成：用失败测试固定持久化契约**
   - 覆盖展示记录写入、历史消息投影、重试/分叉还原技能列表。
   - 验证：旧实现上的失败与报告的三处断点一致。
3. **已完成：最小实现修复**
   - 三处按现有结构化展示字段的惯例补齐技能列表。
   - 验证：失败测试转绿，不改变无技能消息行为。
4. **已完成：回归与差异审计**
   - 运行定向测试、三段类型检查、差异检查和修改白名单审计。
   - 验证：真实退出码为 0；范围只包含本 Bug 必需文件及计划记录。

## 本任务错误记录

| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| `planning-with-files` 会话恢复命中无关旧问答 | 1 | 不纳入本 Bug 事实；保留当前工作区已有改动，另起独立计划小节 |
| 新增纯技能守卫测试沿用邻近夹具的 `as any`，目标文件 lint warning 从 182 增至 183 | 1 | 改用对象合并挂载测试替身，避免本任务新增静态检查警告 |
| 低并发全量测试经 Node 24 调用 npm 后，npm 子进程仍按系统路径使用 Node 22，155 项失败 | 1 | 失败统计保留但不用于源码结论；不重建 Node 24 原生依赖，改为用 Node 24 直接启动 Vitest |
